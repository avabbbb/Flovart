services:
  - type: web
    name: flovart
    runtime: static
    buildCommand: npm ci && npm run build
    staticPath: /dist
    pullRequestPreviewsEnabled: true
    routes:
      - type: rewrite
        source: /*
        destination: /index.html