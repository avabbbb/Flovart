# Flovart 安装与使用指引

本指引面向**不熟悉电脑操作**的使用者。全程不需要安装任何额外软件、不需要打开命令行。
安装包文件名：`Flovart_0.3.1_x64-setup.exe`（约 9.8MB）。

---

## 一、安装前准备

- 一台 Windows 10 或 Windows 11 的电脑（64 位）。
- 一个 AI 服务的 API Key（第一次打开软件时需要，向导里每个服务商都附了获取链接，Google Gemini 可免费申请）。
- 不需要其他任何东西。

## 二、安装步骤（约 2 分钟）

1. 双击安装包 `Flovart_0.3.1_x64-setup.exe`。
2. 如果弹出蓝色「Windows 已保护你的电脑」窗口——**这是正常的**（软件还没购买微软的数字签名证书），请按下面操作放行：
   - 点击「更多信息」
   - 点击「仍要运行」
3. 语言选择「简体中文」（默认即是）。
4. 一路点击「下一步 / 安装」，不需要改任何选项，不会要求输入管理员密码。
5. 安装完成后，桌面会出现 **Flovart** 图标，双击即可打开。

## 三、第一次使用（配置 AI 服务）

1. 首次打开会进入「欢迎向导」。
2. 选择你的服务商（如 Google Gemini、DeepSeek、OpenAI 等）。
3. 把 API Key 粘贴进去，点击「验证并保存」。
4. 每个服务商选项下方都有「获取方法」链接，按提示去对应官网申请即可。
5. 验证通过后进入主界面，就可以开始生成图片、视频、搭建工作流了。

## 四、日常使用与更新

- 打开软件时会**自动检查更新**；有新版本会提示，按提示即可后台完成升级，不需要重新安装。
- 也可以随时点击顶栏的「检查更新」按钮手动检查。

## 五、常见问题

| 问题 | 解决办法 |
| --- | --- |
| 双击后被 SmartScreen / 杀毒软件拦截 | 安装包来自官方渠道就是安全的，点「更多信息 → 仍要运行」放行 |
| 打开后闪退 | 确认系统是 Windows 10/11 64 位；重启电脑后再试 |
| 忘记或更换 API Key | 在主界面进入「设置」，重新填写并验证即可 |
| 想卸载 | Windows「设置 → 应用 → 已安装的应用 → Flovart → 卸载」 |

---

## 六、给分发者的说明

- 本次构建产物：`src-tauri/target/release/bundle/nsis/Flovart_0.3.1_x64-setup.exe`。
- 正式发布流程：打 `git tag v0.3.1` 并推送，GitHub Actions（`.github/workflows/build-desktop.yml`）会自动构建安装包、生成更新签名与 `latest.json` 并发布到 GitHub Releases；老用户打开软件即可自动更新。
- 安装器采用「当前用户」模式（`installMode: currentUser`）：无需管理员权限、不弹 UAC、卸载干净，最适合分发给普通用户。
- 若要消除 SmartScreen 首次拦截，需要代码签名证书（OV 证书或 Azure Trusted Signing），在 `src-tauri/tauri.conf.json → bundle.windows.certificateThumbprint` 填入指纹后重新出包。
