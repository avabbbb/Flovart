---
title: 后续待办
---

本页只保留尚未完成的工作。已经实现但仍需用户确认的变更统一记录在 [待测试确认](pending-test.mdx)，正式能力在用户确认后再进入 [功能特性](../overview/features.mdx)。

## Table：Agent 故事版与媒体预处理工作台

Table 是 Workflow 之外的第二个正式创作工作区，不是电子表格，也不是旧 Canvas / Art 的改名。当前入口已经接入，但主体仍为占位界面。

### 信息架构与渐进披露

- **统一图 + 焦点面板**：⬜ 故事卡、媒体和处理节点共用一张项目图；首层只展示「故事版」与「媒体预处理」两类任务，选中节点后再披露紧凑参数，复杂抠图或逐帧视频处理进入焦点编辑。
- **Agent 故事版**：⬜ 支持 Brief、段落、场景、镜头和分镜卡片节点；Agent Skill 可生成、拆解、排序、修订和解释故事板。
- **媒体预处理**：⬜ 支持源图片、源视频、处理节点和产物节点；默认流程是“选媒体 → 选处理 → 调整关键参数 → 预览 → 保存产物”。
- **紧凑扁平 UI**：⬜ 降低边框、阴影、胶囊和常驻面板数量；工具属性靠近当前节点，批量与高级参数按需展开。
- **弹性反馈**：⬜ 拖放、连线、展开、任务启动、成功回填和失败恢复使用 `motion` spring；动效只解释状态变化，不持续占用注意力。

### 节点与数据模型

- **Table 项目状态**：⬜ 定义 Table Project、Table Node、Connection、Viewport、Selection 与 History；业务数据使用 `localforage` 持久化。
- **节点图底座**：⬜ 在统一图前提下比较复用现有 Workflow 图层与引入 React Flow 的成本，确认后只保留一套 Table 渲染与交互底座，不再增加内部子画布。
- **故事版节点**：⬜ 实现 Brief、场景、镜头、分镜卡、参考素材和备注节点；支持排序、分组、批量选择与 Agent 建议。
- **预处理节点**：⬜ 实现媒体输入、处理操作、预览和产物节点；参数和运行状态归节点所有，不依赖全局弹窗堆叠。
- **产物来源关系**：⬜ 每个预处理产物保存源 Artifact、处理节点、参数摘要和创建时间，可进入素材库并作为 Workflow 参考输入。
- **撤销与恢复**：⬜ 图操作走轻量 History；长时媒体任务使用持久 `taskId`、取消、失败重试和结果恢复，不把二进制塞进撤销栈。

### 媒体预处理能力

- **人物与主体抠图**：⬜ 自动抠图、边缘修正、透明背景预览与导出；保留手动修正入口。
- **深度图与深度视频**：⬜ 图片深度估计；视频逐帧解码、批处理、进度、取消和重新编码，避免阻塞主线程。
- **结构参考提取**：⬜ 边缘、线稿、法线和姿态图提取，产物可直接作为后续生成参考。
- **风格化与故障滤镜**：⬜ 复古 DV、VHS、胶片、RGB 分离、扫描线、噪点和撕裂等可调预设，支持原图/结果对比。
- **服装修改准备**：⬜ 通过人物分离、区域遮罩、身份参考与服装参考组织，为后续 AI 编辑提供结构化输入；AI 生成本身仍由已验证 Provider Route 执行。
- **全能参考准备**：⬜ 把人物、场景、道具、动作和风格素材整理为带角色语义的参考集合，再交给 Workflow 的多模态生成节点。
- **视频处理管线**：⬜ 明确 WebCodecs / ffmpeg.wasm / Worker 的职责和降级边界；大文件必须支持进度、取消、内存上限和失败恢复。

### Agent 与 Workflow 互通

- **Table Agent Skill**：⬜ 复用现有 Agent 对话和 Tool Calling 基础，定义只操作 Table 项目与节点的类型化工具；不得复活 Canvas / Element 命令。
- **Workflow → Table**：⬜ 从 Workflow 选中图片或视频，创建带来源关系的 Table 输入节点，而不是复制无来源临时文件。
- **Table → Workflow**：⬜ 将选中产物保存到素材库，并一键创建或更新 Workflow 参考节点。
- **Canonical Command Registry**：⬜ Table 自动化能力只有在命令 Schema、实现与测试同时落地后才能写入 Skill；当前 CLI 不提供 Table 命令。

## Workflow 交互与能力收口

- **删除 Config 节点**：⬜ 单独评估现有模型/参数入口后移除冗余 Config 节点，避免先删入口再补交互。
- **Scene Prediction**：⬜ 基于首尾帧能力实现向前或向后续写镜头，并明确抽帧产物与新视频节点的来源关系。
- **音频生成**：⬜ 等 Provider 层提供已验证音频生成能力后启用；当前只保留音频上传、播放、引用和配置元数据。
- **布局规范统一**：⬜ 顶栏、侧栏、底部 Dock、外边距、圆角、阴影和主题 token 统一；清理 Workflow 硬编码间距和重复右侧面板实现。

## 制作 Runtime

- **统一 Runtime dispatch**：⬜ Runtime 查询、Task 查询/取消、Event 续读、`runtime.test.delay`、RunningHub GPT Image 2、Grok Imagine Video 1.5 低价路线和 Veo 3.1 Lite 已由唯一 Desktop Runtime 执行；继续迁入其他已验证 Provider Route，迁完后删除双轨执行面。
- **跨入口本地数据桥接**：⬜ Web 站点、Tauri WebView 与 Edge 扩展的 IndexedDB 默认按 origin 隔离；通过受限 Desktop Runtime API 共享 Workflow / 素材 / 历史等业务数据，API Key 只允许本地鉴权后调用，不向扩展返回明文。
- **Provider Task 完整语义**：⬜ 首条 RunningHub Route 已保存外部 job ID、轮询事件和 Artifact；继续补齐 ProviderAttempt、提交不确定态、真实百分比进度、失败分类与远端取消确认。
- **原子能力面**：⬜ 补齐 Provider Job、Artifact、Node、Render 与 Verify 的封闭类型 Atomic Runtime Command；Production Intent Command 由 Runtime 展开为 ProductionRun 与 StageRun。
- **Provider-neutral 能力**：⬜ Agent、CLI、MCP 与 WebUI 只请求产品能力，Provider Adapter 独占路由 payload、凭据和提交生命周期。

## Flovart Agent V1

- **类型化制作工具**：🟨 PI Kernel 已注册 15 条受限的可见 Workflow 查询与编辑工具，并打通真实工具事件、Workspace Adapter 和会话恢复；继续补素材/Runtime 查询、方案修订提案、Production Intent 与 Specialist Capability。不开放 Shell、任意文件和任意网络工具。
- **流式工具调用**：⬜ 在 Runtime `agent-text` 归一化协议中补齐 tool-call delta、结构化错误、断流重试和超时分类，并验证多轮 Tool Result 回灌。
- **主对话与探索分支**：⬜ 保留一条可恢复主对话；探索分支未提升前不得创建正式 Spec Revision 或 Mandate。
- **Production Plan Card**：⬜ 把目标产物、可选 Production Skill、Route Plan、预算和审片点投影为一张制作方案卡；“确认并开始”原子创建 Mandate 和 Run，普通聊天文本不得授权付费。
- **固定 Node Runtime**：⬜ 正式 Desktop 安装包捆绑满足 PI 要求的 Node `>=22.19.0`，Release Manifest 锁定 Node、PI、Agent 协议和 Session Schema；源码开发模式才允许使用合规系统 Node。
- **Managed Agent 生命周期**：⬜ Desktop 正常退出时必须同步终止其托管的 Managed Agent 子进程；实机验收发现源码开发模式关闭窗口后 Node 子进程仍会存活。
- **Agent 空间默认布局**：⬜ 新增 Codex 子任务时按主面板边界计算空位，避免默认位置大面积遮挡 `Flovart Agent`；仍需保留用户拖动后的空间布局。

## ProductionRun 与 Production Skill 生态

- **Production Skill Manifest**：🟨 已用 `community.vox-director` 内置 VOX Skill 示例落地版本、ProductionSpec 扩展 Schema、Runtime Capability、人工 Gate、兼容版本、来源与权限声明，并由 Skill 台读取同一真实包；继续补第三方包导入、签名、锁定快照、发布、撤销与预算策略。Production Skill 不得读取 API Key 或直接构造 Provider HTTP 请求。
- **制作领域模型**：⬜ 建立 `ProductionRun → Shot → Take → Artifact → Delivery`，将当前多个独立生成任务和外部合成脚本收进一条可恢复、可审计的制作图。
- **报价与批准状态机**：⬜ 将 `production.quote`、预算策略/用户批准与 `submit` 分开，支持单镜头和整片费用上限；价格预览与最终账单分别记录。
- **一等后期命令**：⬜ 注册旁白、字幕、混音、合成、响度和交付验证命令，使 Coding Agent 不再依赖评测目录的临时脚本。
- **图生视频一致性流程**：⬜ 先做 style bake-off、角色参考和静态关键帧，再以 image-to-video 生成镜头；把跨镜头角色、调色和品牌污染检查设为 Gate。
- **第三方 Skill 安全**：⬜ 增加签名、网络/Provider/文件权限、Secret 隔离、费用配额与审计日志，之后再开放 Production Skill UGC 安装与发布。
- **Research Adapter 正式来源**：⬜ 为 X 接入经过认证、可审计且遵守来源条款的正式适配器，并补限流、游标、去重和来源级健康度；不得用搜索摘要冒充 X 原帖。Reddit RSS 已完成 tracer-bullet，但仍需加入票数/评论数的正式元数据来源。
- **TUI 制作控制台 V2**：⬜ 在保持 CLI 为唯一命令入口的前提下，增加 Task drill-down、价格批准、Artifact 预览、Agent token stream 和失败镜头重做；TUI 不保存第三份 Runtime/Workflow 状态。
- **运行态 Workflow Projection（当前基线）**：🟨 `production.dry-run` 已持久编译 ProductionRun/StageRun DAG，`workflow.projection.get` 与 Desktop 轮询 Adapter 已把阶段和 Artifact 投到真实 Workflow，并保留用户布局；继续补 `workflow.projection.updated` 事件。该能力只负责运行恢复，不再作为最终编辑模型。
- **AI 原生 Workflow Draft**：🟨 Agent 与设计师已通过同一套 Workflow Draft Action 直接操作画布草稿，节点、连线、Prompt、参考素材都落在 `metadata.prompt`（PromptBar 可见可编辑）；新增「草稿」记录 Tab 持久化每次 AI/CLI/MCP 动作并支持定位节点，新增 `workflow.node.tool` 让 AI 直接调用画布二次处理工具（放大/抠图/拆分/编辑/旋转/宫格/视频剪辑分离拼接抽帧/音频截取变速）。剩余：Agent 写操作进入画布本地撤销栈（当前经 dispatcher 直写 store，Ctrl+Z 不可回退 AI 动作）、可撤销删除/付费生成与冻结 Revision 的确认 Gate 语义收口、Production Plan Card 与 Revision 批准。
- **Workflow Operation Node**：🟨 图片 generate/crop/upscale 已形成输入媒体 → Operation → 输出媒体并支持同节点追加 Take；继续迁移视频、音频、其余图片/本地结果工具，补语义 LOD 与跨媒体一致执行，不得回退为原地覆盖或无来源孤立节点。
- **Operation Prompt Document**：🟨 图片 Operation 已以 `promptDocument` 保存 Prompt 并由现有 PromptBar/Agent 共用；继续删除 `metadata.prompt` 等可独立写入旁路，扩到视频、音频与所有结果工具，不新增第二套 Prompt 表面。
- **Operation Input Binding**：🟨 图片 Operation 已让 Binding ID 同时投影为输入边 ID，并保存角色/顺序/对象版本；继续补角色编辑、所有入口双向同步，迁移并删除 `mentionedNodeIds`、`referenceNodeIds`、`imageReferenceOrder` 与无角色连接真相。
- **Operation Capability Registry**：🟨 已注册 image.generate/crop/upscale，并派生 Agent 工具、Dispatcher 参数/确认和输入 Preflight；继续迁入 4 个视频、2 个音频及 remove-background/split-layers/edit/rotate/split-grid，删除 legacy catalog 与执行 switch 后才能完成。
- **Workflow Agent Tool Loop**：⬜ 内置 Flovart Agent 采用读取当前 Draft → 调用 Registry 工具 → 观察新 Draft/Object Version 与结果 → 继续的迭代循环，一个用户回合归入一个 ChangeSet；支持步数/时间限制、取消、steer、对象冲突重读与授权暂停。长时任务只返回持久句柄，由 Runtime/Workspace 观察，不让 Agent 轮询；旧网站 one-shot JSON 命令批次退出主流程。
- **AI 原生图片 tracer bullet**：🟨 image.generate/crop/upscale 已贯通显式 Operation、共享 Prompt Document/Input Binding、Snapshot、Take、取消、晚到结果和 localforage 重载；继续补精确子图授权卡、partial ChangeSet/时间线、对象冲突与按组撤销。验收门限制完成声明，不阻塞视频、音频及剩余工具继续迁入同一机制。
- **Execution Prompt Snapshot**：🟨 图片 Operation Take 已冻结 Recipe Hash、提交文本、引用绑定、规范化参数、Route 和编译器版本；继续关联 ProviderAttempt/费用，并实现增强差异查看与“采用到 PromptBar”Draft Action。
- **Operation Node 语义 LOD**：⬜ 运行中、失败、待确认、最近修改、选中或固定的操作显示完整卡片；稳定完成的简单步骤才随缩放折叠成带名称、状态、Take 数和警告的连线 chip。两种形态都可直接命中和键盘聚焦，支持单节点固定与全局展开，错误不得被缩放隐藏。
- **Workflow / Table 工具分层**：⬜ Workflow 只保留单步、局部、立即服务下一次生成的 Operation Node；批量、多步骤、多输入/多输出、时间轴精修与可复用处理链进入 Table。抽出共享 `MediaOperationRecipe` Schema、参数控件与执行器，但两个工作区不得共享节点实例、历史或布局。
- **显式 Table Promotion**：⬜ 支持把 Workflow Operation 的输入 Artifact 与 Recipe 提升为新 Table Session；提升后 Table 独占配方编辑，Workflow 只保留可点击引用步骤。Table 候选结果不会自动回写，用户或 Agent 明确发布时才以 Draft ChangeSet 更新 Workflow 的 `publishedArtifactId`，并保留旧发布版本用于回退。
- **语义 Draft ChangeSet**：⬜ 将一个 Agent 回合或连续人工手势归为可读变更集，耐久记录操作者、意图、类型化动作、前后差异、结果及关联 Provider Task/Artifact；状态支持 open/completed/partial/failed/undone。动作流式耐久提交，部分失败保留成功步骤与可重试失败 Operation Node；按组撤销不伪造删除 ProviderAttempt/Artifact，也不把每次按键或指针移动永久事件化。
- **Draft Change Timeline**：⬜ 在 Workflow Workspace 提供默认轻量收起的 ChangeSet 时间线，展示操作者、意图、状态、差异摘要、费用与 Artifact 关联；点击记录聚焦受影响节点，节点检查器可反查来源记录，并提供按组撤销、重做、查看差异和失败重试。聊天只保存 `changeSetId` 链接，不作为唯一历史权威。
- **设计师 / Agent 对象级并发**：⬜ 为 Draft 节点与连线增加单调 Object Version，Draft Action 携带 `baseDraftVersion` 与目标 `expectedObjectVersions`；同对象冲突返回 `PRECONDITION_FAILED`、当前版本和最小差异，Agent 只重读受影响子图。不同对象继续并行，不锁整张画布，也不允许最后写入静默覆盖人工 Prompt、参数或连线。
- **Workflow Layout Intent**：⬜ Agent 只声明相对来源、方向、分支、分组与顺序提示，前端确定性 Layout Planner 按真实尺寸、折叠状态、视口和占用区域找空位；人工拖动节点标记 pinned，Agent 不得移动。派生坐标进入可撤销 ChangeSet，但不更新 Recipe Hash、授权或 ProductionSpec。
- **Workflow Draft Authority 与显式转移**：⬜ 纯 Web 项目由 Browser Workspace/localforage 保存草稿，Desktop 或已配对 Web 项目由 Local Data Service 保存；同一项目禁止双写。实现 Authority Binding、导出校验、导入、切换失败回退与冲突拒绝。
- **Workflow Draft Authority Port**：⬜ UI、Agent、Dispatcher、Timeline 只能通过统一 Port 读写 Draft、ChangeSet、Binding、Take 与布局。图片 tracer bullet 先实现 Browser/localforage Adapter，Zustand 只作 UI projection/cache；Desktop Adapter 后续复用契约，未显式 transfer 前不得双写 Runtime，Browser 页面关闭限制必须如实提示。
- **草稿批准与 ProductionSpec Revision**：⬜ 批准前所有语义与布局修改只进入 Workflow Draft；用户批准时才冻结为不可变 ProductionSpec Revision。运行中细修改从当前 Revision 派生新草稿，重新批准后执行局部 Stage 失效、旧 Take 保留和单镜头重做。
- **精确 Operation 子图授权**：⬜ Production Plan Card 一次锁定 Draft 版本、待运行 Operation 节点与依赖闭包、Recipe Hash、Route Plan、预算和 Gate；多个付费节点不逐项弹窗，未包含的新节点也不继承会话授权。语义修改只要求改动节点及受影响下游重新批准，本地可撤销非 Provider 操作继续直接执行。
- **Operation Take 与晚到结果**：🟨 图片 Operation 已按启动 Recipe Hash 保存 Take；运行中改 Recipe 后结果标为 `outdated_recipe` 且不更新 `selectedTakeId`。继续关联 ProviderAttempt、费用与 Artifact，补比较/明确采用动作，并将语义扩到视频、音频和其余结果工具。
- **删除双轨状态**：⬜ Projection 接管后删除 `shadow-runtime-state.json`、Vite file queue 和浏览器完整快照作为命令真相；WebUI Local Agent Bridge 改为 Production Runtime Adapter。

## Provider 账单与取消

- **任务级取消**：⬜ 只对官方明确提供取消接口的线路保存 `providerTaskId` 并调用取消能力；未确认成功前 UI 只能显示“停止等待”。
- **实际账单回填**：⬜ 有账单或用量接口时将本机预估更新为实际费用；无法查询时保留“费用未知”，不自动退款或假定零消耗。

## 社区、企业与分发

- **Desktop 内置 Agent Toolkit**：⬜ 当前 Desktop 可自动启动显式入口、已安装 Agent Toolkit 或源码开发入口，WebUI 无需手填 Token；正式安装包仍需内置签名后的 Managed Agent 资源和固定 Node `>=22.19.0`，避免全新机器只有 Desktop、尚未安装 Node/Toolkit 时自动连接降级。
- **macOS/Linux Agent Toolkit 发布**：⬜ Node CLI、Workspace Adapter 与 POSIX launcher 已跨平台；正式 tag 流程仍需同时构建、签名并合并 `darwin-*` / `linux-*` Toolkit manifest，不能只发布 Windows artifact 后宣称 `flovart install/start` 全平台可用。
- **UGC 后端**：⬜ 实现 CommunityWorkflow 列表、筛选、搜索、上传、详情、点赞与 fork 计数，并接入真实用户和 R2 媒体预览。
- **项目与素材共享**：⬜ 完成项目云同步、作品画廊和公共素材库；共享 Workflow 不包含模型配置和 API Key。
- **企业联调**：⬜ EnterpriseApp 接入真实后端，补 owner 转让、资源 R2、项目镜像、敏感词预检与资源上架审批闭环。
- **CLI 发布**：⬜ 发布包含当前命令注册表与开发环境命令的 `flovart-cli` 新版本。
- **桌面与扩展分发**：⬜ 验证 Docker 静态资源路径、Windows EXE 安装签名、GitHub Release 实际上传 Agent Toolkit、Edge Add-ons 上架和扩展到 Desktop Runtime 的受限桥接；扩展不得保存 Provider Secret，云同步不得在完成前写入正式能力说明。
