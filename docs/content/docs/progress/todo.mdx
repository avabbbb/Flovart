---
title: 后续待办
---

本页只记录**尚未完成**的工作。当前产品边界以 [主设计](../../../design/flovart-native-effects.md) 为准；已实现但仍需现实环境确认的内容移到 [待测试确认](pending-test.mdx)。

## P0：把当前三 surface 做实

- [ ] Canvas：继续收敛引用、缺失素材、运行中改参、晚到结果与大媒体性能，不引入第二份 Workflow 状态。
- [ ] Table：把现有结构化媒体处理入口补成稳定的输入 → 处理 → 输出闭环，统一进度、取消、内存限制与结果 commit。
- [ ] Agent connection hub：逐个验证 Codex / WorkBuddy / Claude Code / OpenCode / DSH 的安装、登录、状态、切换与真实调用，不在 Agent 页恢复内置聊天。
- [ ] Assistant drawer：验证 Canvas/Table 上的 Assistant / Context / History 在项目切换、窄窗口、断线与恢复时不复制 Host 状态或产生第二份会话权威。
- [ ] 清理仍有运行时引用的 Production Crew / Director / Operator / Production Skill 遗留代码；每次删除必须先证明真实调用点已被当前短路径替代。

## P0：Agent operation 可靠性

- [ ] 修复并回归 `workflow.project.use` 的 browser activation ack，确保下一次 inspect/lease 已绑定新项目。
- [ ] 对稳定 operation 补齐错项目、revision drift、幂等 replay、未知提交与恢复 tracer。
- [ ] WorkBuddy 真实 Connector 导入与自然语言调用。
- [ ] Codex / Claude Code / OpenCode / DSH 按 Support Matrix 各自补齐真实用户会话证据。
- [ ] TeleAgent 只有在真实 MCP Host 导入、权限、等待与自然语言 tracer 通过后再升级状态。

## P1：Provider 与生成

- [ ] 真实 Provider 分别验证认证、参考图/视频 wire、参数、下载持久化、费用、取消、429 与提交未知。
- [ ] 浏览器 key 与 Desktop Runtime credential 边界继续显式展示；不把两者文档化成已经统一托管。
- [ ] 导出/打包前检测本地引用失效，不能静默输出错误内容。
- [ ] 长任务继续复用现有 durable identity/checkpoint，不再建第二套 ProductionTask/Workflow 状态。

## P1：创作软件宿主

- [ ] After Effects / Premiere 先完成真实安装、选择目标、生成素材应用、保存重开和离线导出。
- [ ] Photoshop 验证选区、结果回编与可逆编辑。
- [ ] Resolve 分别验证 Workflow Integration 与 OFX/宿主效果边界。
- [ ] 原生效果 render callback 只读取固定本地素材，不联网、不依赖 Agent 在线。
- [ ] Windows 首批验证通过后再独立排期 macOS，不用源码可移植性代替真实验收。

## P1：发行与证据

- [ ] Hosted CI / CodeQL / release packaging / production signing 按当前候选 SHA 真实跑通。
- [ ] 真实付费 Provider 与真实 Creative Host 不使用 Fake Provider / package build 证据替代。
- [ ] Support Matrix 只根据新的真实 tracer 升级，不用旧 RC 数字推断当前代码。
- [ ] 文档检查保证 active design tree 只保留 current design，不再新增 CURRENT/TARGET/AUDIT 平行目标稿。

每个切片只写“可见目标、最少实现、验证证据”。历史施工过程由 Git 与 archive snapshot 保存。
