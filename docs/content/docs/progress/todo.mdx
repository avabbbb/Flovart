---
title: 后续待办
---

本页只记录未完成工作。主方向以[原生效果与 Agent 协作设计](../../../design/flovart-native-effects.md)为准；实际完成的代码或文档移入[待测试确认](pending-test.mdx)。删除旧计划不等于对应代码已迁移。

## 第一阶段：AE / PR 场景替换

- [ ] 记录原型目标宿主版本、SDK、Windows 构建环境和样例素材；核对主设计中“插件独立生成”“整段替换加遮罩”的两项推荐假设。
- [ ] AE 原生效果读取固定本地素材，暴露混合及已验证参数，完成关键帧、随机帧、保存重开、关闭服务后的导出。
- [ ] 在 PR 验证 AE 效果核心的实际兼容；记录差异，不先建设通用 Host SDK。
- [ ] 建立持久素材版本与明确应用动作，覆盖复制效果、撤销、另存工程、移动素材、晚到结果和缺失检测。
- [ ] 把一个短片段接到现有真实 Provider：固定输入、范围、提示词与模型，返回同一持久任务，完成失败、取消和提交未知恢复。
- [ ] 收敛本地生成入口，验证无 UI 启动、Provider 能力等价和素材存储；不新增 Node/Rust 双调度器，Browser 工具仍保留明确绑定。
- [ ] 完成 12 个片段与 4 个合成序列的 Benchmark，所有初始状态为未测；指标与记录方法见主设计第 6 节。

## 第二阶段：Agent 双入口与其它宿主

- [ ] 继续扩展 CLI/MCP/DSH 的权限、错误与真实 Host parity；当前 CLI/MCP 已对四个 Workflow operation 完成受控 normalized/state-hash parity，MCP 仍只覆盖五个稳定工具。
- [ ] Codex 外部 Skill/CLI/MCP 调用明确效果目标；Flovart 内部使用官方 app-server，实测登录、对话、工具、审批和恢复。
- [ ] WorkBuddy 现有 CLI Connector 完成真实导入与自然语言调用；双向本地助理 API 完成应用审核、授权、在线状态与历史验证，不能冒充离线连接。
- [ ] PS 接原生滤镜，验证选区、素材版本、参数回编、智能滤镜可行性和工程重开。
- [ ] Resolve 分别验证 Workflow Integration 联动与 OFX 效果；确认 Studio/OS/SDK 条件。先修复导入后媒体文件生命周期，不能以独立窗口冒充原生效果。
- [ ] TeleAgent 真实客户端导入、权限、长任务等待和自然语言 tracer；当前仅有 MCP + canonical Skill 准备包。
- [ ] 现有 DSH、Claude、OpenCode 接入按实际支持矩阵维护，不将其升级或私有宿主复制设为首个效果的前置条件。

## 精简实现

- [ ] 按当前切片清理只转发的包装、重复状态和重复执行路径；每次替换给出现有调用点和回归证据。
- [ ] 取消新功能对内部 Operator、Crew Intent、导演投影与复杂 ProductionSpec 的强制依赖；现有相关代码逐处核对后再改，不能整目录盲删。
- [ ] 保留必要目标/版本校验、幂等、费用确认、任务恢复、素材完整性；去掉中间层不能去掉这些行为。
- [ ] 原生效果只消费固定素材，不调用网络、不依赖 Agent；本地生成队列先顺序执行，不引入新队列基础设施。

## 现有工作区维护：按实际需求排期

- [ ] Workflow：继续修复 PromptBar/媒体直接选择、原比例、本地拖入、引用顺序与缺失提示、运行中改参和晚到结果；保留现有交互与撤销。
- [ ] 现有媒体操作：补齐图片处理等尚未贯通的实际入口，统一来源与版本；减少重复 Prompt、引用数组和多处 Provider 参数处理。
- [ ] 大图性能：视口裁剪、缩略图和活动播放器按实际性能问题优化；不先增加可替换画布端口或语义 LOD 平台。
- [ ] Config 节点删除、Scene Prediction 与音频生成继续留作后续需求，按具体任务分别验证，不阻塞原生效果。
- [ ] Table 主体仍待实现：先输入→处理→输出最小节点图；人物抠图、深度/结构参考、风格化、视频批处理和故事版按真实需求逐项推进，不一次建全套算法链。
- [ ] Table 与 Workflow 用明确素材引用和导入交接，保持各自图与历史；大文件需要进度、取消和内存限制。
- [ ] Agent 空间中接入真实对话/任务/产物，保持真实主体入口；不继续新增固定导演层级、复杂面板树或第二套主记忆。
- [ ] 保留现有 Production Skill、VOX、已批准参考图与局部重跑能力；后续只补实际用到的步骤，不以完整成片平台为本轮前置。
- [ ] ProductionTask 继续补齐跨会话 checkpoint、恢复后的真实 scheduler tracer 与任务中心，不复制 Workflow 状态。

## Provider、社区与发行

- [ ] 真实 Provider 的认证、参数、参考图/视频 wire、下载持久化、费用、取消、限流和提交未知分别验收；不根据模型名称猜接口。
- [ ] Runtime-only 路由与浏览器 key 状态分别展示；不向连接器同步原始密钥、不改用户 Plus/OAuth 和代理。
- [ ] Browser Import 使用实际商店 ID、Native Host 注册与安装卸载验证；素材进入可见项目或 Inbox，不能假成功。
- [ ] 保留社区作品/Remix、组织权限和 Enterprise 联调需求，按真实账号与数据隔离验收；个人项目与素材不宣传已云同步。
- [ ] Docker 静态资源、生产部署、生产签名、升级恢复及 Hosted CI/CodeQL 按真实发行范围验证；历史 RC 不代表当前新效果已可发布。
- [ ] 支持等级和宣传随宿主与 Benchmark 证据更新；首个演示使用真实插件操作、等待、版本比较、重开与导出。

不再采用几十阶段通用 Launch 总计划作为日常开发前置。每个切片只保留“可见目标、最少实现、对应验证”。
