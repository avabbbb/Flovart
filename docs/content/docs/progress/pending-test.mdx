---
title: 待测试确认
---

本页只记录当前版本已经进入代码或文档、但仍需用户确认的变更。旧版本的 Canvas、Art、Excalidraw、MCP、Managed/Connected Agent 与阶段施工流水不再保留在这里；需要追溯时使用 Git 历史。完成用户验收后，再把稳定能力更新到[功能特性](../overview/features.mdx)。

## Release Candidate Hardening：发行候选验收

- 已建立 `docs/evidence/release-candidate/RC_BASELINE.md`、安装器/迁移/持久化/Canvas stress/资源/accessibility/security/plugin/provider/Host chaos/updater/offline/diagnostics/red-team 证据文档；本机自主验收不等同于真实账号、付费接口或生产发布认证。
- Windows 本地 NSIS 已实际构建、隔离安装、启动、关闭和卸载；Tauri test-only updater 已在隔离 HTTPS fixture 中完成安装态 N→N+1 替换、篡改包拒绝，并用种子项目证明升级后项目容器仍可见；source-identical packaged WebView2 已完成跨 schema 迁移和备份/当前 envelope 两阶段进程中断恢复。生产 updater private key、Authenticode、生产等价签名和发行机确认仍待完成。
- CI 已加入 docs/typecheck/full Vitest/Web/extension/DSH/Rust/release invariant 门禁；关键套件本机 `10/10` 通过，GitHub hosted workflow 还需在仓库权限和 release secret 下实际执行。
- Release security 已加入 `npm run release:secret-audit`、`.github/workflows/security.yml` 的 CodeQL（JS/TS + Rust）与 PR 高危依赖审查；当前候选本机扫描 923 个 Git-visible files 为零发现，GitHub secret scanning/push protection 与 hosted 首轮结果仍需在仓库设置中确认。
- 远端旧 `main` 的 CodeQL 快照包含 13 个未关闭告警（1 critical、9 high、3 medium）；本地候选已把选题研究产物键改为 4096 字符有界字符扫描并通过恶意超长键回归，但候选 SHA 仍需 Hosted CodeQL 重跑，不把本地修复写成远端关闭。
- 生产与开发依赖安全已完成一次最小收口：移除当前源码未引用的 Transformers/Excalidraw 生产依赖，更新同主版本安全修复并锁定依赖；clean `npm ci` 后官方 registry 的完整 `npm audit` 与 `--omit=dev` 均为 `0 vulnerabilities`。详细版本和命令见 `docs/evidence/release-candidate/RC_DEPENDENCY_AUDIT.md`；Hosted 首轮仍待候选 SHA 执行。
- DSH Plugin 的独立 lockfile 已将构建期 `esbuild` 从 `0.21.x` 更新到 `0.28.2`，clean install 后嵌套依赖 `npm audit --audit-level=moderate` 为 `0 vulnerabilities`；CI 与 Security workflow 已加入该嵌套审计，候选 SHA 的 Hosted 首轮仍待执行。
- 发布 workflow 在 checksum/SBOM 生成后增加产物内容校验：安装包版本名、`SHA256SUMS.txt` 覆盖与实际摘要必须一致；本机 NSIS 和篡改 fixture 已通过，Hosted Syft SPDX 与 provenance 仍待在候选 SHA 上执行。
- 稳定 tag 发布路径新增 `latest.json`/`.sig` updater 产物校验：每个平台必须有版本化 HTTPS URL、对应产物和匹配 sidecar，缺失签名时 fail closed；本机 test-signed feed 与 `4/4` focused tests 已通过，但生产签名、Hosted 执行和公开 feed 仍待确认。
- CLI `--help` / `-h` 现在明确把已退出的 Provider 配置、连接测试和批量生成命令标为 `[retired]`；日常 Agent 仍只使用五个稳定 Workflow 工具，完整 help 与定向回归已通过。
- 修复 Tauri/WebView2 中生成结果为 `data:` URL 时被 CSP 阻断的问题：桌面生成路径直接解码为 Blob，保留普通远程 URL 的 fetch 路径；源代码聚焦回归 `55/55`，源代码一致的带 CDP 观测构建已完成 Fake Provider T2I 并将结果持久化到画布。
- 断网时保留本地项目、素材和 Canvas 编辑，并以产品文案提示远程能力等待恢复；高级 Dock 支持复制脱敏诊断 JSON。默认页面不展示连接 Token、Provider secret 或运行时协议字段。
- 支持等级统一记录在根目录 `SUPPORT_MATRIX.md`：Codex/Claude/OpenCode/DSH 为 Experimental，CodeBuddy Code/Pi/WorkBuddy 为 Planned 或 mainstream projection 候选，真实 Provider/账号/账单不写成 Stable。
- 从同一应用源码构建的测试 overlay NSIS 已安装并通过真实 WebView2 页面完成 Home → 新建 Workflow → 首次 AI 服务配置 → 费用确认 → Fake Provider 图片生成；该 overlay 只用于观测，不改变正式 v10 安装包、签名或 updater 结论，详情见 `docs/evidence/release-candidate/RC_PACKAGED_UI_EVIDENCE.md`。
- 独立 `flovart-cli@0.3.2` 的实际 npm 打包与隔离安装已补齐：修复 `files` 白名单漏发运行时模块导致安装后 `flovart --help` 崩溃的问题，并由 `tests/cliPackageManifest.test.js` 防止可达顶层模块再次漏包；tarball 的 CLI help、结构化 offline status、Skill 和 Managed Agent 投影均已通过。
- 当前 clean candidate 为 `111db63d4261cd81cf7f5cadd340f1044cea32e8`；NSIS `0.3.2`（12,248,572 bytes，SHA-256 `0E8753064D4C9150C67D632396E158EC614945AACE663F47167688C2DB8A1C3F`）、SPDX/checksum、隔离安装/启动/关闭/卸载、Chrome-only 动态端口绑定 smoke、公开 `#/app` Quick Start 和 CLI tarball 隔离安装已重新绑定到该 SHA。该候选未推送、未签名，也没有 Hosted CI/CodeQL/provenance 运行记录。

待用户/外部环境确认：

1. 在 disposable Windows 发行环境使用生产等价签名完成 N→N+1 更新、无效签名拒绝、升级中断恢复、迁移写入中断恢复和 Authenticode 验证。
2. 登录 Codex、真实 Provider（必要时 DSH）完成首次安装/生成/账单/取消/登录态恢复 transcript；本轮 fake provider 不替代这些认证。

## First Run → First Safe Generation：首启到首次安全生成

- 首次打开没有 AI 服务时仍可进入并编辑正常 Canvas；只有执行生成时才显示“添加 AI 服务”入口，不再以 Provider 错误或禁用按钮阻塞首屏。
- 真实可见 Chromium fresh context 已按公开 Quick Start 复走 `/#/app → 稍后再说 → 新建工作流 → 添加 AI 服务 → 自动发现模型 → 费用确认 → 首次图片生成`；生成按钮和确认提示均为产品文案，产物回到画布。证据为 `C:\tmp\flovart-c14-first-user-evidence.json` 及同名截图；Fake Provider 不替代真实 Provider/账号验收。
- OpenAI-compatible 首次配置默认只要求服务地址与 API Key；连接成功后自动读取 `/models` 并补齐缺失的产品模型路由。模型列表缺失时支持手动填写模型，连接失败统一显示可行动的中文提示。
- PromptBar、Workflow Graph、`@` Mention 与素材库引用经过同一 Canonical Input；图生图、图生视频会把引用真实送入 `/v1/images/edits` 或 `/v2/videos/generations`，不支持的 capability 在请求前明确失败。
- 外部生成进入明确的费用确认 Gate。未经真人确认，Agent 的 `workflow.node.run` 即使携带伪造的 `confirmed: true` 也不会提交 Provider 请求；确认后的重试使用同一幂等身份，避免重复提交。
- 本机已用真实 localhost HTTP Fake Provider 与可见 Chromium 完成首次图片生成、图生图、图生视频、401/429/错误地址/超时、视频刷新恢复和错误重试；录制器只保存脱敏 header 与请求摘要，不保存原始密钥或 multipart 内容。
- 生成中的视频任务先持久化 Provider task ID，再进入轮询；刷新后继续查询原任务，不创建第二个提交。生成结果能够回到当前 Workflow 节点并在画布显示。

本轮本机证据：`C:\tmp\flovart-first-safe-generation-20260830.png`、`C:\tmp\flovart-prompt-reference-e2e-20260830.png`、`C:\tmp\flovart-video-refresh-recovery-20260830.png`、`C:\tmp\flovart-byok-unauthorized-20260830.png` 及同名 Playwright 输出；Fake Provider 使用真实 HTTP transport，未调用真实付费服务。

待用户/外部环境确认：

1. 在接近 clean state 的真实桌面环境按“打开 Flovart → 添加 AI 服务 → 配置可信服务 → 生成”走一次，确认首屏文案、费用提示和最终成品符合预期。
2. 使用真实 Provider 凭据分别确认账单、取消、实际价格和生产级 I2I/I2V 视觉质量；本机 Fake Provider 证据不替代第三方费用验收。
3. 登录 Codex 或 DeepSeek Harness 后，再补真实登录态对话/插件生命周期；这不影响本轮 fake-provider 与浏览器 DoD。

## Agent Surface Simplification：Host Projection 与精简工具面

- 新增版本化 Host Registry，把 Agent Identity、IDE Host、Distribution Target、Runtime Surface 和 Director Runtime Binding 分开；`init --target` 负责安装投影，`director.*` 只接受能映射到已实现 Runtime Binding 的 Agent Identity。
- `host.list` 只做 PATH 可执行文件发现，不读取登录文件、OAuth 或 API Key；Agent Host picker 只保存后续任务偏好，不自动切换 Director 会话。
- 内置 Agent 模型工具固定为 `status`、`workflow.inspect`、`workflow.selection.get`、`workflow.apply`、`workflow.node.run`。`command.list` / `command.schema` 仍可用于 CLI bootstrap、discovery 和 debug，但不再进入模型工具集合。
- Codex 是当前 professional golden path；DeepSeek Harness 保留显式 native Plugin projection；CodeBuddy Code、Claude Code、OpenCode、Pi 通过 stable Skill + CLI contract 兼容。WorkBuddy 仅作为未来 mainstream、skill-mediated projection 候选，不加入 `director.bind`，不阻塞本轮验收。
- Settings 中 Runtime 凭证与浏览器 AI 服务保持不同安全边界：Runtime 路由只展示非敏感元数据，浏览器直连凭证继续渐进披露；PromptBar 使用“AI 服务/访问凭证”产品词汇。

待用户确认：

1. 在本机打开 Agent Workspace，确认 Host picker 能显示可用/未安装状态，重新扫描不会要求填写 Token 或 API Key，WorkBuddy 不显示为可绑定 Director。
2. 使用 Codex 完成 `status → workflow.inspect → workflow.apply → workflow.node.run` 的真实可见 Workflow 路径；确认 CLI/Agent 不再要求通过 `command.list/schema` 发现每一个操作。
3. 对 `init --target codebuddy-code-skill`、`claude-code-skill` 和 `opencode-skill` 做 dry-run 或本地安装确认，检查投影目录与 Skill 内容正确；这不等同于 CodeBuddy Code/WorkBuddy 的完整产品接入。
4. 在 Settings 确认 Runtime 凭证不会被伪装成网页 API Key，已有 AI 服务在收起/展开后仍能编辑和验证。

## 引用模型收敛：连接唯一事实来源

- `mentionedNodeIds`、`referenceNodeIds`、`imageReferenceOrder` 已从 `WorkflowNodeMetadata` 删除，全仓库不再写入或读取；引用顺序 = connections 数组顺序，chip 拖拽排序通过新增 `reorder_connections` document op 重排连接数组，Operation 节点同步重排 inputBindings（role 不变）。
- 提示词 `@`（富文本 mention + 纯文本 `@名称`）只作为连接内选择；文本解析同时匹配稳定别名（图片1/音频1）与自定义标题，与运行时 mentionAliases 一致。
- PromptBar 不再持久化引用状态，chips mentioned 徽标与 PromptIntent 全部读取时派生；Operation inputBindings 只由连接变更（connect/delete/reorder）驱动，不再与 metadata 双向投影；`orderDrifted` 写回 effect 已删除。

待用户确认：

1. 拖拽排序 chip 后刷新页面，首帧/尾帧与引用顺序是否保持不变。
2. 自定义标题节点（如 `@配乐`）在 PromptBar 能力校验与运行时 wire 中是否一致。
3. 旧项目（含已删除字段的持久化数据）打开后生成是否正常——旧字段被读时忽略，不做迁移。
4. 素材选择、粘贴引用、分镜批量生成创建节点后引用顺序是否符合预期（新连线追加在尾部）。

## Workflow Graph 输入解析纵切

- 新增统一的 `WorkflowResourceReference` 与 `resolveWorkflowInputs`：生成节点的输入从当前 `nodes + connections` 解析，带上资源类型、来源节点、连接身份和 Artifact/本地存储/可传输地址信息；URL 不作为引用身份。
- 新增 Node Output Contract：图片、视频、音频、文本、配置等节点通过注册的 definition 声明 `WorkflowResource`；Runtime Artifact、素材库和本地媒体分别保留自己的 locator，不再由输入 resolver 猜资源类型。
- 新增 `resolveWorkflowResource` Executable Resource Resolver：把声明 locator 解析为直连 URL、临时 Blob URL、Runtime Artifact 或内联文本；生成入口复用同一媒体 hydrate seam，并继续兼容现有旧字段投影。
- 现有 Workflow 生成入口和 `getUpstreamData` 共用这条解析路径。已连接的图片、视频、音频即使没有 PromptBar `@` 记录，也会进入候选输入；`@` 与 Seedance slot 仍可在同一连线边界内缩小范围和控制顺序。
- 空的已连接媒体不会被解析器静默丢弃，而会保留为不可用资源，让后续媒体解析返回具体缺失错误。
- 本纵切没有改写 Asset Picker、Mention 持久化模型或 Port 角色；它们仍需后续在同一 `ResourceReference → InputResolver` 契约上接入。

待用户确认：

1. 在 Workflow 中只把图片节点连接到图生视频/图生图节点，不输入 `@`，生成前后是否都能识别同一张图片。
2. 删除连接后重新生成，Provider payload 是否不再携带该媒体；重新生成上游后，下游是否读取当前 Artifact 而不是旧 URL。
3. 图片节点只有预览但没有可执行 Artifact 时，错误是否能明确指出来源节点，而不是只显示“需要上传图片”。

## Workflow Input Convergence（Phase B）

- Graph、Prompt `@` Mention 与 Asset 引用现在统一进入 `GenerationReference[]`，再由 `resolveWorkflowInputs` 生成唯一的 `ResolvedNodeInputs`；同一资源通过不同入口引用时会去重，并保留角色、顺序、来源和诊断。
- `buildCanonicalGenerationInput` 根据节点模式和已解析引用确定 capability 与 Provider 无关的 canonical 参数；`image-to-video`、`image-edit`、`text-to-video` 等模式不再由 Provider 或 PromptBar 重新推导。
- `mentionedNodeIds`、`referenceNodeIds` 与 `imageReferenceOrder` 已删除：引用顺序 = connections 数组顺序（新增 `reorder_connections` document op），选择 = 提示词文本 `@`；`seedanceReferences` 仍在 resolver 内作为 config 级 legacy 输入转换；Canonical Input 之后只由统一的引用物化适配层生成 Provider 参考数组。
- 生成入口接收轻量 Asset 索引，真实文件仍由 Executable Resource Resolver 按 locator hydrate；`artifactRef`、本地存储和 Asset locator 不会被转换成另一套引用身份。
- 已增加 Graph + Mention + Asset、重复引用、artifactRef-only、空媒体、角色冲突及 legacy 顺序/Seedance 映射测试；本阶段不改 PromptBar UI、Provider capability、DSH、Agent Workspace 或 Workflow SoT。

待用户确认：

1. 同一图片同时通过 Graph 连接和 `@` Mention 引用时，生成请求是否只携带一份媒体，并保留预期的 role/order。
2. Graph、Mention、Asset 三种入口指向同一资源时，节点预览、Canonical Input 和最终 Provider payload 是否一致。
3. 角色冲突、空媒体和仅有 `artifactRef` 的节点是否都能显示明确诊断，且不会静默降级为文生图/文生视频。

## Provider Capability / Adapter（Phase C）

- 新增独立的 `ProviderGenerationAdapter`：它只接收 `CanonicalGenerationInput`，按当前产品模型、Provider 和 route 声明可用 capability、参考媒体类型、角色及数量上限。
- Workflow 生成链现在先完成 Canonical Input，再经过一次 Provider capability validation 和一次 wire request serialization；Provider/Gateway 不再从 Canvas nodes、connections 或 metadata 重新组装引用。
- Canonical path 会把实际 route ID、生成模式和已 materialize 的参考媒体传入 Gateway；如果 route 不支持 capability、参考类型、角色或数量，会在 HTTP 请求前明确失败，不静默丢引用或降级成文生视频。
- 增加了 Provider seam、Gateway 拦截和最终 OpenAI `/images/edits` multipart body 测试；现有 RunningHub/Veo/Keling 的真实 request body 测试继续覆盖视频序列化路径。本阶段不改 PromptBar UI、DSH、Agent Workspace、Workflow SoT 或 Canvas mutation。

待用户确认：

1. 同一 Workflow 节点在图生图和图生视频场景下，Resolved Inputs、Provider wire request 与最终 HTTP body 是否保持一一对应。
2. 选择不支持当前 capability、参考角色或参考数量的 route 时，是否在发送请求前显示明确错误，且不会悄悄改成其它生成模式。
3. Runtime managed 的 `artifactRef-only` 图片是否仍能沿用同一 Canonical Input，并只在 Runtime wire mapping 阶段转换为 `sourceImageIds`。

## WorkflowExecutor 收敛（Phase D）

- UI、Browser Agent、CLI Workspace Adapter 与 Runtime/operator route 现在都进入同一个 `WorkflowExecutor.runNode()`；Dispatcher 不再持有第二套 `runNode/stopNode` 引用组装路径。
- `WorkflowRunCommand` 统一携带 `projectId`、`nodeId` 和可选 `expectedRevision`；每次执行生成 `runId`，统一传递 `surface`、`correlationId`，surface 不参与输入解析、capability 或 Provider mapping。
- Dispatcher 统一消费 Executor 的结构化结果和错误 code；CLI 的 `workflow.node.run/stop` 不再访问已删除的文件队列，只通过 loopback Workspace Adapter 转发到浏览器 Workflow Authority。
- 生成链新增 canonical observer，执行器可追踪同一次运行产生的 `CanonicalGenerationInput`；专项测试覆盖 UI、Browser Agent、CLI、Runtime 四种 surface 的 canonical JSON 等价性。
- 本阶段没有修改 PromptBar、DSH、Workflow SoT、Agent Workspace 或 Canvas `applyOps`；Phase C 登记的 DSH baseline failure 保持不变。

待用户确认：

1. UI、Browser Agent、CLI 与 Runtime/operator 对同一节点运行时，是否都显示同一 `runId`、错误语义和节点状态。
2. CLI 通过已连接 Workspace Adapter 运行/停止节点时，是否确实进入当前浏览器 Workflow，而不是回落到本地文件状态。
3. 提交 `expectedRevision` 过期时，所有入口是否都得到 `REVISION_CONFLICT`，且不会启动 Provider 请求。

## Browser Workflow Contract 与 Local Agent binding（Phase F0）

- 新增版本化 `BrowserWorkflowContract`：`WorkflowAgentBridge` 只把 Workflow 命令交给浏览器 Contract，Contract 再进入现有 Dispatcher、Draft Authority 与 `WorkflowExecutor`；Bridge 不直接组装第二套 React/Provider 执行路径。
- 普通 Vite 浏览器支持显式 loopback Agent binding：`#/dock` 现在明确是开发者配对/调试面，只有 `/crew/protocol` 认证成功后才把连接放入当前页面内存并通知可见 Workflow adapter；已保存的本地配对可在普通 `#/app` 启动时恢复，Tauri 仍只使用受限 IPC discovery。
- Local Agent Session 不再在没有浏览器连接时自动启用 native workspace；native Workflow 只有经过明确的 `/workflow/native/register` 才能成为当前会话的执行 authority。
- `workflow.node.run/stop` 已在 canonical Registry 中标记为 `available`，但仍只由 Workspace Adapter 转发到浏览器 Workflow Executor，不会变成 Production Runtime 的第二套执行器或内置 Agent 的默认工具。
- CLI 仍只通过 loopback Workspace Adapter 调用浏览器，浏览器 Workflow 继续拥有节点、连线、Draft 与运行入口；本阶段没有新增私有 HTTP、shadow state 或 PromptBar Provider 分支。

待用户确认：

1. 启动 `npm run flovart:agent` 后，在 `#/dock` 输入 Agent 配对信息，确认 Dock 与 Workflow 都显示已连接，且 `workspace.status` 返回浏览器 Workspace `ready`。
2. 浏览器配对后通过 CLI 执行 `workflow.project.list`、`workflow.inspect` 和一个可撤销节点变更，确认 CLI 看到并修改的是当前可见 Workflow，而不是 native 文件状态。
3. 关闭或未配对浏览器时，CLI/Agent 应诚实返回 Workspace 不可用；不能自动创建或读取 native Workflow。只有显式 native 注册的 DSH/Runtime 场景才允许走 Native Draft。
4. 同一 Agent 重连或切换项目后，旧 Workflow adapter 不应继续接收命令；最新快照的 browser client 才能成为命令目标。
5. 记录真实使用的 Agent URL、HTTP 响应和失败状态；若端口无监听，先启动 Agent 或修正配对配置，不把连接失败归因于 PromptBar 或 Provider。

## Auto Local Agent Bootstrap（Phase F1）

- `flovart start --open` 在源码仓库中会复用或启动本机 Workflow Agent，等待真实 WebUI/Agent 就绪后直接打开主 `#/app`；启动 URL 携带一次性 bootstrap 参数，不再把 `/dock` 作为正常入口。
- 主 App 挂载时由独立 `AgentConnectionBootstrap` 完成 loopback health/protocol 认证，认证成功后立即清理地址栏参数，并只在当前 session 保存连接信息；Token 不进入 UI 状态、CLI JSON、日志或持久 localStorage。
- Bootstrap 成功后复用既有 `BrowserWorkflowContract → WorkflowWorkspaceAdapter → WorkflowExecutor`，Workflow/Table 不依赖 Agent 在线状态；`#/dock` 继续作为 Developer Diagnostics。
- `status --json` 现在是可用的本机系统状态命令，报告 frontend、Agent、browser binding、clientId、projectId 与 revision；新增 `open-flovart` Skill 只编排 `status/start`，不读取 Agent 配置、不猜端口、不实现 binding。

待用户确认：

1. 执行 `flovart start --open` 后，用户浏览器是否直接打开主 Workflow 页面、无需复制 URL/Token，并显示 `Local Agent Ready`；命令级默认启动与 Browser binding 已在本机通过。
2. `flovart status --json` 是否能看到 `frontend.ready`、`agent.ready`、`browserConnected`、当前 `clientId/projectId/revision`；关闭浏览器后是否明确变为不可用。命令级字段已通过，仍需用户确认窗口体验。
3. CLI `workflow.inspect/apply/node.run` 是否命中刚刚打开的可见 Workflow；两个浏览器 Tab 是否保持独立 binding，不串写项目。
4. 错误 Token 或 Agent 未启动时，主 Workflow 是否仍可打开并明确显示 Offline/Auth Failed，而不是卡在 Dock 或切换到错误工作区。

## Workflow Mutation Contract（Phase G1）

- Workflow Document Mutation 统一通过 `WorkflowMutationEnvelope → applyWorkflowOps`，包含 `projectId`、`expectedRevision`、`mutationId`、`source` 与批量 `ops`；UI、CLI、Browser Agent 和 DSH Browser projection 共用同一 Draft Authority。
- Document Ops（节点/连线/布局）进入 Draft ChangeSet、revision 与 Undo/Redo；View Ops（selection、viewport、focus）不改变 Workflow revision；节点执行仍只经过 `WorkflowExecutor`。
- 新增 `workflow.apply` public contract；既有 granular `workflow.*` 继续保留为 compatibility adapter，只翻译为同一批 Document Ops，不再持有独立 mutation logic。Mutation Receipt 随项目持久化，重复载荷可重放，不同载荷明确拒绝。
- Browser-bound Agent 在没有浏览器绑定时返回 Workspace unavailable，不会隐式进入 NativeWorkflowStore；显式 Native/Headless workspace 仍保持独立边界。

待用户确认：

1. UI 与 CLI 对同一批节点/连线操作是否得到相同 Workflow state，并且 batch 只形成一个可撤销 ChangeSet。
2. 重复 `mutationId` 是否重放原 Receipt，旧 `expectedRevision` 是否返回 `REVISION_CONFLICT`，View Ops 是否不增加 revision。
3. `flovart start --open` 打开主页面后，浏览器是否自动完成 binding；CLI bootstrap contract 与 Chrome for Testing smoke 已通过，Windows 默认 URL handler 不作为自动化验收路径，仍需用户复查手动首次窗口体验。

## PromptBar Reference Contract（Phase G2）

- Workflow Node PromptBar 新增 provider-neutral `PromptIntent`：只表达目标节点、提示词、稳定引用身份和用户动作；现有 PromptBar 视觉、`@` 下拉、参考 chip、模型/参数交互保留。
- Graph、富文本 `@` mention、Asset 和 Runtime Artifact 现在统一进入 `GenerationReference[] → resolveWorkflowInputs → CanonicalGenerationInput`；同一资源通过 Graph 与 `@` 重复出现时只保留一份，并保留来源、顺序、角色和诊断。
- PromptIntent 的 mentions 只从富文本文档与 Operation inputBindings 派生，不再读取 metadata 引用数组；`first_frame`、`last_frame`、`reference`、`character` 角色已从 intent/resolver 验证到 Provider wire serializer。Provider 不支持某种引用时仍在请求前明确失败，不会静默退化。
- 产品模型、Provider route、capability、参数限制和费用展示计算已抽到 PromptBar policy service；组件不再直接导入 Provider Catalog/Gateway/wire serializer 或读取密钥内容。

待用户确认：

1. 在同一 Workflow 节点分别用 Graph、`@`、素材库和 Runtime Artifact 引用生成图像/视频，确认最终 Provider 请求只携带一份媒体且角色/顺序正确。
2. 在 PromptBar 中编辑提示词、添加/移除/拖动引用、切换图生图/图生视频/首尾帧/全能参考后，确认当前 UI 交互不变，生成前的引用仍准确反映最新意图。
3. 使用不支持所选引用类型、角色或数量的 Provider route，确认发送请求前显示具体错误，不会退化成无引用生成。

## Provider Extension Contract（Phase G4）

- 正式 Provider 继续使用 canonical capability/serialization seam；新增 `ProviderGenerationExtensionRegistry` 管理 User Script Provider，未绑定用户线路时仍回到正式 adapter，两个入口不共享 Canvas 读取旁路。
- User Script 第一版使用受限 JSON mapping DSL：request、response、poll、cancel 都是固定数据映射，不执行任意 JavaScript，不使用 `eval`/`new Function`，也不开放文件系统、React、localforage、任意网络或原始凭据。
- User Script 只能看到脱敏的 `CanonicalGenerationInput` manifest 和已物化引用；凭据只在宿主构造最终 HTTP header 时读取。endpoint 仅允许 HTTPS 公网地址，媒体输出仅允许 HTTPS 或 data URL，并限制 path 留在同一 endpoint。
- `executeUnifiedIgnition` 已支持通过 `UserApiKey.extraConfig.providerScriptId` 进入 User Script adapter；图片 immediate response、视频 task/poll/cancel 和 Graph reference → canonical → 实际 wire body 都有专项测试。

待用户确认：

1. 注册一个不修改 Flovart 源码的 custom image provider，确认 Graph 图片进入 `input.references` 和实际请求 body，Provider key 不出现在 mapping body、节点或日志。
2. 注册一个 custom video provider，确认 submit → poll → media URL 与 cancel path 按声明执行；Provider 只返回 pending 或不支持取消时 UI 不伪造成功。
3. 尝试 HTTP、loopback endpoint、跨 endpoint path、受限 header 和不安全媒体 URL，确认安装或执行明确拒绝。

## Workflow Node Plugin SDK（Phase G5）

- 在既有 Node Output/Resource Contract 之上新增最小 `WorkflowNodePluginDefinition`：插件以 `outputs`、可选 `inputs`、`render`、`panel`、`toolbar`、`onDoubleClick` 声明节点能力，宿主统一映射到既有 `WorkflowResource` 输出契约。
- 插件只能通过 `WorkflowNodePluginContext` 获取脱敏快照、上游节点、结构化 `applyOps`、隔离 localforage storage 和项目/插件事件；不能导入 Provider、Runtime DB、React store、凭据或直接修改宿主状态。
- 注册表支持 install/update/enable/disable/uninstall，并阻止重复 pluginId/type；禁用或卸载不删除已有项目节点，未知插件节点仍保留为可恢复占位。
- Workflow 已挂载三个 reference plugin：Markdown、Storyboard Card、Style Bible；内置媒体节点、PromptBar、Toolbar 与执行路径保持原有职责。

待用户确认：

1. 在 Workflow 中创建或导入三个 reference plugin 节点，确认渲染、面板、工具栏和双击交互正常，页面刷新后旧项目仍能打开。
2. 对插件节点执行一批 `applyOps`，确认只产生正常 Draft ChangeSet，Undo/Redo、锁定节点和 Browser Workflow authority 不被绕过。
3. 分别禁用、更新、卸载再安装插件，确认节点不会被删除，未启用时显示明确占位，恢复后资源输出和下游引用可继续使用。

## Agent Canvas Contract（Phase G3）

- Agent 的稳定 Workflow surface 已收敛为 `workflow.inspect`、`workflow.selection.get`、`workflow.apply` 与 `workflow.node.run`；`workflow.node.stop` 仍作为执行控制兼容命令存在，但不进入 Document Mutation Core。
- `workflow.selection.get` 返回脱敏的当前选择投影，不写入 Draft、不增加 Workflow revision；Document Ops 仍通过唯一 `applyWorkflowOps`/Draft Authority，View Ops 与 Execution State 分开。
- UI、PromptBar、CLI、Browser Agent 与 DSH Browser adapter 共用同一 mutation boundary；旧 granular `workflow.*` 只翻译为统一 operations。Agent command 默认绑定当前 Browser，先 inspect/selection，再用结构化 `workflow.apply`，生成只经 `WorkflowExecutor`。
- 没有 Browser binding 时，Browser/Agent command 明确返回 `WORKSPACE_UNAVAILABLE`，不会隐式读取或写入 NativeWorkflowStore；DSH Native 只有请求显式携带 `workspaceMode: native` 时才走 Native adapter。
- Agent contract 禁止鼠标模拟、React setter/localforage Workflow graph 直读、Provider 重新解析与第二套 Runtime；Agent source 的 Workflow 命令始终强制 Browser mode。

待用户确认：

1. 连接浏览器后，`workflow.inspect`、`workflow.selection.get`、`workflow.apply` 与 `workflow.node.run` 是否都指向当前可见 Workflow；未连接时是否只显示可行动的 Workspace unavailable 错误。
2. 从 UI 与 CLI 提交同一批节点/连线操作，确认得到相同 Draft、一次 ChangeSet、可正常 Undo/Redo，选择/聚焦操作不改变 revision。
3. 通过 Agent 执行批量结构化操作，确认 Agent 先读取状态、不模拟鼠标、不访问 React/localforage graph，也不会回落到 Native workspace。

## Canvas Platform Convergence 最终集成（Phase G7）

- G1-G6 已完成统一 Mutation/Generation/Agent/Provider/Plugin/Skill 契约的集成回归；Document Mutation、View State、Execution State 保持分层，Browser-bound Workflow 不会隐式回落 Native。
- 使用系统 Chrome 的真实 Browser 会话验证了自动 bootstrap、`status --json` Ready、`workspace.status` 的 browser authority、UI 节点/连线/拖动/删除/Undo/Redo，以及 CLI `workflow.apply → inspect/selection` 同一 Draft 回流。
- 真实 Browser 会话验证了 `plugin:markdown` 节点由 CLI 写入后在 Workflow 中渲染；stale revision 与 mutationId 复用错误均可见。全量 131 个测试文件、926 passed / 1 skipped，typecheck、Web build、DSH build 和 diff check 均通过。

待用户确认：

1. 用默认 `flovart start --open` 流程在自己的可见浏览器中确认页面自动进入 Ready，并在关闭/断开浏览器后显示明确 Offline，不伪造仍可用的工作区。
2. 使用真实 Provider 凭据完成 Graph/`@`/Asset/Runtime Artifact 的 I2I/I2V/首尾帧与自定义 Provider 线路确认；无凭据时不要把本地 fake provider 测试当作第三方账单验收。
3. 在 Workflow 中确认 Markdown、Storyboard Card、Style Bible 的 install/enable/disable/update/uninstall、刷新恢复、锁定节点与下游资源输出行为。

## Agent 目标架构与文档收口

- 新增 [Agent 架构设计包](../../../design/agent/README.md)，用 15 份编号文档统一架构总览、权威职责、外部 Harness、唯一内置 Operator、本地协议、会话交接、Agent Workspace、Workflow 工具、安全、迁移验收、DSH/Flovart P0 契约与 UI 能力门控。
- ADR 0066 与 SPEC-005 固定 DSH Mode 的导航规则：有效 Session 内始终显示 Flovart view；Runtime offline、Agent unavailable 或 Provider unavailable 只阻断对应动作，不隐藏入口；P0 不伪造 DSH 全局 Flovart 页面。
- 新增 SPEC-001 至 SPEC-004，固定 DSH/Flovart P0 的 `ctx.flovart` Service、显式 Session/Project Binding、`expectedRevision + mutationId` 变更契约，以及由 Flovart Runtime 事件重建的 Conversation Node / DSH Job 投影。
- ADR 0061 固定系统只有两个 AI 角色：External Coding Agent Harness 是导演台，Workspace Operator 是唯一内置执行 Agent；Production Crew、Dispatcher、Runtime Worker 与 Review Tool 都不是额外 Agent。
- ADR 0062 固定 DeepSeek Harness 主壳中的 Flovart 原生画布边界：专用 Flovart Profile；session-scoped `conversation.view` 直接渲染原生 Workflow 节点画布，Runtime 持有 Native Draft，当前 Harness 会话建立 DeepSeek Director Binding；WebUI/Dock 只作为 Table、Provider 和恢复降级入口。
- Coding Agent projections 共享 Operation Skill + CLI 模型工具基线；Codex 是当前 professional golden path，DeepSeek Harness 额外保留显式 native Plugin projection，其余 Coding Agent 通过稳定 contract 兼容。WorkBuddy 属于未来 mainstream projection，不在本轮 Director Binding 范围内。
- 明确一对一 Director Session Binding、显式 Handoff、独立 Operator Model Route、Operator Assistance Budget、Production Session Workspace、首次安装确认、兼容集原子升级/回滚与 Harness 崩溃后 ProductionRun 继续运行。
- 已删除迁移跳转壳、损坏的 Docker 指南、过期面试快照、已完成的 RunningHub/S0-S1 施工清单与 LibTV 旧版本对照；现行入口统一回到 `docs/index.md`、ADR、Route Catalog、Runtime 契约和本设计包。
- `docs/README.md` 已收缩为 7 个公开页面入口；Agent 设计、ADR、Runtime 与后端契约只通过内部索引进入，不再在面向使用者的首屏逐篇展开。
- 本节是设计与文档变更，不代表 DeepSeek Profile、Embedded Plugin、Workspace Operator 迁移或各 Host Projection 的实机验收已经完成。

待用户确认：

1. 任一入口是否都能一眼看出只有“外部导演 + 内置 Operator”两个 AI 角色。
2. 是否清楚 Codex 是当前 professional golden path、DSH 是显式 native Plugin projection，其它 Coding Agent 走 stable CLI compatibility；WorkBuddy 不在当前 Director Binding。
3. 是否清楚 DeepSeek Harness 是主壳、Flovart 是 Dock/Workspace 插件，模型工具仍以 CLI 为权威，而完整工作区 UI/事件不是 CLI 轮询。
4. [迁移与验收](../../../design/agent/10-migration-and-acceptance.md) 是否可以直接拆成后续实现批次。

## S1 核心纵切：External Director → CLI → Workspace Operator → Workflow → Receipt

- Canonical Command Registry 新增可用命令族：`director.bind / director.status / director.unbind`、`crew.intent.submit / crew.intent.get / crew.intent.cancel`、`crew.receipt.get`、`crew.event.watch`；registry hash 已重算，Node 与 Rust 共享同一契约并各自校验通过。- 新增 `crew-intent.v1` / `crew-receipt.v1` 协议契约，Crew 事件复用 `runtime-event.v1` 形状，按递增事件游标可恢复。
- Node Workspace Adapter 新增持久 Crew Store（Intent/Receipt/Director Binding/事件，重启后把未完成 Intent 标记 `interrupted` 并给出最终 Receipt，不伪造仍在推理）与确定性 Workspace Operator（只展开 draft-only 意图：读取选择 → 逐图 `create-connected` 建并行图生视频分支 → 每步观察 Draft/Object Version → 返回 ChangeSet/Receipt）。
- 浏览器 Draft Authority 已接受 `operator` 来源：draft-only 写动作进入同一语义 ChangeSet 且不逐命令确认，delete/run 等高风险命令仍要求确认。
- CLI 经同一 loopback + token Workspace Adapter 协议调用 Crew 命令，调用前先做协议版本与 Registry Hash 握手校验；`crew.event.watch --jsonl` 输出逐行事件并携带游标。

待用户确认：

1. 启动 Workspace-only Operator 并连接真实 Workflow Authority 后，`crew.intent.submit` 能把所选图片真实建成并行分支，且画布 ChangeSet/时间线可见。
2. 执行中手工修改同一节点，Operator 应返回 `waiting/PRECONDITION_CONFLICT` 并列出冲突对象，而不是覆盖人工修改。
3. 关闭 Agent 进程后 `crew.intent.get / crew.receipt.get` 仍能读取持久 Intent 与中断 Receipt；重启时未完成 Intent 标记 `interrupted`。
4. 相同 `idempotencyKey + payload` 重放原 Receipt，不同 payload 返回 `IDEMPOTENCY_CONFLICT`；`director.bind` 的 Registry/Adapter 契约已覆盖五类支持的 Agent Identity，实际外部 Host 可用性以本页 External Agent 证据与待确认项为准。
5. 无浏览器连接时提交 Intent 返回诚实失败（`OPERATOR_FAILED`/无法读取 Workflow），不伪造完成。

## DeepSeek Harness 插件包（S3B Harness 侧纵切）

- `@flovart/dsh-plugin` 现在只注册 RC8 原生 `conversation.view` 与 `shell.overlay`，并按公开 `ctx.slots.inject(...)` 生命周期挂载；不占用根 `sidebar` / `conversation` / `conversation.session`，也不再注册 `sidebar.footer.action`。
- `npm run dsh:start` 会安装匹配的 `0.1.0-rc.8` Profile、准备 Workspace-only Operator、注入 Host 私有连接信息，再以随机可用端口启动 Harness；默认开发与 Tauri 启动不再预热 ManagedAgent。
- Browser Client 只调用 Harness 同源 `/flovart-workspace`；Host 白名单仅转发健康检查、原生工作页注册、Workflow 命令和 Director Binding。Workspace Token 不进入浏览器、节点、Draft、回执或 Provider 参数。
- Native Workflow Store 不再在激活时制造空项目；没有内容或已有项目为空时，原生页先显示 Production Brief，引导创建首个文本节点，也允许显式空白开始。
- Agent 主入口和 Workflow 右侧区已由第二套 Flovart 聊天改为 Production Crew 状态面；当前入口显示 Host Projection/Director Binding 摘要，不把某个 Coding Agent 写死为唯一产品 Host。
- RC8 Client bundle 继续满足 `window.__ModuleLoader__.load({id, factory})` 契约；新增 Slot 注册、同源代理、Brief 引导、Workspace Store 和默认启动行为回归测试。

已完成的真实验收（DeepSeek Harness `0.1.0-rc.8`）：

1. 真实 Profile 安装与 `--dump-config` 通过；Harness 启动后插件进入 boot 图谱，`Flovart` 原生页签可见。
2. 首屏展示 Production Brief，引导创建后进入 `Draft v2`，画布出现 `Production Brief` 节点并绑定当前 Harness Session。
3. 页面 iframe 数为 0、地址输入数为 0、Token 输入数为 0、外部 Flovart 侧栏按钮数为 0，浏览器无 page error / console error。

待用户确认：

1. 页面重载、Harness 崩溃、Workspace Operator 重启后，Draft、Binding 和状态是否按预期恢复。
2. 多 Harness Session / 多 Workflow Project 切换是否需要更明确的 Handoff UI。
3. 连接失败和版本不兼容的真实错误面，以及失败升级的原子回滚。
4. Agent / Workflow 中 Production Crew 状态密度是否足够，是否需要直接增加 Approval、Task 和 Artifact drill-down。

## DeepSeek Dock 插件页面（S3B Flovart 侧纵切）

- 新增 `#/dock` 可嵌入路由：左侧固定 Flovart Dock（waiting/error/Artifact 徽标）、中央 Production Control（Director Binding、Intent 提交/列表/取消、Receipt 详情、Crew 事件流、产物汇总）、右侧可折叠 Agent Bridge（已发现 Host Projection 连接状态与单导演说明）。
- 新增 `flovart-dock` v1 postMessage 协议：Harness 可发送 `handshake / focus-surface / theme / session.bound / session.unbound`，Flovart 发布 `ready / badges / intent.status / receipt.completed / open-surface / open-window`；版本不匹配时保留只读模式并输出可复制 CLI 命令。
- 前端 Crew 客户端复用同一 loopback + 短期 Token 通道（`flovart.agent.url` / `flovart.agent.token`），只允许本机 loopback；配对失败留在连接面并返回 `{ code, message, retryable }`，不向 Harness 泄漏凭据，也不再盲探测未监听的默认端口。
- `dsh-plugin/assembly.json` 兼容集清单与 README 现在锁定原生 `conversation.view`、Native Runtime Draft、CLI 工具派生、隐私边界和 WebUI 降级入口；历史 Dock 协议仍服务 Flovart 侧控制页，不是原生画布主协议。
- `#/dock` 保留为独立 Flovart 控制/调试表面，不再由 Harness 侧栏或原生页主路径打开；Table/Provider 高级面仍属于独立 WebUI。会话切换、崩溃恢复和升级回滚继续待测。

待用户确认：

1. `#/dock` 页面在本机浏览器/独立窗口可打开，未连接时明确显示开发者配对说明，不把未认证状态误显示为 Director/制作控制已连接。
2. 连接 Workspace Operator 后能提交有界 Intent、看到真实执行状态与 Receipt，画布 ChangeSet 同步可见。
3. 模拟 Harness postMessage `focus-surface` 可切换表面，徽标随 waiting/error/Artifact 计数更新。
4. Agent Bridge 明确显示“同一项目只有一个 Active Director，切换必须显式 Handoff”。
5. Dock 不再作为 Harness 原生页验收条件；只需验证其作为独立调试面没有误导为第二个主入口。

## Production Skill 包、Skill 台与首页入口

- 当前工作树新增本地 Skill Package Registry、稳定内容 Hash、Manifest 校验、安装/卸载保护、Hub Client/Store 与 CLI Skill 命令；远端安装只允许 HTTPS 或 loopback，内置包不可被普通卸载覆盖。
- 首页 Skill 台可同时展示内置 VOX 示例与本地已安装 Production Skill；Hub 条目跳转外部详情并提供本地安装入口，不把远端目录伪装成本机已安装能力。
- 首页创意输入创建真实项目和文本节点，再直接打开 Workflow；旧 ToC/ToB Landing mock、模板和演示动画已删除。
- Skill 详情统一使用“准备到 Workflow”：创建带 `Production Brief` 文本节点的项目，不再连接旧内置主 Agent，也不自动发送、调用 Provider 或产生费用；后续由当前 Host Projection 中的 Director 推进。

待用户确认：

1. 首页能区分内置、本地安装和 Hub 中的 Skill，并正确显示版本、来源和安装状态。
2. 安装、重复安装、卸载、内置包保护与 Hub 失败都给出可理解错误。
3. 从首页输入创意或选择 Skill 后，Workflow 中出现真实项目/文本节点，页面不再跳转到第二套 Agent composer。

## Workflow 布局、媒体节点与浮层

- Workflow 自动布局使用确定性分层算法，覆盖链路、交叉边、孤立节点、隐藏节点、循环图与不同节点高度；人工固定位置不应被无关操作覆盖。
- 图片、视频、文本、脚本、音频、配置与 Operation 使用统一媒体卡语义；视频优先显示持久 Poster，显式激活后只挂载一个播放器，Esc、切换选择或进入工具时卸载。
- 底部添加菜单向上展开；Agent 面板打开时 Dock 按剩余画布宽度让位；PromptBar、ElementToolbar、视频控制和底部 Dock 需要在浅色/深色主题下保持无遮挡。
- Workflow 媒体元素本身必须可直接选中，并稳定挂载 PromptBar 与 ElementToolbar；本地图片/视频拖入不能只依赖 MIME。

待用户确认：

1. 对包含分支、回路、孤立节点和不同尺寸媒体的项目执行整理，确认无重叠且固定节点不移动。
2. 导入本地视频后确认默认只有 Poster，加载后可播放/拖动，Esc 恢复 Poster。
3. 同时展开 Agent、PromptBar、ElementToolbar 与底部 Dock，确认媒体控制可点击且无遮挡。

## Browser Import V1

- Edge/Chrome 扩展收缩为 Desktop-first 薄伴侣：右键单图经明确配对、Native Messaging 分块、SHA-256 与 MIME 字节签名校验进入内容寻址 Artifact，再投影到活动 Workflow 或 Browser Import Inbox。
- 扩展不保存 Provider Secret、不直连 Provider、不内嵌完整 WebUI、不常驻 `<all_urls>`，也不扫描整页图片；跨域图只临时申请精确来源权限并在读取后撤销。
- Desktop 离线时 Native Host 可有界尝试唤起；未配对、来源不符、分块错序、Hash/MIME 不符、目标项目消失或超时都不得误报成功。
- 正式商店 ID、Native Host 注册与 NSIS 安装/卸载 Hook 尚未完成发布验收。

待用户实机确认：

1. 用真实 Edge/Chrome 扩展 ID 注册 Host，首次连接能看到并批准准确 Origin。
2. 分别导入同源图、跨域 CDN 图与大图，确认比例、来源、Hash 和临时权限撤销。
3. 无活动项目时只进入 Inbox；Desktop 关闭时唤起或失败状态准确，不出现幽灵节点。

## Workflow Operation、Draft ChangeSet 与 VOX

- 图片生成/裁剪/放大、视频剪辑/音视频分离/拼接/抽帧、音频截取/变速使用同一 Operation Registry、Recipe、Prompt Document、Input Binding、Execution Snapshot 与 Take 语义。
- Operation 形成“输入媒体 → Operation → 类型化输出媒体”；重跑追加 Take，不覆盖源媒体。失败、取消与旧 Recipe 晚到结果可追溯，旧结果不能自动替换当前选择。
- @ 引用完整性修复：原位替换产生的隐藏输入节点进入 chip/下拉/水合/运行时共享的同一输入集合（别名编号一致）；非批量原位重生成只在旧 storageKey 无其它持有者时物理删除；被引用媒体缺失时抛出指名节点的可行动错误；@ 名称未匹配时运行报错给出指名提示而非静默丢弃；mention 徽章与下拉对不可达缩略图回退类型图标。

- VOX Skill 使用同镜头多主题 Bake-off，用户批准的图片 Artifact 是后续关键帧风格参考；关键帧与 OCR Gate 未通过时不得进入 image-to-video。真实付费视觉质量仍需单独批准 Smoke Test。

待用户确认：

1. 每种媒体至少执行一次 Operation，确认源媒体、角色边、Operation、输出和 Take 均可追溯且刷新后仍在。
2. 运行中修改 Recipe，确认晚到结果标记为旧配方且不覆盖当前选择。
3. 手工改动 Agent 将要编辑的节点，确认对象版本冲突会拒绝旧动作而不是最后写入覆盖。
4. VOX 只在用户选定风格样图、批准关键帧与 OCR 后进入付费视频阶段。
5. 对一张生成结果再次 @ 引用并运行图生图，确认不再误报「至少需要引用 1 个媒体节点」，chip 显示真实缩略图或类型图标而非破图。
6. 故意把 @名称 写错后运行，确认报错指名该引用并说明加入方式，而不是笼统的“至少需要引用”。
7. 克隆/粘贴共享同一张图的节点后对其中一个原位重生成，确认另一节点的图仍能正常显示。

## Production Runtime、CLI 与 Provider Route

- 当前基线包含安全本地 Control Plane、Canonical Command Registry、持久 Task/Event、Artifact、Production Dry Run、Workflow Projection，以及首批真实图片/视频 Route；实际可用命令必须以 `command.list` / `command.schema` 为准。
- RunningHub Route Catalog 以 Product Model + Generation Mode + Route Capability Schema 约束 17 条已登记 Route；未登记或未验证 Route 可展示但不得执行，用户参数不能被未声明默认值静默覆盖。
- Provider Secret 仍由当前运行边界持有；CLI、Operation/Production Skill、Workspace Operator、Receipt 与日志不得输出原始密钥。
- `command.list` 仍含 legacy-only 路径和旧安装文案；旧 Managed/Codex 适配器内部也仍保留 MCP transport。它们不是正式外部接入面，迁移完成前必须继续列为待删项。ProviderAttempt、Submission Unknown、真实账单、远端取消、完整 Draft Authority 转移与发行态恢复仍在 todo，不得把 Runtime V1 写成已全部完成。

待用户确认：

1. Desktop/WebUI/Harness 分别断开后，已持久提交的 Task/ProductionRun 继续运行并可按事件游标恢复。
2. 对一条最低成本图片 Route 和一条视频 Route，在单独批准费用后验证认证、提交、查询、Artifact、Workflow 投影与费用记录。
3. 未配置、未验证、预算不足、提交结果不确定和 Provider 不支持取消时，状态与提示均诚实可见。

## 社区、企业与分发边界

- Enterprise 的组织、部门、角色和权限前后端仍需真实账号、权限拒绝、跨组织隔离和 PostgreSQL 联调验收。
- 项目、素材和生成记录当前主要保存在浏览器本地；社区发布、云同步和共享素材不能写成已完成。
- Docker Compose 当前只用于 Web/Hub/Enterprise/PostgreSQL 本地联调，访问端口为 `1635`；静态资源生产路径、安全配置和正式部署尚未验收。
- Windows 安装签名、GitHub Release 附件、Edge Add-ons、macOS/Linux Toolkit 与升级回滚仍需发行态实测。

## 发布前复核

本轮文档收口不替代代码回归。进入发布候选前至少需要重新执行：

- `npx tsc --noEmit`
- `npm test`
- `npm run build`
- `npm run ext:build`
- `cargo test --all-targets`（`src-tauri`）
- 真实 Workflow、Desktop Runtime、Edge 扩展与各 Host Projection 的可见端到端路径

涉及 Provider 费用、外部发布、系统注册表、正式安装器或账号权限的验收必须单独取得用户确认，不进入默认自动测试。

## External Agent Golden Path（本轮新增）

- 已实现并通过本机验证：PATH-only Host discovery、Codex/Claude/OpenCode Skill projection、动态端口 Web 启动、Bootstrap Coordinator、Browser Workflow 自动重新注册、Active Writer、五个稳定 Agent 工具和 DSH RC8 profile/bundle projection。
- `flovart start --open --json` 的 CLI bootstrap contract 已通过：可复用已有 WebUI、启动本机 Agent 并生成带短期 bootstrap 的主页面；自动化验收改由 Chrome for Testing 直接打开 bootstrap URL，Windows 系统 URL handler 不再作为当前自动化通过证据。不会要求手工输入 URL、Token 或端口。
- Launcher Writer 已进一步收敛：CLI 打开的新页面获得一次性 `activateBrowserWriter=1`，认证后自动清理 URL 并在首个 snapshot 成功后激活为 Writer；Coordinator 会等待新 client 成为 Active Writer，不会把旧标签的 ready 状态误报为当前连接。隔离双标签 smoke 已验证同项目切换、token/flag 清除和一次性 session signal 消费；证据为 `C:\tmp\flovart-bootstrap-writer-20260829.json` 与 `C:\tmp\flovart-bootstrap-writer-20260829.png`。
- 真实浏览器 tracer 已覆盖 `inspect → apply`（新增/移动/连线/修改节点）、刷新、Undo/Redo、双 Tab、关闭页面后的 `WORKSPACE_UNAVAILABLE`、显式重新激活和 Runtime 重启恢复；证据保存在本机 `C:\tmp\flovart-external-agent-golden.json`、`flovart-writer-recovery.json` 与 `flovart-recovery-control-20260829-f.result.json`。
- DSH 已真实完成 profile install、dump-config、boot、浏览器无 plugin error，以及隔离 Native Workspace 的 `create/apply/inspect`；Workspace Operator 被精确终止后自动重启，Native view 增加重注册；重启后的 CLI 已完成 Director bind/handoff/status，旧 Binding 归档。没有可见 Browser Workflow 或 Provider 时，`run` 明确返回不可用，不伪造执行结果；profile uninstall 只移除应用管理的 Flovart profile。
- Claude Code 与 OpenCode 已分别完成真实 `status → workflow.inspect → workflow.apply → workflow.inspect` CLI tracer，未调用 Provider；临时节点及清理 receipt 记录在 `C:\tmp\flovart-external-host-tracers-20260829.json`。
- Host Picker 已完成真实浏览器 smoke：从带 bootstrap 的 `/app` 进入 Agent Workspace，显式选择 Claude Code 后，认证请求 `/hosts/prepare` 返回 200，页面显示“Claude Code 的 Flovart Skill 已准备”；选择 DeepSeek Harness 显示“由其 Plugin/Profile 管理”，WorkBuddy 仍为禁用的未来入口。地址栏已清理，普通 UI 未泄露连接实现细节。证据在 `C:\tmp\flovart-host-projection-20260829.json`、`C:\tmp\flovart-host-projection-20260829.png`、`C:\tmp\flovart-dsh-picker-smoke-20260829.json` 与 `C:\tmp\flovart-dsh-picker-smoke-20260829.png`，不含原始 Token。
- Host Picker 的发现请求已与 CLI/诊断分层：页面只请求轻量 PATH 发现（`includeVersion=false`），不会为普通选择器同步探测版本；版本与可执行文件详情仍只在 `host.list`/Advanced Diagnostics 中提供。完整 Vitest 为 136 个文件通过、972 passed、1 skipped（973 total）。
- 开发者 Dock 的未绑定 Director 状态不再用时间戳伪造 DSH sessionId；现在只提供可复制的显式 `director.bind` 命令，真实 Harness/Plugin Session 才能建立绑定。
- 打包路径已补齐：`npm pack --dry-run` 包含 Agent/Host/Skill 模块；隔离临时项目实际启动 packaged Managed Agent，PATH discovery 返回 7 个 Host，并把 Codex projection 写入调用方项目而不是 bundle 目录。Toolkit launcher 传递调用方 `FLOVART_PROJECT_DIR`，失败启动会清理子进程；脱敏记录为 `C:\tmp\flovart-packaged-agent-smoke-20260829.json`。
- 新鲜 bootstrap 浏览器会话已通过 canonical CLI tracer：自动认证并激活 writer 后，`workflow.inspect → workflow.apply → workflow.inspect` 实际新增两个节点、移动、修改、连线并在页面显示；记录为 `C:\tmp\flovart-canonical-cli-browser-golden-20260829.json`，截图为 `C:\tmp\flovart-codex-golden-20260829.png`。这是 CLI/Browser 闭环证据，不替代用户登录 Codex 后的真实对话验收。
- 安全执行 fixture 已在真实 Browser binding 上通过：隔离临时 Workflow 先用本地 `image.crop@1` 建立 Operation，再由 `workflow.node.run --agent-identity codex` 成功追加 Take 和输出节点；未调用 Provider 或付费接口，随后删除临时项目并恢复原项目 revision 10。证据为 `C:\tmp\flovart-browser-node-run-local-fixture-20260829.json`；Provider 成功仍仅由已有 fake/wire 测试覆盖。
- granular `workflow.node.tool` CLI 适配器已补齐数值、数组及 dashed alias 的参数归一化，避免字符串 flag 直接触发严格 Operation schema 拒绝；`workflowCli` + `workflowDispatcher` 聚焦回归 26/26 通过。该命令仍是 CLI compatibility adapter，不进入五个模型-facing tools。
- DSH CLI 的 object/array 参数已改为 JSON flag；Rust Runtime Registry Hash 与 canonical registry 已重新对齐，Rust 全量 targets 通过。
- 历史 Windows URL-handler smoke 曾在系统默认 Edge 上通过，但已不再作为当前自动化验收方法；当前回归统一使用独立 Chrome for Testing profile、`--no-open --web-port=0 --agent-port=0` 和一次性 Browser bootstrap。旧 Agent 进程持有 stale client 时当前行为仍是显式启动/Workspace 失败，不会静默回落 Native；该恢复边界已记录在 HANDOFF。

待用户/外部环境确认：

1. 登录 Codex CLI 后，从新 Codex 对话执行“打开 Flovart”，确认 0 次 URL、Token、端口、JSON 手工配置，并保存 transcript；当前机器 `codex login status` 为 `Not logged in`，此前 `codex exec` 返回 401，未触发付费请求。
2. 首次执行 `flovart start --open`、Host 切换和手动画布增删改连线的用户可见确认；默认启动的命令级证据已通过。
3. DSH 真实登录态页面重载、升级回滚以及 Browser/Native Authority transfer；Workspace 崩溃重启、state isolation、多 Session Handoff 和 profile uninstall 已有本机证据。
4. Provider 仍只使用 local fake/wire fixture；真实付费图像或视频生成、账单、取消和生产发布需单独批准。

## Release Candidate 认证收口

- DSH Native view 的 loopback proxy 现在只接受带显式端口的 `127.0.0.1` HTTP 地址，并从固定 loopback origin 重建出站目标；路径、方法、查询字段、凭据、localhost、IPv6 和无效端口均有回归覆盖。
- Desktop release workflow 的写权限与 attestation 权限已从全局收窄到实际发布 job；YAML 权限范围由 release red-team 检查。
- 当前候选已重新构建并通过 NSIS artifact checksum/SBOM 校验、安装、启动、优雅关闭和卸载；Hosted CodeQL、生产签名、GitHub 发布和真实 Provider/Codex 认证仍是外部门禁。
- 浏览器启动回归已改用独立 Chrome for Testing profile：普通动态 WebUI origin 不会建立 Browser binding，带一次性 bootstrap 的页面真实建立 `clients=1 / hasWorkflow=true`；源码 WebUI 与 Browser Agent 通过 `--web-port=0 --agent-port=0` 使用随机 loopback 端口，故意占用 `37522` 时也会自动回退且不触碰占用者。Windows 默认 Edge 不再作为自动化验收浏览器。
- Skill 启动说明和 `web.open` 无服务提示已移除“单独运行 `npm run dev` 即可连接 Agent”的歧义；源码路径统一使用 `flovart-cli start --source --web --open`，并明确 `37522` 只是首选端口、普通 origin 不含 Browser bootstrap。当前工作树复跑 full Vitest `154 files / 1035 passed / 1 skipped`、docs/version/red-team/secret、TypeScript、Web、扩展、DSH、Rust 和 critical `10/10` 均通过。
- Skill projection 的 Node.js 最低版本已与 `package.json` 的 `22.19.0` 对齐；docs contract 现在从 package engine 自动校验三份源投影与生成投影，避免 clean install 按过时版本说明执行。
- 自动化浏览器验收现在固定使用 Playwright Chrome for Testing、隔离 profile 与 `--no-open --web-port=0 --agent-port=0`；当前复跑为 `clients=1 / hasWorkflow=true`、零 console/page error，且 `37522/17373/1635` 均无残留监听。Docker Compose 的 Web 端口默认值已与实现对齐为 `1635`，冲突时由 launcher 按实际依赖闭包选择可用且互不重复的 host port；Dockerfile 也已从 Node 20 对齐到 Node 22。当前工作树新增回归后 focused dev-command 为 `20/20`、full Vitest 为 `154 files / 1040 passed / 1 skipped`，DSH audit 固定官方 registry 后为 `0 vulnerabilities`。
