---
title: 待测试确认
---

## VOX Director 内置示例 Skill

- 新增 `.agents/skills/vox-director` 标准 Skill 包：本地 Coding Agent 与应用内 Skill 台读取同一份 `SKILL.md`、`flovart.skill.yaml`、ProductionSpec 扩展 Schema、创意指导、评测用例和示例 ProductionSpec，不再由首页维护一份脱离运行契约的 VOX 展示假数据。
- VOX 上游工作流已改造为 Flovart 的 Provider-neutral Director Skill：保留叙事节拍、纸张拼贴、风格试片、关键帧审片、OCR 和音频设计；示例包不读取 API Key、不直连 Provider、不负责轮询、下载或渲染，只声明 Runtime Capability 和人工 Gate。
- `/app/home` 的 Skill 台现在显示真实 `community.vox-director` 内置示例，可打开来源、安全边界、制作能力与导演检查点详情；“用示例创建项目”会创建 `VOX Director 示例` 并进入真实 Workflow。目前尚未自动绑定 ProductionSession 或提交 ProductionRun。
- 自动验证已通过：目录解析、扩展 Schema、安全边界与质量门定向 Vitest 6 项、Skill 标准结构校验、VOX 示例质量门 100/100、TypeScript 与生产构建；Playwright 在真实 `/app/home` 验证卡片、详情、源码入口和创建项目跳转，页面无运行时错误。全量 Vitest 为 81 个文件、638 项通过、1 项跳过，仍只有 `tests/workflowGeneration.test.ts` 的 6 项既有 Provider reference 失败。
- 待用户验证：打开应用首页确认 Skill 台的 VOX 卡片和详情符合预期；点击“用示例创建项目”后确认 Workflow 项目名正确。第三方 Skill 的安装、锁定、签名、发布和撤销仍是待办。

## ProductionRun 与真实 Workflow Plan Projection

- 新增 `production.dry-run`：Coding Agent/CLI 可把一个 Provider-neutral Director ProductionSpec 交给 Desktop Runtime；Runtime 在一个 SQLite 事务中创建 ProductionSession、不可变 Spec Revision、ProductionRun、StageRun DAG、依赖和 Workflow Plan Projection，并通过原有 Task/Event Ledger 返回可恢复 Task。
- 新增 `production.status` 与 `workflow.projection.get`。当前 Dry Run 不提交 Provider、不扣费；缺少 Route Plan、Run Budget、TTS/音乐/渲染/验证能力时明确保持 `action_required`，不会把已编译计划冒充为可执行成片。
- 顶层 Production Projection Adapter 每 1.5 秒读取 Runtime 权威投影并自动合入匹配的真实 Workflow。它只替换 Runtime-owned 投影节点/依赖，保留用户节点、用户连线、选择和视口；相同 Session/Stage 的稳定节点身份会保留用户手动移动、缩放和锁定状态。
- 真实 VOX 验收已通过：`production-spec.v1.json` 的 VOX Gate 为 100/100；PowerShell CLI 创建 `production_run_019f9d1f-44c6-7ac0-9b28-2614d02f3b3d`，持久化 20 个 StageRun 和 21 个投影节点/19 条依赖；无 Playwright/CDP 回读真实画布得到 21 个投影节点，同时保留原“VOX 镜头 1（已细修）”，Workspace `clients: 1`。
- 自动验证：Rust 全量测试通过；ProductionRun/Projection 定向 Vitest、TypeScript、Web 生产构建、Flovart Skill 结构校验与 63.8 kB CLI npm 发布包 dry-run 通过。单线程全量 Vitest 为 79 个文件、634 项通过、1 项跳过，仅保留 `tests/workflowGeneration.test.ts` 的 6 项既有 Provider reference 失败。
- 待用户验证：打开项目“真实CLI同步验收-稳定连接”，确认 `VOX 1776 Production Plan` 及 20 个阶段节点可见；手动移动任一计划节点，等待多轮同步后位置不应回弹。Artifact 到 Stage 节点的自动挂载、Route Plan、预算、审批和正式 `production.run` 尚未完成。

## Desktop Managed Agent 自动连接

- Tauri Desktop 启动时会幂等拉起 Managed Agent，并通过受限 IPC 向 WebUI 返回本机 loopback URL 与连接 Token；Token 不再打印到终端，也不需要用户复制到 Agent 面板。
- Workspace Adapter 已挂到 `RouterHost`，Landing、Workflow、Table、Agent 任一路由和空项目首启都会保持连接。项目创建或切换只推送最新快照，不再重建 SSE，避免命令已执行但回传断开造成“假失败”；并发快照会串行压缩到最新版本，StrictMode 异步 discovery 也不会遗留幽灵连接。
- 不使用 Playwright/CDP 的真实验收已通过：独立 PowerShell CLI 看到 `runtime.status=ready`、`workspace.status=ready`，随后成功创建并切换项目、创建文本节点、细修标题与 VOX Prompt、移动节点，再由 `workflow.inspect` 读回相同节点与坐标。应用内保留项目“真实CLI同步验收-稳定连接”供用户肉眼确认。
- 自动验证已通过：Managed Agent/Workspace 定向 Vitest 5 个文件 15 项、Rust Managed Agent Contract 2 项与 `cargo check --all-targets`、`npx tsc --noEmit`、`npm run build`。不用 CDP 的最终重启验收为 `clients: 1`。全量 Vitest 为 78 个文件通过、1 个既有 `workflowGeneration.test.ts` 文件的 6 项 Provider reference 断言失败。待用户验证：关闭并重新打开 Desktop，不进入 `#/app`、不打开 Agent 抽屉，直接运行 `node tools/flovart/cli.js workspace.status --json` 应返回 `ready`；打开 Workflow 后应看到上述验收项目及位于 `(360, 220)` 的“VOX 镜头 1（已细修）”节点。

## 统一模型映射中心

- 设置页已删除全局“模型偏好”和 API Key 弹窗中的重复模型绑定，只保留一个“模型映射”入口；提示词增强、脚本拆解、Agent 文本、图像理解四种文本能力与媒体产品模型都在这里绑定 Provider Route。
- 映射页已改为“图像模型 → 视频模型 → 文本与 Agent”的媒体优先顺序。API Key 返回模型列表后会自动匹配产品模型与 Provider Route，并显示待确认建议；只有点击“应用全部建议”才会写入，不会静默启用误识别线路。没有返回模型列表时不会凭空生成建议。
- 每个映射目标按“主线路 / 备用 1 / 备用 2”表达优先级，可直接添加、删除和上下调整，不再暴露 `priority`、`enabled`、`confirmed` 等难懂字段。自动建议同样只包含当前 Provider Adapter 已实现的生成模式。
- Workflow 图片/视频节点和 Table 抠图/换装必须先明确选择产品模型，再按“产品模型 × 生成模式”解析线路；没有选择产品模型或没有对应映射时会在提交前阻止运行，不再回退到全局偏好、最近使用或第一条可用模型。
- 视频 PromptBar 的比例、时长和可选能力已改为“产品模型能力 ∩ 最终 Provider Route Schema”；例如只接受 8 秒的 RunningHub 首尾帧线路不再显示 4 秒或 6 秒，Google Veo 只显示官方声明的 `16:9` / `9:16`。
- 新建或历史遗留的空模型图片/视频节点会自动采用当前生成方式下第一个已映射产品模型；视频 PromptBar 的“参数”入口始终可见，尚无可用模型时点击会先打开模型选择，不再把比例和时长功能完全隐藏。
- Workflow 的 `@` 引用在最终线路边界校验媒体类型与数量：已验证 RunningHub Route 全部由 Catalog media schema 驱动；Google Veo、Seedance、Kling、xAI 等直连 Adapter 使用明确上限。不支持或超量的 `@图片/@视频/@音频` 会在 PromptBar 阻止生成并说明原因，不再静默丢弃。RunningHub V3.1 Fast 图生视频可按线路上限把 3 张有序图片传入 `imageUrls`。
- 文本相关调用按 Runtime Capability 独立解析线路。主线路提交前不可用而备用线路可用时，会显示主线路与备用线路并要求用户确认；用户拒绝则停止提交，Provider 已开始任务后不会自动改走备用线路。
- 自动验证已通过：`npx tsc --noEmit`、Vitest 66 个文件（583 项通过、1 项跳过）、`npm run build`，以及不调用付费生成的 Playwright 真实 Workflow 回归；浏览器中已确认新建映射视频节点自动显示 Seedance 2.0、比例和秒数。构建只保留既有 chunk size 与 RunningHub 静态/动态混合导入警告。
- 待用户验证：① 添加或刷新一个同时返回图像/视频模型的 API Key，确认映射页先显示媒体建议且点击前不写入；② 应用全部建议后刷新，确认图像、视频、文本映射顺序和主备线路保持；③ 新建视频节点，确认自动选中首个已映射模型并立即显示参数，再分别打开 Veo 首尾帧、Seedance 多模态确认比例与秒数随线路变化；④ 用不支持的 `@音频` 调 Google Veo，确认生成被阻止并显示原因；⑤ 用 RunningHub V3.1 Fast 图生视频按顺序 `@` 三张图片，抓包确认 `imageUrls` 保留三张。

## 浏览器本地业务存储收口

- Provider API Key 的浏览器持久化从 `localStorage` 迁到加密的 `localforage` Vault；旧版密文与盐会一次性迁移，明文旧 Key 会加密后迁移。桌面端后续仍以系统 Credential Manager 为最终存储边界。
- Workflow 项目与媒体 Blob、Agent 工作区布局、生成历史、素材、社区内容、用量记录、模型选择和 Provider 模型缓存统一通过独立的 `localforage` store 写入 IndexedDB；生成历史与 Workflow JSON 不内嵌视频等大二进制。
- 本机 Agent 连接 Token 和 Hub JWT 改为 `sessionStorage`；Hub 会在首次读取时迁移并删除旧 `localStorage` 明文 Token。主题、语言、面板宽度等小型同步 UI 配置仍可保留在 `localStorage`。
- 已通过 `npm test -- tests/browserPersistence.test.ts tests/generationHistory.test.ts tests/agentWorkspaceStore.test.ts tests/workflowStore.test.ts tests/mediaDB.test.ts tests/workflowMedia.test.ts`（6 个测试文件、36 项测试）及 `npm run build`。待用户验证：保存 Workflow/视频与 API Key 后刷新仍可恢复；关闭会话后 Agent/Hub Token 不继续持久化；旧版本 API Key 首次打开后可正常迁移。

## Flovart Skill、CLI、Agent Toolkit 与发布链路

- npm 发布包已统一命名为 `flovart-cli@0.3.0`，确保用户可直接使用 `npx flovart-cli ...`；包含 CLI、TUI、MCP Server、Flovart Skill、Managed Agent、Host 适配与 Toolkit 安装器。
- Canonical Command Registry 当前导出 53 条显式命令；31 条命令标记为 `available`：2 条无需 Desktop 的客户端 Registry 查询、12 条 Production Runtime 命令、16 条可见 Workspace 命令与 1 条 Research Adapter 命令。其余迁移期命令明确标记为 `legacy-only`；`command.list` 不再出现 Canvas / Element，`command.schema --command workflow.run` 返回 `UNKNOWN_COMMAND` 并以非零状态退出。
- Production Runtime S0.1/S0.2 已建立 Node/Rust 共用的版本化 Registry 与 JSON Schema，并完成安全本地 Control Plane：Desktop 监听 `127.0.0.1:0` 随机端口，每次启动生成 256-bit Token，把协议、Runtime Instance、Registry Hash、端口和 Token 原子写入仅当前用户/SYSTEM 可读的 Discovery Record；退出时只删除自己的 Record。
- CLI、MCP 与 Native Host 读取 Discovery Record 后，只能调用鉴权的 `GET /v1/status` / `POST /v1/commands`；WebUI 通过受限 Tauri IPC 进入同一个 `ProductionRuntime`。固定 `7421`、CDP/`window.__flovartAPI` 生产探测、任意 Native Host HTTP path 转发和原始 Secret HTTP GET 已删除。
- 当前可执行 `runtime.status`、客户端 Registry 查询、`runtime.test.delay`、Task/Event 查询与取消、`provider.status`、`generate.image` 和 `generate.video`；真实 RunningHub GPT Image 2、Grok Imagine Video 1.5 低价路线与 Veo 3.1 Lite 已进入唯一 Desktop Runtime。旁白、字幕、成片合成与交付校验仍由评测目录脚本完成，尚未伪装成 Production Runtime 的正式命令。扩展端桌面 Key 同步继续关闭，待后续类型化 Native Messaging Provider 协议落地；扩展本地 Key 仍保存在自己的加密存储中。
- Flovart Skill 已升级为 Production Control + visible Workspace 总控说明：Provider Task/Artifact 仍由 Runtime 管理；项目、节点、连线和视口修改经 Workspace Adapter 回到 WebUI 的同一个 `workflowDispatcher`，与用户手动操作共享浏览器 Workflow store。Skill 强制先检查 `workspace.status`、inspect 后修改、写命令使用稳定幂等键，并禁止 shadow runtime/Vite file queue 回退。
- CLI/MCP 现在分别通过 `runtime-command-surface` 与 `workspace-command-surface` 选择深模块 Adapter：`provider.status` / `generate.video` 进入 Desktop Production Runtime；16 条 Workspace 命令进入受本机 Token 保护的 Managed Agent loopback，再由拥有最新项目快照的浏览器执行。未连接项目时稳定返回 `WORKSPACE_UNAVAILABLE`，不会显示假成功。
- CLI 是 Node.js 程序而不是 PowerShell 脚本；PowerShell、macOS zsh/bash 使用同一组单行命令。WSL 可把 Managed Agent 运行在同一发行版；若复用 Windows Agent，则通过 `FLOVART_AGENT_CONFIG` 读取 Windows 配置并要求 mirrored networking，使 `127.0.0.1` 仍保持安全边界。macOS/Linux 安装后不会自动修改 shell 配置，会提示把 `~/.flovart/bin` 加入 PATH，或继续使用 `npx flovart-cli`。
- 自动验证已通过：S0.2 定向 Vitest 5 个文件 23 项测试、Rust Native Host/Runtime Contract/Control Server 8 项测试、`cargo check --all-targets`、`npx tsc --noEmit`；全量 Vitest 65 个文件 562 项通过、1 项跳过，Rust 全套测试、Web 生产构建、扩展构建与 CLI npm 发布包 dry-run 也通过。Desktop 未启动时直接 CLI 在约 0.5 秒内稳定返回 `RUNTIME_UNAVAILABLE`。待用户验证：启动 Desktop 后 CLI `runtime.status` 返回同一 Runtime Instance；WebUI 的 Key 写入与脱敏 metadata 列表仍可用。
- `npx flovart-cli install` 默认下载版本化 Agent Toolkit，校验 manifest 的字节数与 SHA-256 后才解包、切换 `current` 和写入本地 launcher；`start` 默认启动 Desktop Runtime + Managed Agent，源码开发需显式使用 `--source`。
- `init --host codex|opencode|claude|cursor|windsurf|vscode|all` 可生成 Skill/MCP 配置，`--dry-run` 可先预览写入；Skill 文档仅使用当前 Workflow 命令面。
- GitHub `Build Desktop` 工作流在 Windows tag 构建时生成并上传 Toolkit archive + manifest；根包、CLI、Cargo、Tauri 与 `VERSION` 已统一为 `0.3.0`，tag 构建前会执行版本一致性检查。
- 待用户验证：发布 `flovart-cli@0.3.0` 与 `v0.3.0` GitHub Release 后，在全新 Windows 用户上下文执行 `npx flovart-cli install && npx flovart-cli start`；确认 PATH launcher、Runtime、Managed Agent 与 Host 配置均可用。
- 本轮新增自动验证覆盖：定向 Vitest 证明 CLI 节点命令确实发往 Workspace Adapter、幂等键透传、只向拥有最新项目快照的浏览器派发、断线不回退 shadow state、Managed Agent MCP 不暴露 legacy 工具，并覆盖 WSL 引导与 PowerShell JSON 参数解析；Rust Runtime Contract 5/5 通过，Web 生产构建与 npm 发布包 dry-run 通过，发布清单已实际包含 `workspace-client.js` / `workspace-command-surface.js`。
- 真实浏览器与 CLI 同步已经完成：Playwright 连接本机 Agent 后，Workspace 达到 `ready`，CLI 创建 4 个可见节点与 3 条连线，并真实执行脚本节点更新、移动、选择与视口修改；`workflow.inspect` 与画布截图一致，浏览器 console error 为 0。测试同时发现并修复了 CLI 把 `metadata` / `patch` / JSON `ids` 字符串直接送进浏览器的问题，PowerShell、zsh 和子进程边界现在统一恢复为 schema 类型。证据保存在 `tools/flovart/evaluations/reddit-politics-2026-07-25/workflow-sync-verification.json` 与 `workflow-cli-sync.png`。

## 30 天选题研究、低价模型短片与 TUI V1

- 新增可执行 `research.topic.collect` Research Adapter：支持 Reddit/X 来源结构、时间窗、subreddit、X handle、数量限制、输出目录和稳定幂等键；Reddit 使用 Atom RSS 与系统代理，X 缺少认证适配器时显式返回 `degraded`，不以搜索摘要伪造覆盖。ready/degraded 制品可重放，失败结果修复网络后可用同一 key 重试。
- `flovart tui` 已封装为 Ink Terminal Command Center：每 2.5 秒经 canonical CLI 观察 Runtime、Workflow、持久 Task 和 Event，提供 `/runtime`、`/workspace`、`/tasks`、`/research`、`/models`、`/run`、`/start` 等命令；`--snapshot` 支持无 TTY 回归。TUI 仍不是新的状态权威。
- 真实政治热点测试从 Reddit 获得 25 条近 30 天条目，并选择“南卡参议院空缺如何触发临时任命与选举”的中性制度解释角度；事实由 AP 与 South Carolina Public Radio 复核，官方死因仍待检验，不使用阴谋推断或候选人劝服。
- Production Runtime 真实完成 5 张 GPT Image 2 图片与 5 段 Grok Imagine Video 1.5 低价视频；10/10 成功，价格预览合计 ¥1.70（不是最终账单）。旧 5 秒 Grok 参数在价格预检以业务错误 1007 被阻止，没有 Provider task ID；按官方路线修正为 6 秒后成功。
- 本地后期交付 `reddit-politics-one-empty-chair.mp4`：60.000 秒、1280×720、24 fps、H.264/AAC、中文旁白和硬字幕，SHA-256 为 `9f442189bb607a1dab42a8035417ea02cafddf934ebaf822d0cddd21cd1771c8`。模型视频自带的 AAC 被全部丢弃，成片只使用统一旁白。
- 本轮自动验证：单线程全量 Vitest 为 75 个文件、624 项通过、1 项跳过，仅 `tests/workflowGeneration.test.ts` 保留 6 项既有失败；研究、Runtime/Registry、Provider Route、Workspace CLI 同步与 TUI 定向 186 项通过；Rust Runtime 17 项通过；`npx tsc --noEmit`、Web 构建、CLI 发布包 dry-run 与 Skill 结构校验通过。
- 待用户验证：① 播放成片并确认节奏、字幕和纸雕风格；② 到 RunningHub 控制台核对 10 个 Provider task 的最终扣费；③ 运行 `npm run flovart:cli -- tui` 检查交互 TUI；④ 提供正式 X 数据适配凭据后再验证真实 X 覆盖。完整评估见 `tools/flovart/evaluations/reddit-politics-2026-07-25/evaluation-report.md`。

## PromptBar、Workflow 媒体与 Provider 最终回归

- PromptBar 挂载时不再因初始文本回调误写一条 History；直接点击图片/视频媒体可挂载 PromptBar 与 ElementToolbar，`@` 选择器会插入结构化 mention。
- Workflow 裁剪新媒体在项目状态落地前会先注册临时引用，避免被 prune 竞态删除；Undo/Redo 保存真实当前帧，文件拖放与 JSON 导入对 jsdom/旧浏览器增加 FileReader 降级。
- 生成历史现在正确映射为 Workflow 可插入媒体数组；反推 Prompt 会选择实际文本模型与 Key，不再把尺寸参数错传为模型参数。
- RunningHub 企业代理会正确合并已含 `/openapi/v2` 的 Base URL 与 endpoint，避免重复路径；Route Catalog / Adapter 仍是 Provider payload 的唯一事实源。
- 自动验证已通过：`npx tsc --noEmit`；Vitest 63 个文件，546 通过、1 跳过；Hub 与 Enterprise 两个 Go 模块 `go test ./...`；Vite + Tauri release `--no-bundle` 构建；以及不消耗付费 API 的 Playwright 真实浏览器回归。
- 待用户验证：使用自己的 RunningHub 余额各执行一次图像/视频真实任务，对照账单和网络 payload；本轮自动化没有发起付费生成。

## dev 构建链路修复

- 删除近 265 个未跟踪的 `.js` 副本（源码 `.tsx/.ts` 的旧编译产物），这些副本因 Vite 默认 `resolve.extensions` 把 `.js` 排在 `.tsx/.ts` 之前而遮蔽源文件，导致 dev/vitest 偶尔加载陈旧实现。同时改 `vite.config.ts` 显式声明 `resolve.extensions = ['.tsx', '.ts', '.jsx', '.js', '.json', '.mjs']`，把 tsx/ts 提到最前。dev 服务器与测试现在强制加载源码，无法再被未跟踪副本遮蔽。

## Landing Page v2 调整（应用内首页重构）

- `/`（ToCLanding）回到无 UGC 社区生态的旧版（HEAD = `4587b56/7498d65` 那版手绘风）；3 个 "开始创作" 按钮跳转 `/app/home`；模板卡片仍跳 `/app?templateId=xxx`。
- 新增路由 `/app/home`，对应新组件 `components/home/FlovartHome.tsx`：参考 flova.tv/zh-CN/skill/ 生态——左侧 Sidebar 导航 + 右侧 Hero + 大对话框 + 热门 Skills 横滚 + 最近项目横滚（读 `useWorkflowStore.projects`，首张为虚线"创建新项目"卡，点击进入 `/app`）+ FlovaTV dense grid（分类 tabs + 视频卡 hover 显 Skill 胶囊与"添加为我的 Skill/已添加" toggle）+ Footer。所有 Skill / FlovaTV 视频均前端 mock。
- 删除 4 个未跟踪 WIP 文件：`BannerCoverflow.tsx`、`CommunityFeed.tsx`、`PersonalProjects.tsx`、`useCloneWorkflow.ts`。`communityTypes.ts` 保留（仍被 `community/communityStore.ts` / `community/WorkflowUploadModal.tsx` 引用）。

## Canvas 与 Workflow 双系统

- Canvas 已移除节点引用连线可视化，保留原有 `@` 引用数据和生成传参，并继续使用元素历史与 SVG/Konva 渲染路径。
- Canvas 与 Workflow 复用同一套顶部菜单状态与命令，一键切换工作区，并分别保存当前画布或当前 Workflow 项目。
- 顶部菜单统一承载 Flovart 标识、项目标题、API 能力状态、语言、主题和设置；Canvas 原底部状态与重复菜单已移除。
- Canvas 工具栏移动到底部并支持紧凑换行；裁剪确认、取消和形状/画笔弹层保持可点击，不再被旧底部菜单或滚动容器遮挡。
- Canvas 与 Workflow 左右抽屉按各自工作区定位和缩放，不再使用叠加的固定顶部偏移。
- Canvas 关闭左侧抽屉后，双击聚焦和适配视图不再错误预留侧栏宽度；开始拖拽或滚轮操作会立即停止尚未结束的视口动画。
- Canvas 元素拖拽在松手前复制最终偏移再提交，避免异步状态更新读取到已清空的偏移而回跳原位。
- Workflow 支持独立多项目、图片/文本/视频/音频/配置节点、拖拽缩放、框选、多选对齐与层级、复制粘贴、撤销重做、连线、缩略图和背景切换。
- 双击空白处可新建节点；从节点连线拖到空白处可选择节点类型并一次完成创建、连接和选中。
- 图片、视频和音频可直接拖入画布、从工具栏导入、粘贴或替换；拖入支持浏览器空 MIME 文件的扩展名识别，媒体使用本地持久化并在缺失时显示可恢复状态。
- 点击图片内容或视频播放控件会先选中节点并显示 Flovart 自己的 PromptBar 和 ElementToolbar；PromptBar 支持 `@` 引用、模型、批量、状态、停止和重试。
- Workflow 图片/视频 PromptBar 改为 880px 自适应宽度与单行底栏：固定产品模型采用“能力筛选 + 左侧模型家族 + 右侧具体版本”的两级渐进式选择，未完成 BYOK 映射的模型会引导到设置；参数按当前产品模型 capability 渐进披露（图片画质/尺寸/比例，视频模式/清晰度/比例/时长/音频）。
- 模型、参数、高级和更多面板改为挂载到 `document.body` 的浮层：持续监听 PromptBar 位置、画布滚动、窗口缩放和面板尺寸，自动选择向上或向下展开并限制在视口内滚动；入场使用 Framer Motion spring。待用户重点验证靠近窗口顶部/底部的图片与视频节点、深浅主题和窄窗口。
- PromptBar 参考行使用三态展示：0 张只显示左侧 `+`，1 张显示单缩略图和删除按钮，2 张以上叠放并在参考行悬停时展开；`+` 打开与资产管理共用数据的大型选择弹窗，可选择已存在的画布图片/视频、个人资产或本地图片。本地图片与个人资产会先创建为真实画布节点，再自动连线到当前节点。
- Workflow 会从 PromptBar 正文解析 `@图片1`、`@图片2` 等已连接节点标题并保持文本顺序；生成前再次在 Provider 边界解析，只有“已连线且被 `@`”的媒体会作为参考输入。GPT Image multipart 请求会按官方上限发送最多 16 张参考图。
- 左侧资产页改为紧凑的“个人 / Agent”布局，个人资产复用 PromptBar `@` 与参考弹窗的同一份本地 `assetLibrary` 数据，支持搜索、标签筛选、文件夹和素材操作；Agent 数据源尚未接入时显示明确空状态。
- Workflow 可导入/导出包含本地媒体的严格 JSON，导入会重建媒体存储；删除项目需要二次确认。
- 共享素材支持搜索、图片/视频筛选和预览，并提供内置 Prompt Library。
- Workflow 右侧面板默认展开并记住开关和宽度，复用 Canvas 的紧凑面板外壳；嵌入式 Agent 的模式栏和导航栏改为透明背景无边框，与抽屉 Tab 栏视觉统一，不再出现双层 header 叠摞。
- Workflow 节点已移除顶部标题栏（图片/视频等标签边框），节点为纯媒体/纯内容；错误提示移至右上角小角标，拖动改为整节点可拖。
- 媒体节点（image/video/audio）顶部 22px 区域为拖动条（`data-workflow-drag-handle`），可从此处拖动节点；视频原生控件在拖动条下方仍可正常使用。
- Workflow 配置节点的比例和时长设置从普通行改为胶囊可展开标签（`.workflow-config__chip`），提到模型选择正下方独立横排展示；标签收起时显示当前值 + 下拉箭头，点击展开原生下拉。选项仍来自模型 capability 数组，模型未声明时标签不出现。
- Workflow 底部工具栏已移除上传图片/视频/音频按钮，改用 Canvas 的 `isl-icon-btn` 扁平风格，保留选择/平移/文本/配置/共享素材/撤销/重做/适应/网格/Agent 入口。
- Workflow 的生成历史与素材库支持搜索、图片/视频和素材分类筛选、点击添加、拖到画布落点、下载、反推 Prompt、重命名和删除；点击或拖入的本地媒体继续使用现有 Blob/IndexedDB 持久化，不把 base64 写进项目 JSON。
- Workflow 右侧历史/素材、画布拖入和反推 Prompt 会先解析 `idb:board:*`、`idb-video:*`、`cold-media:*` 等本地媒体引用，不再把这些 IndexedDB 协议直接交给浏览器图片/视频标签。
- Canvas 与 Workflow 支持跨模块复制粘贴图片/视频：Ctrl+C 复制选中媒体元素到应用内共享剪贴板（`useClipboardStore`），图片同步写入系统剪贴板 `navigator.clipboard`；Ctrl+V 按当前鼠标位置粘贴，鼠标不在视口内时回退到视口中心。Workflow 工具栏复制按钮和右键菜单复制也同步写入共享剪贴板。粘贴优先级：Workflow 为 `clipboardRef` > `useClipboardStore` > `navigator.clipboard.read`；Canvas 为 `useClipboardStore` > `navigator.clipboard.read`。

## Workflow 生成

- 配置节点复用 Canvas 当前 API Key、模型选择和 Provider 能力元数据。
- 上游文本和 `@` 文档会合并到提示词，上游图片、视频和音频只在当前模型支持时作为参考输入。
- 文本、图片和视频生成支持请求身份、停止、过期结果隔离、错误、重试、批量和进度状态。
- 单次生成（count=1）时发起节点原地替换为对应媒体类型（config→image/video，text 节点保留 type=text 并写入 content），不新建节点不连线；批量生成（count>1）仍创建新结果节点并自动连线。生成中状态直接在发起节点显示 loading。图片和视频结果写入现有生成历史。
- 图片节点提供裁剪、滤镜、高清放大、移除背景、扩图、蒙版修改和图层拆分；并发工具使用单事务，撤销历史不会串到其他节点。
- 音频媒体节点可上传、播放、引用和编辑音色/格式/语速等配置；音频生成仍明确标记为 Provider 未支持。

## Workflow 节点动效

- Workflow 节点入场/退场使用 Framer Motion spring 物理（scale 0.85→1，stiffness 380 damping 24），悬停时轻微放大 1.02；生成中状态启用呼吸动效（scale `[1, 1.015, 1]` 1.8s 循环）、脉冲遮罩和扫光条（`.workflow-node__shimmer`，Framer Motion `animate.x` -120%→120% 1.6s 循环）。
- 生成中节点禁用 `whileHover` 避免与呼吸动效冲突；选中态边框/阴影用 `cubic-bezier(.34,1.56,.64,1)` overshoot 过渡。
- Workflow 连线使用 `motion.path` + `pathLength` spring 入场（stiffness 120 damping 18）；选中连线添加 SVG 原生 `<animateMotion>` 数据流粒子和 CSS `@keyframes workflow-connection-flow` dash 流动。
- AGENTS.md 已更新：Canvas 保留生成中动画禁令，Workflow 放开限制允许弹性呼吸/脉冲/扫光等动效，动效统一使用 Framer Motion spring 物理，不手写 CSS keyframes 模拟弹性。

## 生成状态与批量导出

- Canvas 生成中节点复用静态毛玻璃占位：蓝灰至紫色半透明渐变、轻磨砂噪点、青绿色细描边和左上角进度标签；Canvas 保留横向扫光动画禁令。
- Canvas 节点生成新增独立请求控制器、停止按钮、图片三分钟超时、视频十一分钟超时、旧请求结果隔离和可见错误信息；图片请求的取消信号会继续传入当前 Provider 与 Gemini SDK。
- Canvas 与 Workflow 可框选多个节点后批量导出媒体 ZIP；文件按画布纵坐标、横坐标排序，保留可读中文、清理非法文件名字符。

## Workflow 批量结果堆叠与导出

- 批量生成（count > 1）的结果节点自动分配同一 `batchId` 和 `batchIndex`；折叠态下同批次节点全部隐藏，显示堆叠卡片（两层背景偏移 + 主图预览 + 明确的 `4张 / 16张` 展开按钮）。只有点击数量按钮才展开，悬停和拖动卡片本体不会触发展开。
- 4 张及以内展开为独立结果节点，每张都可显式“设为主图”；超过 4 张展开为主预览 + 底部缩略图选择器，可切换当前预览并将其设为主图。节点标题默认透明，只有悬停或编辑时显示半透明玻璃底。
- 堆叠卡片使用 Framer Motion spring 入场/退场和轻量 hover 反馈；展开时各节点或结果画廊通过 `AnimatePresence` spring 入场。
- 折叠态下连向隐藏节点的连线同步隐藏；展开后恢复。
- 批量导出 ZIP 文件名格式改为 `项目名_节点标题_序号.ext`（通过 `exportMediaArchive` 新增 `projectName` 参数）；未传 `projectName` 时回退原 `序号_名称.ext` 格式，向后兼容。

## Provider 与模型映射

- Workflow 图片/视频生成严格以节点明确选择的平台产品模型为入口，再按当前生成模式解析统一映射；未知裸上游模型会在提交前被拒绝，不能绕过固定产品目录。
- 产品模型路由会同时校验 Key 状态、能力范围和当前模型清单；失效、能力不匹配或已从 Key 模型清单移除的线路不会参与提交。线路顺序相同时优先默认 Key，再按 Key ID 保持稳定选择。
- Workflow PromptBar 在所选产品模型没有可用映射时禁用生成（包括按钮和键盘提交）；AI 网关增加最终防线，任何未解析的 `flovart:*` 产品 ID 都不会被发送给供应商。
- Google BYOK 运行时改为分别保存文本、图片、视频 Base URL，并在 Workflow 图片/视频调用时显式传递当前映射 Key 的 Base URL 与上游模型，避免不同能力使用不同代理时串线。
- 浏览器扩展同步 API Key 时会合并同一 Key 的模型映射、价格规则和预算策略，不再因为 Key 已存在而丢弃新增路由字段。
- 待用户验证：① 在设置页为 `GPT Image 2` 或视频产品模型添加一条映射后，从 Workflow 节点生成，供应商实际收到映射后的上游 ID；② 删除映射后 PromptBar 显示未就绪且生成按钮不可点击；③ 同一目标配置主备两条线路，主线路异常时必须确认备用线路；④ Google 图片与视频分别配置不同代理 Base URL 时，两类请求各走自己的地址；⑤ 在旧 Workflow 项目中写入未知裸媒体模型时显示“仅支持平台预设产品模型”，且没有供应商请求。

- RunningHub 已作为原生标准模型 Provider 接入设置页、模型拉取、图片生成和视频生成网关；模型列表内置当前项目使用的官方标准模型端点（全能图片、Seedance 2.0、V3.1 视频等），page-api 拉取的模型作为补充，仍可手动补充模型 ID。
- RunningHub API Key 验证会使用标准模型任务查询端点确认鉴权，并在保存或后台刷新时写入可选模型；模型生成返回的 `results[].url` 会转换成现有图片/视频结果格式。
- RunningHub 设置页入口文案已改为"3 个官方模型包"，并说明"获取模型"只是补充拉取官方列表；不再提示必须先拉取旧预设。
- RunningHub WebApp API（`/task/openapi/*`）已移除浏览器禁用的 `Host` 请求头，文件上传、AI App 提交和输出查询在浏览器环境可正常调用。
- RunningHub 标准模型调用会按详情页字段自动映射 `imageUrls`、`firstFrameUrl`、`lastFrameUrl`、`ratio`、`videoUrls` 和 `audioUrls`，并为内置模型包补齐 `resolution`、`duration`、`generateAudio`、`returnLastFrame`、`seed` 等默认载荷；本地 data URL 参考媒体会先上传为临时公网 URL。
- Workflow 生成节点的媒体参考输入收窄为"连到当前节点的上游节点 + Prompt `@` 引用"同时满足；未连线的节点不会出现在 `@` 候选里，连线但未 `@` 的媒体也不会作为参考传给 Provider。Seedance 显式参考槽位同样只允许选择已连到当前节点的上游媒体；当前媒体节点自身和历史遗留的隐藏 `referenceNodeIds` 不再自动传参，避免 RunningHub 视频节点暗中多出参考图。上游文本节点仍按 Workflow 规则自动合并到提示词。
- RunningHub API Key 验证已改为解析 HTTP 200 内的业务认证错误码，避免 `APIKEY_USER_NOT_FOUND` 被误判为验证成功；标准模型上传也兼容官方 `code: 200` + `success` 响应，不再把成功上传误报为失败。
- RunningHub Seedance 2.0 已对齐官方 `rhart-video/sparkvideo-2.0` 命名空间：内置文生、图生、多模态及 Fast 三种端点；旧 `seedance-2.0-global*`、`bytedance/seedance-2.0-global*` 和 `seedance2.0/图生视频` 等展示名会在提交前归一到官方 endpoint。图生视频发送 `firstFrameUrl` / `lastFrameUrl`，多模态发送 `imageUrls` / `videoUrls` / `audioUrls`；RunningHub Seedance 默认载荷改为 `generateAudio: true`，图生/多模态补齐 `realPersonMode: true` 与 `conversionSlots: ['all']`。
- RunningHub 视频 polling 挂死修复：`rhSubmitTask` / `rhQueryTask` / `rhCancelTask` 改用新增 `fetchWithTimeout`（submit 60s / query 30s / cancel 15s），组合 user signal + timeout AbortController；旧代码 fetch 无 timeout，RunningHub 服务器某次查询不响应时 fetch 永远挂起导致 polling 循环卡死。polling 循环 catch 重构：user abort → 调 `rhCancelTask` 后 throw；query timeout（TimerError）→ console.warn + continue（短暂网络抖动不让整次生成失败）；其他错误 → throw。
- RunningHub 停止按钮真正取消服务器任务：新增 `rhCancelTask`（`POST {host}/task/openapi/cancel`，body `{apiKey, taskId}`，best-effort，swallow 所有错误），`handleStopNodePromptGeneration` abort 时调用该端点；旧代码 abort 只停客户端 polling，RunningHub 服务器任务继续运行继续扣费。
- RunningHub Seedance `duration: "-1"` bug 修复：`SEEDANCE_DURATIONS` 首项 `-1` 是 truthy，`payload.duration = String(durationSec || payload.duration || 5)` 在用户选 `-1` 时传 `"−1"` 给 RunningHub 导致参数错误；改为 `(durationSec && durationSec > 0) ? durationSec : (payload.duration || 5)`。
- RunningHub 新增全能视频 V3.1 低价渠道：Fast/Pro 的文生视频、图生视频、首尾帧生视频共 6 个官方 endpoint，并保留已接入的官方稳定版条目。低价展示名（如 `google/veo3.1-fast/start-end-to-video-channel-low-price`）和 RunningHub API 文档页 URL（如 `runninghub-api-doc-cn/api-448183085`）会归一到 `rhart-video-v3.1-*`。字段映射：Fast 低价图生视频用 `imageUrls`，Pro 低价图生视频用 `imageUrl`，低价首尾帧用 `firstFrameUrl` / `lastFrameUrl`；官方 Fast 图生视频用 `imageUrl` / `lastImageUrl`。低价 V3.1 发送 `aspectRatio`（仅 `'16:9'` / `'9:16'`，其他值降级为 `'16:9'`），duration 固定为 `'8'`，不发送文档中没有的 `generateAudio`。
- RunningHub 新增 SkyReels V4 Omni 模型（天工 AI 统一多模态视频，`@tag` 引用机制）：`skyreels-v4/omni-reference-fast`。当前仅支持文生视频模式（`prompt` + `aspectRatio` + `duration`(数字 3-15) + `resolution` + `promptOptimizer`）；参考输入（`refImages`/`refVideos` 结构需 `tag`/`type`/`image_urls`）暂未支持，连参考图时抛清晰错误提示用户去掉参考图。
- RunningHub 新增全能视频S 低价渠道版：`rhart-video-s/text-to-video`（文生视频，¥0.9/次）。duration 为 `'10'` / `'15'` 字符串枚举（snap 逻辑：≥15 → `'15'`，否则 `'10'`）；`aspectRatio` 只支持 `'9:16'` / `'16:9'`，其他值（如 `'1:1'`）会自动降级为 `'16:9'`；含可选 `storyboard` 布尔字段（默认 `false`，当前不暴露到 UI）。
- RunningHub 新增 Veo 3.1 官方稳定版（lite-official）三端点：`rhart-video-v3.1-lite-official/text-to-video`、`rhart-video-v3.1-lite-official/image-to-video`、`rhart-video-v3.1-lite-official/start-end-to-video`。Lite 文生/图生沿用官方字段（duration 4/6/8 snap、generateAudio 兼容），Lite 首尾帧按 schema `api-448184431` 删除 `duration` 和 `generateAudio` 字段（只保留 `aspectRatio` / `resolution`）。
- RunningHub 新增 Veo 3.1 fast-official 端点：`rhart-video-v3.1-fast-official/reference-to-video`，发送 `imageUrls` 数组（最多 3）+ `aspectRatio`（`'16:9'` / `'9:16'`，其他降级 `'16:9'`）+ `resolution`（`'720p'` / `'1080p'`）+ `generateAudio`，无 `duration`。Pro-official reference 字段映射修正：删除 `aspectRatio`（schema 无此字段），保留 `resolution` + `generateAudio:false` 等官方字段。
- RunningHub 标准模型新增 schema 数量兜底：V3.1 `imageUrls` 最多 3；Seedance 2.0 多模态最多 9 张图片、3 个视频、3 个音频，超过时提交前裁剪，避免平台返回 `1007 maximum number of inputs`。
- RunningHub 新增悠船文生图 v8.1：`youchuan/text-to-image-v81`，发送必填 `hd:false`，参考图（垫图）映射到 `imageUrl`（单数）而非 `imageUrls` 数组；包含 `aspectRatio`(`'1:1'`/`'4:3'`/`'3:2'`/`'16:9'`/`'3:4'`/`'2:3'`/`'9:16'`) 字段。
- RunningHub 新增全能图片 G-2.0 文生图官方端点：`rhart-image-g-2/text-to-image`，发送 `prompt` + `aspectRatio` + `resolution`(`'1k'`/`'2k'`/`'4k'`)。
- RunningHub 产品展示 slug（如 `nano-banana-pro/edit-channel-low-price`）会在提交前重写为真实 API endpoint（如 `rhart-image-n-pro/edit`），避免上传成功后打到错误路径。
- RunningHub 标准模型提交、查询和上传会透出平台返回的 `errorCode` / `errorMessage`，并附带不含密钥的 `baseUrl`、最终提交 URL、模型 ID 与 payload 字段摘要；上传未返回公网 URL、模型 ID 仍是未知详情页/搜索页 URL，或 payload 中仍有本地引用时会在本地中断，不再退化成 `No taskId returned`。
- Seedance 2.0 后端路由修正：`doubao-seedance-2.0` 不再被误判为文本/图片模型；Seedance 视频/音频参考在本地 `data:` / `blob:` 时会在调用 Provider 前明确失败，视频任务停止信号贯穿提交、轮询和下载。
- 模型映射由 API Key Vault 本机保存；Workflow 与 Table 共用同一套目标映射，不再维护全局模型偏好状态。
- 根级 `CONTEXT.md` 已补充 Provider、Route Mapping Target、Route Mapping、Standard Model 和 AI App 的领域词边界，并新增 ADR 记录单一映射权威与 RunningHub 原生 Provider 的取舍。

## CLI、Agent 与 MCP

- 网站 Agent 使用当前项目的文本模型/API Key，图片附件先走现有视觉能力，结构化命令通过统一 Workflow dispatcher 执行，写操作逐项确认。
- 本机 Agent 提供网站/本机模式、连接、对话、历史、日志、附件、流式消息、工具卡片、确认、线程新建/恢复/读取/归档和断线重连。
- CLI 新增 `workflow.project.*`、`workflow.node.*`、创建并连接、运行/停止、连接、选择和视口命令及 `flovart_workflow_*` 别名。
- `npm run flovart:agent` 启动仅监听本机的 token/SSE Agent，首次认证时绑定网页 Origin。
- MCP 工具从同一 CLI registry 自动生成；Agent/MCP 写操作需要 Workflow 面板确认后才执行，幂等缓存、超时、断线清理和敏感字段脱敏已接入。
- CLI/Agent 默认视频模型改为 `doubao-seedance-2.0`，`models.list` 暴露 Seedance 2.0 的 9 图、3 视频、3 音频、4-15 秒与分辨率能力；`provider.status` / `provider.test` 新增不含密钥的 readiness 和下一步 setup/model action。
- `flovart:cli -- doctor --json` 新增一键 Seedance 2.0 自检：读取本机 shadow runtime 状态，输出 Volcengine credential、Seedance 模型、参考素材限制、Canvas/Workflow 生成面是否就绪及下一步动作，不输出密钥。
- Codex app-server 按 Workflow 项目隔离线程工作区，图片附件作为临时 `localImage` 输入并在 turn 结束后删除。

## Overlay 迁移与画布主题

- 旧 SVG/Konva 画布中嵌入在 `<foreignObject transform="scale(1/zoom)">` 内的所有 overlay UI 已全部迁出为 `CanvasFixedOverlay`（`createPortal` 到 `document.body`，z-index 50）+ `placeOverlay` 纯函数定位，不再随 zoom 缩放变换：
    - image / video 节点 PromptBar、视频生成中 stageLabel、多选/单选/滤镜面板 ElementToolbar、inpaint 蒙版 prompt、文本编辑 textarea 均已迁移。
    - 内容本体类 foreignObject（text 显示态、HTML cover/typesetting、video 内容控件本体）按决策保留在 `<g>` 变换组内，避免破坏 selection/分组逻辑与性能。
- 新增 `utils/canvasOverlayViewport.ts`：`placeOverlay()` 支持四向智能翻转（上/下/左/右）+ 边界避让 clamping，锚点类型含 `above-center` / `below-center` / `center`（centroid 锚点）；`canvasToScreen()` 用于 textarea 等无需 flip/clamp 的直角映射。
- 新增 `components/canvas/CanvasFixedOverlay.tsx`：portal 容器 + `useElementContainerRect` hook；新增 `styles/index.css` 的 `.canvas-overlay-fixed` 基类（`data-no-transition` 开关瞬贴），同时修复该文件字体拼写 `BlinkMacMacSystemFont` → `BlinkMacSystemFont`。
- `ElementToolbar.tsx` 已重新整合 AI 动作（Split/Upscale/RemoveBG/Outpaint/Mask/ReversePrompt 及对应 state props）回单一工具栏；原独立的 `ElementAIPopBar.tsx` 因无任何 import 引用，已作为死代码删除。
- Canvas 视频节点未选中时叠加透明 `<rect>` 覆盖层捕获点击以触发选中；选中后覆盖层移除，放行到 `<video controls>` 原生播放控件，修复视频无法选中导致 ElementToolbar 不出现的交互问题。
- `Toolbar.tsx` 重组为左/中/右三簇布局并用 `ClusterDivider` 分隔：左 Boards&Layers + Upload + Assets；中 Select/Pan/Shapes/Drawing/Text + 颜色 + 粗细；右 WheelAction + Settings + Undo/Redo；水平 `justify-between`，垂直时线性排列。

## Excalidraw 阶段 A 托管渲染

- ~~通过 `?excalidraw=1` dev 路径渲染 `FlovartExcalidrawStage`~~（Phase 1.1 已移除 dev gate，Excalidraw 现为主路径，见下方 Phase 1.1 节）。
- Flovart Element 仍是单一数据源，Excalidraw 仅作视图+输入层、不保留自有状态；`onChange → api.updateScene` 回环用 `isSyncingRef` 守卫避免重入。
- 新增 `components/canvas/ExcalidrawInner.tsx`：Excalidraw 用 `memo(() => true)` 包裹，防止 React 19 + StrictMode 下内部 Zustand store 无限循环；图片通过 `api.addFiles([{ id, dataURL, mimeType }])` 注册。
- 新增 `utils/excalidrawAdapter.ts`（Flovart ↔ Excalidraw 元素双向转换层，645 行）、`components/canvas/FlovartToolbar.tsx`（自定义工具栏）、`components/canvas/ExcalidrawLayerPanel.tsx`（图层面板）、`components/canvas/excalidraw-canvas.css`（隐藏 Excalidraw 原生 UI + isl-\* 主题映射）。旧 Canvas overlay 服务路径为后续 Phase 1.5 在 Excalidraw 挂同套 overlay 预留。

## 编辑器与画布交互修复

- `RichPromptEditor.tsx`：Tiptap Suggestion 设为 `allowedPrefixes: null`，中文用户在 `@` 前不输入空格也可触发引用；tippy 改为 `strategy: 'fixed'`、z-index 99999，避免被 SVG 容器裁剪。
- `PromptBar.tsx` 模型下拉新增 `overflow-y-auto` + 滚轮 `stopPropagation`，长列表不再撑出面板、滚动不再误触画布。
- Workflow 侧 `WorkflowSidebar.tsx` 图层拖拽补 `dataTransfer.setData`，修复拖入接收。
- `aiGateway.ts` 新增 `blobToDataUrl()`，RunningHub 上传前把 `blob:` URL 转 data:，避免浏览器拒绝 fetch blob 跨域。
- `handleNodePromptGenerate` 中通过 `resolveColdMediaRef` 解析 `idb:board:*`、`idb-video:*`、`cold-media:*` 等本地媒体引用，不再把 IndexedDB 协议 URL 直接传给 Provider。
- Canvas `wheelAction` 默认 `'pan'`，Workflow 侧新增 `wheelMode`，滚轮默认平移，按住修饰键才 invertzoom。
- 新增 `hooks/useIdbImageResolution.ts`：SVG 渲染路径中 `idb:board:*` 协议的图片 href 和 mask 通过异步 IDB 查询解析为 data URL，不再直接传给 `<image href>` 导致 `ERR_UNKNOWN_URL_SCHEME`；`resolveHref` 在 `<image>`、mask canvas、SVG 导出字符串三处统一使用。

## App.tsx 重构阶段 1

- IDB 持久化逻辑提取到 `utils/boardPersistence.ts`，运行时类型提取到 `types/runtime.ts`。
- 移除 `App.tsx` 重复的 `BOARDS_STORAGE_KEY` / `ACTIVE_BOARD_STORAGE_KEY` 声明。
- 大量节点提示生成 hook (~20+ 依赖参数) 与画布渲染 JSX (~800 行) 提取评估为净负面可读性，按决策暂不拆分。

## CanvasSettings 模型映射编辑器 + PromptBar 扁平化（本轮）

- CanvasSettings 模型 Tab per-Key 映射编辑器：顶部三选下拉保留，下方按 Key 平铺映射行；可改 upstream/priority/enabled、增删映射；改后自动 `confirmed=true` 并经 `handleUpdateApiKey` 持久化，参与 `resolveProductModelRoute` 路由。
- PromptBar 扁平化：合并「高级」chip 进「更多」popover（顶部分段「高级选项」）；popover 去卡片背景与 `PopoverHeader`，改 `isl-pop` 基础浮层 + 内联段标题；动作 chip 去 chevron 与 `isl-chip` 胶囊，改无边框 `triggerClass` + 轻 hover；`ExpandPanel` 删除 `'advanced'`。
- typecheck 通过（仅 `tests/workflowImageTools.test.tsx(81,94)` 预存语法错误）。
- 待用户验证：① 设置-模型 Tab 给某 Key 添加/启用一条映射后，Workflow 生成路由到该上游模型；② 禁用/删除映射后 PromptBar 生成按钮不可点；③ PromptBar 各 popover 为扁平浮层（无卡片边框/阴影），段标题为内联文本；④ 「更多」popover 顶部含「高级选项」分段（联网搜索/真人素材预检测）；⑤ 动作按钮（润色/翻译/重试/+Key）为无边框文本 + 图标 + 轻 hover。

---

本文件中的测试项尚未由用户执行确认。本次累计变更包括：Workflow 节点 header 删除、底部工具栏重构、右侧抽屉 header 对齐、RunningHub Host 头修复与 LEGACY_PRESET 黑名单解封、@ mention 前缀修复、滚轮默认平移、模型下拉溢出修复、图层拖拽数据传输修复、blob→data URL 转换、cold-media 引用解析、App.tsx 阶段 1 持久化/类型提取、overlay 全量迁移、ElementAIPopBar 气泡新增、ElementToolbar AI 动作剥离、底部工具栏三簇重组、Excalidraw 阶段 A 托管渲染验证、Workflow 节点 Framer Motion 弹性动效（入场/退场/悬停/生成中呼吸/扫光/连线 pathLength/数据流粒子）、Workflow 批量结果堆叠折叠与展开、批量导出自动命名 `项目名_节点标题_序号.ext`、`idb:board:*` 协议 SVG 渲染路径异步解析修复。RunningHub 链路已通过 Node.js 脚本实跑验证（`rhart-image-n-g31-flash/text-to-image` + Bearer + `{ prompt, resolution: '1k' }` → 成功生成图片，花费 ¥0.19）。按项目约定未执行前端测试、构建或语法检查，等待用户实际验证。

## 全量代码 Review 后统一修复（本轮）

- **B1 安全**：`rhGetWebAppNodes` 在保留 `apiCallDemo` GET 端点必需的 query apiKey 之外，额外附加 `Authorization: Bearer` header 作为认证双保险，并添加注释说明端点限制。
- **B4 错误码**：RunningHub WebApp 查询返回的非 0 / 非 804 / 非 813 code（含 802 认证错误等）不再静默落 `UNKNOWN`，统一视为 `FAILED` 并透出 `failedReason`，避免 key 无效时无反馈。
- **C1 视频选中**：Canvas 视频节点（非 Konva 渲染分支）在未选中且非生成中时叠加透明 `<rect pointerEvents="all">` 覆盖层捕获 mousedown，触发 SVG 既有的 `data-id` 选中链路；选中或生成中时移除覆盖层，放行到 `<video controls>` 原生播放控件。
- **死代码清理**：`components/ElementAIPopBar.tsx`、`components/KonvaDemoPage.tsx`、`components/LayerPanel.tsx`、`components/LayerPanelMinimizable.tsx` 经全仓 grep 确认零 import 引用，已删除；旧 Canvas 图层面板实际由 `components/canvas/ExcalidrawLayerPanel.tsx` 承载。
- **临时文件污染**：根目录 14 个 `.App.unified.tmp` / `.InfiniteWorkflow.unified.tmp` / `.workflow-*.unified.tmp` 等 `.tmp` 临时文件已物理清理（仍在 `.gitignore` 中）。
- **B6/B7 注释澄清**：`providerAdapterRegistry.resolve` 在无 explicit `providerId` 时按 Map 插入顺序匹配首个 `supportsModel` 的适配器（ES2015+ Map 顺序规范保证，且每个 modelId 通常只被一个 provider 支持故无歧义）；`seedanceProviderAdapter.generate` 是可选快捷路径，调用方（aiGateway）可走 registry submit+poll 分步流程，两者复用同一 `submitTask`/`pollTask`，无重复实现。已加注释说明。
- **B8 可取消**：`rhRunWebApp` / `rhSubmitWebAppTask` / `rhQueryWebAppOutputs` 新增可选 `signal?: AbortSignal` 参数并透传给 `fetch`；轮询循环改为 abortible sleep（signal abort 时提前结束 setTimeout 等待），取消时抛出 `AbortError`，不再继续空转直到 10 分钟超时。
- **D4 部署按钮**：README 顶部新增 Vercel / Render 一键部署按钮（指向 `vercel.com/new/clone` 与 `render.com/deploy`），新增 `render.yaml` 静态站点配置（`npm ci && npm run build`，`staticPath: /dist`，SPA rewrite），部署形态文案从"四种"更新为"六种"。

## UGC 社区主页（Banner + 个人项目 + 社区瀑布流）

- ToCLanding（`/` 路由）改造完成，从原"Hero + Features + 模板画廊 + Showcase"四区块改为 UGC 三区块：BannerCoverflow + PersonalProjects + CommunityFeed；保留 Header（导航 goto 社区/我的项目/提示词 + 个人版/企业版切换 + 登录 + 开始创作）+ Footer + Bottom CTA（文案改为"上传你的工作流，让社区一起做同款"）。
- **BannerCoverflow**：`embla-carousel-react` coverflow 3D 轮播，6 个精选工作流卡片，中心卡片放大无旋转、两侧卡片缩放 + rotateY 透视；底部圆形 dot 指示器 + 顶部两个入口按钮（"开始创作""浏览社区作品"），中心卡片点击触发"一键做同款"。
- **PersonalProjects**：读取 `useWorkflowStore` 已 hydrate 的本地项目，按 `updatedAt` 取前 6 个 + 一个"新建工作流"虚位卡片；预览图取首个 image/video 节点的 `metadata.href || metadata.poster`（过滤 blob:/file: 本地引用）；卡片点击跳 `/app`。
- **CommunityFeed**：响应式 2/3/4 列 masonry（依据 `window.innerWidth` 切列数），12 个 mock 社区工作流卡片；搜索（title/description/tags/author）+ 7 分类 tab（`COMMUNITY_CATEGORIES = ['全部','TV Show','人物','风景','产品','动漫','抽象']`）；卡卡 hover 显示点赞按钮 + "做同款"按钮；点赞数 local state。
- **数据类型 `CommunityWorkflow`**：`id/title/description/thumbUrls[]/coverVideoUrl/gradient/author{ name,avatar? }/likes/views/forks/category/tags[]/workflowJson: WorkflowProject/createdAt`。
- **一键做同款 `useCloneWorkflow`**：深拷贝 `workflowJson`，重新生成项目 id（nanoid）+ 全节点 id 映射 + 重写连线 id/端点 + 清除生成状态/媒体引用/版本（storageKey/href/poster 若为 blob:/file: 也清空，避免本地引用暴露）；调 `useWorkflowStore.importProjects([normalizeWorkflowProject(cloned)])` + 跳转 `/app`，toast `已克隆到"我的项目"，正在打开工作流`。
- 技术栈：`embla-carousel-react@8.6.0` + `@tanstack/react-virtual@3.14.5`（虚拟滚动 import 已加但 mock 数据量小，暂用响应式列数 + 非虚拟的 column masonry；后续接入真实后端大列表时再启用 `useVirtualizer`）。
- 项目 mock 数据放在 `components/landing/communityTypes.ts`（`COMMUNITY_WORKFLOWS` 12 条 + `BANNER_WORKFLOWS` 前 6 条，覆盖 6 个 category + 12 套渐变色 + 7 个虚拟作者），后续接入真实后端时只需替换数据源。
- typecheck 通过（仅 `tests/workflowImageTools.test.tsx(81,94)` 预存语法错误）。
- 待用户验证：① 进入 `/` 看到 Banner coverflow 轮播并能切换；② 中心卡片点击触发做同款，跳 `/app` 后画布上有克隆出的项目（标题带"（做同款）"后缀）；③ 个人项目网格显示本地已有项目缩略图；④ 社区 feed 搜索/分类筛选/点赞按钮工作；⑤ 响应式列数随窗口宽度从 2 列变 3 列变 4 列。

## UGC 社区上传与发布闭环

- 新增 `components/community/communityStore.ts`：localforage 持久化 UGC 社区工作流上传（`storeName='community_workflows_v1'`，USER_KEY='user-uploads'）。3 个 API：`loadCommunityWorkflows()` 合并内置 mock + 用户上传按 `createdAt` 倒序、`publishCommunityWorkflow(input)` 写入用户上传条目、`toggleCommunityLike(id)` 内置/用户通用点赞。
- 新增 `components/community/WorkflowUploadModal.tsx`：发布工作流模态，选择本地工作流（Select）+ 标题 + 简介 TextArea + 分类 Select + 渐变色预设 Select + 标签管理（回车添加、最多 8、点击删除）+ 发布按钮；克隆 `workflowJson` 时清空 `agentSessions/activeAgentSessionId/selectedNodeIds` 避免隐私泄漏；作者用 `user?.username || '匿名创作者'`。
- `ToCLanding.tsx` 接入发布按钮：Header 新增"发布工作流"按钮（未登录跳 AuthModal）；动态 `items` 状态启动调 `loadCommunityWorkflows`；`bannerSeed` 用于发布成功后 remount Banner + Feed；底部 CTA 文案贴合 UGC 闭环。
- 待用户验证：① 下载未登录点"发布工作流"弹出登录框；② 登录后选本地项目 + 填写信息 + 发布，Banner/Feed 刷新看到新条目；③ 点赞按钮可切换；④ 用户上传条目刷新后仍在（localforage 持久化）。

## Workflow UI 精简与节点连接气泡

- **顶部菜单**：`StudioTopMenu.tsx` 加 LOGO 下拉（项目切换/新建）+ 画布名 inline 编辑 + 画布序号切换器；`App.tsx` 接 `projectList/activeProjectIndex/projectActions`。
- **节点交互**：节点双击改为聚焦动画（仅超出视野才居中），原"放大播放"移到 `WorkflowNodeToolbar.tsx` `Maximize2` 按钮；节点重命名改为右键菜单"重命名"入口 → `renameSignal` nonce → `WorkflowNode` 触发已有 inline 编辑。
- **工具栏**：`WorkflowToolbar.tsx` select/pan 合并为单按钮切换（Framer Motion spring 旋转图标过渡）；3 个添加按钮合并为 1 个大"+"按钮，弹出竖排菜单（image/video/text/script/audio/config），click-away 关闭，AnimatePresence 出场动画；缩放/小地图/磁吸整合进工具栏。
- **创建菜单**：`WorkflowCreateMenu.tsx` 从 2×3 网格卡片改为竖排单列列表项。
- **左栏**：`WorkflowSidebar.tsx` 删除常驻 aside，改为折叠态触发按钮 → 从屏幕左侧滑出的临时浮层（图层/资产双 tab），点外部收起；资产 tab 嵌入 `StudioMediaBrowser(mode='assets')`。右抽屉 `WorkflowWorkspace.tsx` 移除 assets tab，仅保留 agent + history（生成历史固定 `mode="history"`）。
- **节点加号 mouse-follow 吸附**：节点 source/target handle 各内嵌 `Plus` icon（22px，hover/选中才显）；从 handle 拖出连线时浮动"+"气泡（`.workflow-connection-bubble`，Framer Motion spring x/y 追踪 `connectionDrag.local` 世界→屏幕坐标，stiffness 600 damping 32）跟随光标，松手在节点上吸附连接、在空白处打开创建菜单。target handle 触发反向连接（新节点→当前节点）：`connectionDrag.direction='in'`，`WorkflowConnections` active path 反转，`getConnectionDropTarget` 校验反向 `validateWorkflowConnection`，空白处 drop 时为当前节点创建上游节点。
- typecheck 通过（仅 `tests/workflowImageTools.test.tsx(81,94)` 预存语法错误）。
- 待用户验证：① 工具栏单按钮切换 select/pan，图标 spring 旋转；② 点"+"弹出竖排菜单选类型建节点，点外部收起；③ 左栏按钮点开浮层，图层/资产 tab 切换，点外部收起；④ 节点双击聚焦（在视野内不硬居中）；⑤ 右键节点"重命名"进 inline 编辑；⑥ 从 source handle 拖出看到"+"气泡跟随光标，拖到另一节点吸附连线；⑦ 从 target handle 拖出，松手在空白处弹出"创建上游节点"菜单，建好后自动连回当前节点。

## 共享生态后端骨架（待测）

- 在 `backend/` 子目录新建 Go + Gin + GORM 后端 `flovart/hub`，作为 monorepo 一部分独立部署，耗材托管在 Cloudflare R2（S3 兼容）。
- 已分层落地：`config/config.go` 加载环境变量；`model/` 定义 `User` / `PromptPack` / `PromptItem` / `PromptLike` 及列表 `Query` / `PageResult`；`repository/` 仅做 GORM 访问；`service/` 负责鉴权、bcrypt、JWT（HS256，默认 168h）、默认值与校验；`handler/` 只解入参调用 service 返回 `{code,data,msg}`；`middleware/auth.go` JWT 解析中间件。
- 提示词生态 MVP 路由：`/api/v1/auth/{register,login,me}`、`/api/v1/prompts` 列表/详情/`by-slug`/`download`/上传/修改/删除/`like`。
- 同步新增：`backend/go.mod`、`backend/.env.example`、`backend/docker-compose.yml`（PostgreSQL 16 本地起库）、`backend/.gitignore`。
- `/healthz` 健康检查与 `gin.ReleaseMode` 在生产时启用。
- `AutoMigrate` 启动时自动建表；表结构同步落入 `docs/content/docs/backend/backend-database.mdx`，接口规则落入 `docs/content/docs/backend/api-response.mdx`，并已在 `docs/index.md` 添加入口。
- 待用户验证：本地 `docker compose up -d db` 起库后 `go run ./cmd/server` 能正常 AutoMigrate 并通过 `/healthz`；登录/注册/JWT 鉴权在 `Authorization: Bearer <token>` 下生效。

## 企业后台角色用户管理骨架（待测）

> ⚠️ 本节描述的 `organization_members` 表与 owner/admin/member 三角色设计已被 E1 M1-M5 取代（M4 删除 `organization_members` 表，改为部门级角色绑定 + 11 权限点 + 递归 CTE 继承；M5 重写前端为三标签页）。以下保留为历史记录，实际可测变更见下方「E1 M1-M5」节。

- 新建 `backend/enterprise` 独立服务 `flovart/enterprise`，作为 monorepo 一部分独立部署，复用 hub 同一 PostgreSQL 与 `JWT_SECRET`，共享 `users` 表（不重建该表，仅 AutoMigrate 自有 `organizations` / `organization_members` 两张表）。
- 角色固定三枚：`owner` / `admin` / `member`，无独立角色表（遵循 AGENTS.md"只实现当前明确需要的功能"）。
- 权限矩阵：owner 一切；admin 仅管理 `member`，不可动 owner 或其他 admin；member 只读成员列表。
- owner 不可通过成员接口被添加/降级/移除，需走转让流程（后续版本）。
- 成员添加通过 `byUsername`，被加者必须已是 flovart 平台注册用户。
- 路由：`POST /api/v1/enterprise/orgs`、`GET /api/v1/enterprise/orgs`、`GET /api/v1/enterprise/orgs/:id`、`DELETE /api/v1/enterprise/orgs/:id`、`GET /orgs/:id/members`、`POST /orgs/:id/members`、`PUT /orgs/:id/members/:userId`、`DELETE /orgs/:id/members/:userId`、`GET /healthz`。
- 同步新增：`backend/enterprise/go.mod`、`.env.example`、`.gitignore`；数据库结构补充到 `docs/content/docs/backend/backend-database.mdx`；接口规则新落 `docs/content/docs/backend/enterprise-api.mdx` 并已加入 `docs/index.md`。
- 待用户验证：hub 起库注册一个用户后，用同 `JWT_SECRET` 起 enterprise 服务，创建组织、添加成员（同 hub 用户名）、修改/降级角色、移除成员，对照权限矩阵确认拒绝路径与放行路径都正确。

## R2 上传服务 + 前端 API 客户端（待测）

- 在 `backend/storage/r2.go` 封装 Cloudflare R2（S3 兼容）客户端，包含 `PutObject` / `PresignPut`（默认 15 分钟）/ `PresignGet`（默认 1 小时）/ `DeleteObject` / `HeadObject` / `PublicURL`。
- hub 新增 `service/upload_service.go`（`Presign` 生成 `uploads/<purpose>/yyyy/MM/dd/<uuid>.ext` 格式 key 并预签名；`ConfirmUpload` 用 `HeadObject` 验对象真实存在）与 `handler/upload_handler.go`（`/api/v1/uploads/presign` 与 `/api/v1/uploads/confirm`）。
- 启动行为：R2 配置不完整时不返回错误，仅日志提示"upload routes disabled"，对应路由不会注册；hub 在 dev 环境可零存储启动。
- `purpose` 白名单：`prompt_cover` / `workflow_showcase` / `avatar` / `workflow_media` / `misc`，未知值统一回退 `misc`。
- go.mod 引入 `aws-sdk-go-v2` 系列依赖（config / credentials / s3）。
- 前端新增三个 API 客户端文件（沿用 `services/` 目录约定）：
  - `services/hubClient.ts`：`HUB_BASE_URL` / `ENTERPRISE_BASE_URL`（读 `VITE_HUB_BASE_URL` / `VITE_ENTERPRISE_BASE_URL` 环境变量，默认 `localhost:11452/8081`）、`authApi`（register / login / me）、`getToken` / `setToken`（localStorage 持久化）、统一 `request<T>` 自动注入 `Authorization: Bearer` 与 `{code,data,msg}` 解码。
  - `services/promptApi.ts`：列表/详情/`by-slug`/下载/创建/修改/删除/`toggleLike`。
  - `services/uploadApi.ts`：`uploadApi.presign` / `confirm` + 一站式 `uploadFileToR2(file, filename, contentType, purpose)`——拿 presign 后直传 R2 再 confirm。
  - `services/orgApi.ts`：组织 CRUD + 成员 CRUD。
- 待用户验证：① R2 配上 4 个环境变量后 hub 启动日志显示"R2 storage ready"；② 前端用 `uploadFileToR2` 跑完整 presign → PUT → confirm 链路后能通过 `publicUrl` 访问对象；③ 无 R2 配置时 hub 仍能正常启动且 `/api/v1/prompts/*` 可用，`/api/v1/uploads/*` 不会返回 404 也不会 panic。

## 前端企业后台路由 + 登录/组织/成员管理 UI（待测）

> ⚠️ 本节中 `RoleBadge`、角色下拉切换、`orgApi.ts` 旧成员 CRUD 已被 E1 M5 重写：前端改为三标签页（成员名册只读+增删 / 部门左树右面板 / 角色 checkbox 网格），`orgApi.ts` 全量对齐 M4 部门级角色绑定契约。路由容器与登录面板部分仍有效。实际可测变更见下方「E1 M1-M5」节。

- 在 `package.json` 新增依赖 `react-router: ^7.16.0`（v7 已将 `react-router-dom` 合并到 `react-router`；DOM 端 import 直接从 `react-router` 取）。
- 新增 `RouterHost.tsx` 顶层路由容器：`<HashRouter>` 兼容 Tauri / CF Pages 子路径 / GH Pages / Vercel；`/` 渲染现有 `App.tsx`（其内部 canvas/workflow 视图切换保持不变），`/enterprise/*` 渲染 `EnterpriseApp`，其余路径回退到 `/`。
- `index.tsx` 改为渲染 `<RouterHost>`，移除直接渲染 App 的代码；保留 `React.StrictMode` 与 `./styles/index.css` 加载次序。
- 新增 `components/enterprise/EnterpriseApp.tsx` 默认导出（遵循"页面里只有一个主业务组件时直接写在 page.tsx"），未登录显示登录/注册面板（复用 OnboardingWizard 视觉风格，isl-tabbar 切登录/注册、isl-surface 卡片），登录后展示组织列表 + 创建组织表单 + 进入组织详情后展示成员表格 + 添加/修改角色/移除成员。
- 角色显示使用 `RoleBadge`：owner（mint 盾勾）、admin（灰盾）、member（默认）；可管理的当前用户自动出现下拉切换角色与删除按钮，owner 不可被普通动作移除/降级（前端+后端双重）。
- 鉴权流程：`authApi.register`/`login` → `setToken` 存当前会话的 `sessionStorage['flovart.hub.token']`；进入组件时若有 token 调 `authApi.me` 验证，无效则清空 token，回到登录面板。
- `components/studio/StudioTopMenu.tsx` 在右上角加一个 `<Link to="/enterprise">` 按钮（lucide `Building2` 图标），不破坏 `StudioMenuModel` 接口，从主页可一键进后台。
- toast 反馈复用 `hooks/useToast.ts` + `components/Toast.tsx`。
- 待用户验证：① `npm install` 装 `react-router` 后 `npm run dev` 主程序启动正常、`/#/` 主路由仍可访问；② 点 StudioTopMenu 右上 Building2 进入 `/#/enterprise` 显示登录面板；③ 注册/登录成功，token 写入 localStorage；④ 创建组织、添加成员（同 hub 用户名）、修改角色、移除成员，对照权限矩阵确认拒绝/放行都正确；⑤ 在 Tauri dev 与 build 后 hash 路由都正常工作。

## E1 M1-M5 — 企业后台组织/部门/角色权限（待测）

取代上方两个历史节（M4 删除 `organization_members` 表 + org 级三角色；M5 重写前端 UI）。

- **M1 模型**：`backend/enterprise` AutoMigrate 改为 `Organization` + `Department` + `DepartmentMember` + `Role` 四张表。`OrganizationMember` 完全删除。
  - `Department`：邻接表 `parent_id`（nullable，根部门 `_all` 的 `hidden=true` 对前端隐藏）+ `slug/name/sort/hidden`。
  - `DepartmentMember`：`dept_id` + `user_id` + `is_lead` + `roles uuid[]`（部门级角色绑定，非 org 级）。
  - `Role`：`org_id` + `name` + `is_builtin` + `permissions text[]` + `sort`；`permissions` 存 11 个硬编码权限点。
  - `model/permissions.go`：11 权限常量（`org:manage` / `member:invite` / `member:manage` / `dept:manage` / `role:manage` / `credit:grant` / `credit:adjust` / `asset:approve` / `asset:publish` / `workflow:publish` / `view:audit_log`）+ `BuiltinOwnerPerms` / `BuiltinAdminPerms`。
- **M2 RBAC 引擎**：`repository/rbac_repository.go` 用 PostgreSQL `WITH RECURSIVE` CTE 求用户在（本部门 ∪ 祖先部门）上所有角色的 `perms` 并集；`owner` 走 `Organization.OwnerID` 快通道不等 CTE。`service/rbac_service.go` 暴露 `Satisfy` / `IsMember` / `EffectivePerms`；`middleware/rbac.go` 注入 `RequirePerm` 中间件。
- **M3 部门/角色 CRUD**：`dept_handler.go` 8 端点（树查询 `GET /orgs/:id/departments` 返回 `DeptNode[]`、建/改/删部门、部门成员增查改删）；`role_handler.go` 4 端点（列表/建/改/删）+ `GET /orgs/:id/me/permissions` 返回当前用户有效权限 `{ permissions: string[] }`。建组织时自动 seed `Owner` / `Admin` 两个 builtin 角色。
- **M4 org 级成员 API 重构**：`org_service.go` 的 `Create` 改为事务（建 org → seed builtin 角色 → 建根部门 `_all` → owner 加入根部门）；`ListByUser` 改为 `JOIN department_members`；`ListMembers` 改为跨部门汇总按 `user_id` 去重返回 `[]DepartmentMember`（根部门绑定记录，非有效权限集）；`AddMember` = 加入根部门默认空角色；删除 `UpdateMemberRole`。
- **M5 前端管理 UI 重写**：`services/orgApi.ts` 全量重写——删除 `OrgRole/OrgMember/updateMemberRole`，新增 `Department/DeptNode/DepartmentMember/Role/ALL_PERMISSIONS/Permission` 类型 + dept/role/myPerms API 对齐 M4 契约。`components/enterprise/EnterpriseApp.tsx` 全量重写——`EnterpriseApp` root 瘦身（`openOrg` 仅设状态不拉数据）；`OrgDetailPanel` 拥有自己的数据（`useEffect` 4 并行拉取 members/perms/depts/roles）+ `can(p)` 权限助手（来自 `myPerms`）+ targeted refreshers；三标签页：
  - **成员名册**：只读表（username/email/joinedAt）+ add（byUsername）+ remove，无角色下拉（`ListMembers` 返回根部门去重记录，旧 per-member role 已无意义）。
  - **部门**：左 `DeptTree` 递归树（`border-l` 缩进）+ 右面板——选中节点展示该部门成员，add 从组织已有成员里选、toggle `isLead`（Star 图标）、toggle role checkboxes、remove。
  - **角色**：角色卡片列表 + create(name) + 11 权限 checkbox 网格 + builtin 只读锁定 + dirty 检测（`JSON.stringify` 比对）+ save + delete 保护（builtin 不可删）。
  - `AuthPanel` / `FormInput` / `OrgListPanel` 原样保留；`RoleBadge` 已删除。
- 待用户验证：① 启动 enterprise 后端（需 `DATABASE_URL` + `JWT_SECRET`）+ `npm run dev`，进 `/#/enterprise` 登录；② 创建组织后确认自动生成 `_all` 根部门 + Owner/Admin builtin 角色；③ 三标签页分别测试：成员名册 add/remove；部门树建子部门 + 加成员 + toggle isLead + toggle role + remove；角色 create + 改权限 + save + delete 保护；④ `can(p)` 权限助手在前端正确放行/隐藏按钮（用非 owner 账号验证）；⑤ `GET /orgs/:id/me/permissions` 返回的权限集与前端 `can()` 行为一致。

## Phase 1.1 — Excalidraw 上提主路径（Magic Canvas 迁移第一步）

- `vite.config.ts` 的 `optimizeDeps` 新增 `esbuildOptions: { target: 'es2022', treeShaking: true }`，确保 Excalidraw 0.18.1 在 Vite 6.2 下正确预构建。
- `App.tsx` 移除 `?excalidraw=1` dev gate（原 3378-3389 行），`FlovartExcalidrawStage` 不再只在 dev 路径渲染，而是直接作为 AppShell `main` prop 内容在主路径渲染。
- `App.tsx` 删除旧 SVG + CanvasFixedOverlay 混合 Canvas 主体（原 3700-4543 行，843 行），替换为 `<FlovartExcalidrawStage>`。AppShell 外壳（顶栏 / 左侧栏 / 右侧 dock / 工具栏 / 图层面板）保留不变。
- `FlovartExcalidrawStage` 新增 `onSelectionChange` prop，在 `handleSceneChange` 中将 Excalidraw `selectedElementIds` 转发到宿主 `setSelectedElementIds`，图层面板高亮与 Excalidraw 选中同步。
- 删除 `components/canvas/KonvaCanvas.tsx` + `components/canvas/KonvaCanvasDemo.tsx`（零引用死代码）。
- **Overlay 暂下线**：原 SVG 内的 PromptBar / ElementToolbar / inpaint textarea / stageLabel / 属性面板浮层（CanvasFixedOverlay 实例）随 SVG 主体一并移除，Phase 1.5 已补回 ElementToolbar（viewport 通过 `onViewportChange` 暴露 + `containerRef` 附着 Excalidraw 容器）。inpaint prompt / stageLabel 未补回（需重新设计交互或属 Phase 2 范畴）。bottomDock / 侧边栏 / 顶栏 / 图层面板不受影响。
- App.tsx 从 4549 行缩减到 3700 行；旧 SVG 交互变量（`svgRef` / `handleMouseDown` / `zoom` / `panOffset` / `canvasToScreen` / `selectionBox` / `croppingState` / `contextMenu` 等）仍保留在组件中但不再被 JSX 引用，`tsconfig.json` 未开启 `noUnusedLocals` 故不阻塞编译，后续 Phase 清理。
- 待用户验证：① `npm run dev` 后主路径（非 `?excalidraw=1`）直接显示 Excalidraw 画布；② 侧边栏 / 顶栏 / 工具栏 / 图层面板正常显示和操作；③ 在 Excalidraw 中画矩形 / 椭圆 / 文字 / 自由绘制，元素同步到图层面板；④ 选中元素时图层面板对应项高亮；⑤ 切换深色/浅色主题时 Excalidraw 主题跟随；⑥ 切换到 Workflow 视图再切回 Canvas，Excalidraw 画布恢复正常。

## Phase 1.2 — Magic Generate 入口 + 底部 PromptBar

- `FlovartExcalidrawStage.tsx` 新增 `bottomBar` prop（React 节点，绝对定位在 Stage 容器底部）和 `onMagicGenerate` 回调（截图 dataUrl + 宽高 + 选区 bounds）。
- `FlovartExcalidrawStage.tsx` 新增 pop-bar 浮动按钮：选中 ≥2 元素时在选区 bounding box 顶部上方 44px 处显示 Magic Generate 按钮（motion 弹入/弹出 + Wand2 图标 + backdrop-blur 毛玻璃背景），点击触发 `exportToCanvas` → `toDataURL('image/png', 0.8)` → 调用 `onMagicGenerate` 回调。
- `App.tsx` 新增 `promptDocument` state（Tiptap 富文本编辑器 JSON）、`mentionItems` useMemo（从当前画板元素排除已选中元素构建 @ 引用列表）、`handleMagicGenerate` handler（创建 `ImageElement` href=截图 dataUrl → `commitAction` 加入画布 → 清空选中 → 设置 mentionedElementIds → 调 `handleGenerate`）。
- `App.tsx` 在 `<FlovartExcalidrawStage>` 上传入 `bottomBar={<PromptBar ... />}`（inline variant + popoverDirection up + 全量 props：）和 `onMagicGenerate={handleMagicGenerate}`。
- 两种生成入口均已接入：① jaaz 截图范式（选中 ≥2 元素 → pop-bar Magic Generate → 截图 → 创建 ImageElement → handleGenerate）；② PromptBar 范式（底部 PromptBar 写 prompt + @ 引用 → onGenerate → handleGenerate）。
- 待用户验证：① 底部 PromptBar 正常显示，输入 prompt 后点击生成按钮触发 `handleGenerate`；② @ 引用弹出列表显示当前画板元素（排除已选中）；③ 选中 ≥2 元素时选区上方出现 Magic Generate 浮动按钮；④ 点击 Magic Generate 后截图作为新 ImageElement 出现在画布上并自动触发生成；⑤ pop-bar 在选中 <2 元素时消失；⑥ PromptBar 的 mode 切换（image/video）、视频参数、批量、角色锁、auto-enhance 等控件正常工作。

## Phase 1.3-1.4 — Agent Chat 主交互（Tool-Calling 循环 + 13 画布工具）

- 新建 `services/agentChatStream.ts`：`chatCompletionWithTools(opts)` 调 `/chat/completions` + `tools` + `stream:true`，SSE 流式解析 `delta.content` + `delta.tool_calls`（按 index 拼接），支持 `onDelta` 增量回调 + `AbortSignal` 取消；复用 `aiGateway.ts` 的 `inferProviderFromModel` + `normalizeProviderBaseUrl` + `DEFAULT_BASE_URLS` 解析 provider/baseUrl/apiKey。
- 新建 `components/agent-chat/ChatMessage.tsx`：消息组件（用户/Agent/系统 + 工具调用卡片），工具卡片显示工具中文名 + 参数摘要 + 结果 + 状态图标（pending …/success ✓/error ✕/confirmed ✓），附件预览（图片缩略图/文件占位）。
- 新建 `components/agent-chat/ChatTextarea.tsx`：输入框（文本 + 图片附件拖拽/粘贴 + 模型选择下拉 + 发送/停止按钮），Enter 发送 Shift+Enter 换行，自动高度，附件可删除。
- 新建 `components/agent-chat/Chat.tsx`：主面板，13 工具定义（读类 3 + 创建类 3 + 编辑类 4 + 视口类 1 + 生成类 2），Tool-Calling 循环（最多 4 步，首轮 `tool_choice:"required"`，`parallel_tool_calls: false`），工具执行函数（`canvas_get_state`/`get_selection`/`export_snapshot` 读画布快照；`create_text`/`create_image`/`create_video`/`generate_image`/`generate_video` 创建元素 + `handleGenerate`；`update_element`/`move_elements`/`delete_elements`/`select_elements` 操作元素），compactSnapshot 上下文注入，confirmTools 人工确认开关（默认自动执行），会话日志 localStorage 持久化（`flovart.agentChat.logs.v2`，最近 100 条）。
- `RightPanel.tsx` agent tab 替换：props 齐全时渲染新 `<Chat>`（传入 elements/selectedElementIds/setSelectedElementIds/commitAction/handleGenerate/userApiKeys/modelPreference），否则回退旧 `<AgentChatPanel>`。
- `App.tsx` RightPanel 调用处新增 7 个 props 传入（elements/selectedElementIds/setSelectedElementIds/commitAction/handleGenerate/userApiKeys/modelPreference）。
- 系统提示（ONLINE_AGENT_PROMPT 适配版）：首轮必须调用工具；不输出 JSON ops；不编造执行结果；用真实 id；意图不明直接问用户。
- 待用户验证：① RightPanel agent tab 显示新"画布助手"面板（非旧 Agent Chat）；② 输入 prompt 后 Agent 流式回复文本；③ Agent 调用工具时显示工具卡片（如"读取画布"）；④ `canvas_generate_image` 工具触发后画布上出现新 ImageElement 并开始生成；⑤ `canvas_create_text` 工具创建文本元素出现在画布上；⑥ `canvas_delete_elements`/`move_elements` 工具操作反映到画布；⑦ 停止按钮可中断流式；⑧ 清空按钮重置对话；⑨ confirmTools 开关切换不影响自动执行（当前默认自动）；⑩ 对话日志刷新页面后保留。
- Phase 1.3 "加入对话"入口：⑪ 选中画布上 1 个图片元素时，选区上方出现"加入对话"浮动按钮（MessageCircle 图标）；⑫ 点击"加入对话"后按钮消失、选区清空，Agent Chat 输入框附件区出现该图片缩略图；⑬ 选中 ≥2 元素时只显示"Magic Generate"不显示"加入对话"（互斥）；⑭ 选中非图片元素（如文本/形状）时不显示"加入对话"；⑮ "加入对话"注入的附件可正常随消息发送给 Agent。

## Phase 1.5 — Overlay 适配 Excalidraw viewport + ElementToolbar 补回

- `FlovartExcalidrawStage.tsx` 新增两个 props：`onViewportChange?: (vp: ExcalidrawViewport) => void`（在 `handleSceneChange` 中每次视口变化时回调）和 `containerRef?: React.RefObject<HTMLDivElement | null>`（ref 附着到 `.excalidraw-container` div）。
- `App.tsx` 新增 `excalidrawViewport` state（`{ x: 0, y: 0, zoom: 1 }`）+ `excalidrawContainerRef` ref；`canvasViewport: CanvasViewport` 通过 `useMemo` 从 `ExcalidrawViewport` 转换（`panScreenX = x * zoom, panScreenY = y * zoom`）。
- `canvasContainerRect` 从旧 `svgRef`（null ref，Phase 1.1 SVG 已删除）切换到 `excalidrawContainerRef`，`useElementContainerRect` 恢复正常 rect 读取。
- `handleGroupSelection`（选中 ≥2 元素 → 创建 `GroupElement` 加入画板）和 `handleExportSelection`（选中媒体元素 → 逐个触发 `<a download>` 下载）新建为 App.tsx 内函数。
- `<ElementToolbar>` JSX 补回到 `main` 片段中 `<FlovartExcalidrawStage>` 之前，传入 `viewport={canvasViewport}` + `containerRect={canvasContainerRect}` + 全量 handlers（align/group/export/copy/download/delete/property/crop/filter/upscale/removeBg/outpaint/mask/split/reversePrompt）。
- `<FlovartExcalidrawStage>` 新增 `onViewportChange={setExcalidrawViewport}` + `containerRef={excalidrawContainerRef}` 传入。
- `canvasOverlayViewport.ts` 本身无需修改——它早已设计为 viewport 无关（`CanvasViewport` 接口的 `panScreenX/Y` 对 Konva 和 Excalidraw 均适用，注释已说明 Excalidraw 转换公式）。
- 未补回的 overlay：inpaint 蒙版 prompt（旧 SVG lasso 工作流，Excalidraw 需重新设计交互）、stageLabel（视频生成中状态标签，属 Phase 2 视频工具范畴）、属性面板（Excalidraw 自带属性面板，冗余）、文本编辑 textarea（Excalidraw 自带双击编辑文本）。
- 待用户验证：① 选中单个元素时 ElementToolbar 浮动工具栏出现在元素上方（含对齐/复制/删除/属性等按钮）；② 选中 ≥2 元素时多选工具栏出现（含对齐/导出/打组）；③ 滚动画布（pan）后 ElementToolbar 跟随元素位置移动；④ 缩放画布（zoom）后 ElementToolbar 定位正确、不偏移；⑤ ElementToolbar 中的 AI 工具按钮（裁剪/滤镜/高清/抠图/扩图/蒙版/拆层/反推）点击后弹窗正常打开；⑥ ElementToolbar 超出视口上沿时自动翻转到下方；⑦ 切换深色/浅色主题后 ElementToolbar 样式跟随。

## Phase 1.5 补充（第二批）— 坐标转换重建 + Category B 死代码清理

- **坐标转换重建**：App.tsx 新增 `screenToCanvas(screenX, screenY)` 和 `getCanvasCenter()` 两个函数，基于 `excalidrawContainerRef.current.getBoundingClientRect()` + `excalidrawViewport`（Excalidraw screen = (canvas + scrollX) * zoom，故 canvasX = (screenX - rect.left) / zoom - viewport.x）。
- **useGeneration.ts 替换完成**：6/6 处 `svgRef`/`getCanvasPoint` 全部替换为 `getCanvasCenter()`——video keyframe、video main、image with mentions(mentionPrompt2)、image plain(effectivePrompt)、handleSelectBatchResult、handleSelectAllBatchResults。params 接口移除 svgRef/getCanvasPoint，新增 getCanvasCenter。grep 确认 0 残留。
- **App.tsx 替换完成**：4/4 处 `svgRef`/`getCanvasPoint` 全部替换——handleAddImageElement→getCanvasCenter()、handleAddVideoElement→getCanvasCenter()、handleAssetDrop→screenToCanvas()、agent runtime 删除内部重复 getCanvasCenter 定义（复用外部传入）。useGeneration 调用更新：移除 handleInpaint/inpaintState/inpaintPrompt/svgRef/getCanvasPoint/setInpaintState/setInpaintPrompt，新增 getCanvasCenter。
- **editingElement 死代码删除**：state + handleStopEditing + 2 useEffects + keyboard branches（Delete/Backspace/Esc）+ board-switch setEditingElement(null) 全部删除。grep 确认 0 残留（仅 useCanvasInteraction 解构待清理，已在本批完成）。
- **视口动画死代码删除**：`animateViewportToElement`/`cancelViewportAnimation`/`viewportAnimationRef`/`handleElementDoubleClickFocus` 全部删除（函数 + ref + agent runtime 调用 + WorkspaceSidebar onElementDoubleClick prop + getCanvasVisibleRegion import）。grep 确认 0 残留。这些函数驱动的 `activeBoard.panOffset`/`zoom` 不再渲染，后续 Phase 如需视口动画需基于 Excalidraw API 重建。
- **crop 死代码删除**：`croppingState`/`setCroppingState` state + `handleStartCrop`/`handleCancelCrop`/`handleConfirmCrop` 函数 + board-switch setCroppingState(null) + useCanvasInteraction call params + Toolbar `isCropping`/`onConfirmCrop`/`onCancelCrop` props + Toolbar crop UI 块（crop controls 弹层）+ ElementToolbar `handleStartCrop` prop + crop 按钮 全部删除。grep 确认 0 残留。
- **mask/inpaint 死代码删除**：`inpaintState`/`inpaintPrompt`/`maskEditingId`/`maskBrushSize`/`maskBrushMode`/`maskCanvasRef` state + `paintMask` + `startMaskEditing`/`commitMask`/`cancelMask`/`clearMask` 函数 + mask toolbar JSX + ElementToolbar `maskEditingId`/`startMaskEditing` props + mask 按钮 + useCanvasInteraction call params 全部删除。grep 确认 0 残留。
- **handleCanvasImageDragStart 删除**：旧 SVG 拖拽事件，SVG 已删除，死代码。
- **useCanvasInteraction.ts 清理**：接口 `UseCanvasInteractionParams` 移除 editingElement/setEditingElement/croppingState/setCroppingState/setInpaintState/setInpaintPrompt/maskEditingId/paintMask/onElementDoubleClick；解构移除对应参数；refs 移除 svgRef（保留为内部 ref 供 handleWheel）、editingTextareaRef；handleMouseDown 移除 mask painting intercept、cropping block、setEditingElement(text tool)、double-click setEditingElement/onElementDoubleClick；handleMouseMove 移除 mask painting intercept、crop handling block；handleMouseUp 移除 mask painting intercept、inpaint lasso block；handleWheel 移除 croppingState/editingElement check；return 移除 svgRef/editingTextareaRef/getCanvasPoint。
- **App.tsx useCanvasInteraction 解构更新**：移除 `getCanvasPoint`/`svgRef`/`editingTextareaRef`，仅保留 `elementsRef`/`previousToolRef`/`spacebarDownTime`。
- **Toolbar.tsx 清理**：接口移除 `isCropping`/`onConfirmCrop`/`onCancelCrop`；解构移除对应参数；删除 `if (isCropping) { return <crop controls> }` 整个 UI 块。
- **ElementToolbar.tsx 清理**：接口移除 `maskEditingId`/`handleStartCrop`/`startMaskEditing`；解构移除对应参数；删除 crop 按钮（`{element.type === 'image' && <button title={t('contextMenu.crop')} ...>}`）和 mask 按钮（`<button title="图层蒙版 / Layer Mask (Inpaint)" ...>`）。
- 待用户验证：① `npm run dev` 编译无报错、无未定义引用；② 画布主路径正常显示 Excalidraw；③ 选中图片元素时 ElementToolbar 不再显示裁剪和蒙版按钮（已删除）；④ Toolbar 不再显示 crop 确认/取消弹层（已删除）；⑤ Agent Chat 生成图片/视频功能正常（getCanvasCenter 替换后）；⑥ 从本地拖入图片/视频到画布落点正确（screenToCanvas 替换后）；⑦ 键盘快捷键（删除/复制/撤销/重做/空格临时切换平移工具）不报错（editingElement 分支已清理）。

## Phase 1.5 补充（第一批）— App.tsx 死代码清理

- 删除 `canvasStageSize` state + ResizeObserver useEffect（SVG 已删除，`svgRef.current` 恒为 null，state 从未被读取）。
- 删除 `canvasKonvaReadyIds`/`canvasKonvaFailedIds` state + `canvasKonvaDisabledIds`/`canvasKonvaDimmedIds` useMemo + 清理 useEffect（Konva 在 Phase 1.1 已移除，这些计算结果从未被 JSX 消费）。
- 删除 `contextMenu`/`setContextMenu` state + `handleContextMenu` 函数 + 4 处 `setContextMenu(null)` 调用（右键菜单仅由旧 SVG 渲染，现已无 JSX 消费；`useCanvasInteraction` 的 `contextMenu`/`setContextMenu` 参数改为 optional）。
- 删除 `relationFocusElementId`/`setRelationFocusElementId` state + `getRelatedCanvasElementIds` callback + `relationFocusIds`/`selectedRelationIds` useMemo + 关联 useEffect + `selectedNodePromptElement` useMemo + `isNodePromptActive`（"节点 prompt 高亮"是旧 SVG overlay 特性，Excalidraw 不需要；`setRelationFocusElementId` 从未被设为非 null 值，整条链路恒为空）。
- 删除 `handleFitView`（仅作为 `onTripleClickEmpty` 传给 `useCanvasInteraction`，SVG 三击空白事件已不可能触发）。
- 删除 wheel useEffect（绑定 `svgRef.current` 的 wheel 监听，null → no-op）+ mouseup/blur useEffect（`handleMouseUp` 在 interactionMode 恒 null 时空转）。
- 删除 `cursor` 变量计算（引用已删的 `interactionMode`，且从未被任何 style 消费）。
- 从 `useCanvasInteraction` 解构中移除 `handleMouseDown`/`handleMouseMove`/`handleMouseUp`/`handleWheel`/`getSelectableElement`/`selectionBox`/`setSelectionBox`/`alignmentGuides`/`lassoPath`/`dragTick`/`interactionMode`（全部无 JSX 消费）；保留 `getCanvasPoint`/`svgRef`/`editingTextareaRef`/`elementsRef`/`previousToolRef`/`spacebarDownTime`（仍有活跃消费者）。
- 清理未使用 import：`placeOverlay`/`canvasToScreen`/`OverlayAnchor`（canvasOverlayViewport）、`isPointInPolygon`/`rasterizeElement`/`rasterizeMask`/`SNAP_THRESHOLD`/`Guide`（canvasHelpers）、`PathElement`/`ShapeElement`/`ArrowElement`/`LineElement`（types）。
- App.tsx 从 ~3876 行缩减到 ~3387 行（净删 ~489 行）。
- 保留但已失效（Category B，需后续 Phase 重建为 Excalidraw 等价物，本次不动）：`animateViewportToElement`/`cancelViewportAnimation`/`viewportAnimationRef`（仍被 agent API `igniteAtomicElement` 和 LayerPanel `onElementDoubleClick` 调用，但驱动的 `activeBoard.panOffset`/`zoom` 不再渲染）、`editingElement`/`editingTextareaRef`（commitAction 文本分支仍引用，但 `editingElement` 恒为 null）、`croppingState`/`maskEditingId`/`inpaintState`（UI 入口仍挂载但画布交互已失效）。
- 待用户验证：① `npm run dev` 编译无报错、无未定义引用；② 画布主路径正常显示 Excalidraw；③ 图层面板双击元素不再报错（no-op 可接受）；④ 键盘快捷键（删除/复制/撤销/重做/空格临时切换平移工具）不报错；⑤ 工具栏、侧边栏、右栏 dock 正常显示。

## Phase 2.1 — Script 节点 + 工作流编排基础

### 类型与数据结构
- **新增 Script 节点类型**：`WorkflowNodeType` 新增 `'script'`，作为 Workflow 第 6 种节点类型。Script 节点定位为"剧本拆解 + 分镜编排 + 批量生成"容器，Canvas 纯视觉不背复杂状态机。
- **新增类型定义**（`types.ts`）：`ScriptAsset{id,kind:'character'|'scene'|'prop',name,description?,settingImageNodeId?}`、`ScriptShot{id,index,emotion?,action?,dialogue?,sfx?,scene?,promptOverride?,colorTag?,imageNodeId?,videoNodeId?,status?}`、`ScriptBreakdown{assets,shots,sourceText?,referenceVideoNodeId?,modelId?}`、`WorkflowBatchGroupSource='auto'|'manual'`。
- **WorkflowNode 扩展**：新增 `batchGroupSource?: WorkflowBatchGroupSource` 字段，区分手动打组（Ctrl+G）和自动打组（批量生成 count>1）。
- **WorkflowNodeMetadata 扩展**：新增 `referenceNodeIds?: string[]`（显式声明上游引用节点，覆盖连线推断）、`scriptBreakdown?: ScriptBreakdown`（Script 节点的分镜数据）。
- **新增 3 种 WorkflowOp**：`group_nodes{ids,batchId,source}`、`ungroup_nodes{ids}`、`execute_group{nodeIds}`。

### 常量与节点规格
- **Script 节点默认尺寸**：480×560，`metadata.scriptBreakdown` 默认 `{ assets: [], shots: [] }`。

### 操作与算法（ops.ts）
- **`topoSort(nodes, connections, nodeIds)`**：Kahn BFS 拓扑排序，用于整组执行时确定节点运行顺序。含 cycle fallback（检测到环时退回原始顺序，不阻塞执行）。
- **`getUpstreamData(targetNode, nodes, connections)`**：连线数据推断函数。`referenceNodeIds` 优先（显式声明），否则用入边推断。返回 `UpstreamData{imageHrefs,videoHrefs,audioHrefs,textContents,referenceNodeIds}`，供 `workflowGeneration.ts` 后续注入下游参考输入。
- **Script 连线规则**：`validateWorkflowConnection` 新增——Script 节点只能连向 image/video 节点；Script 节点只能接收 image/video/text 输入。
- **`group_nodes` op 处理**：给选中节点分配同一 `batchId` + `batchGroupSource`，复用现有 batch 折叠/展开 UI。
- **`ungroup_nodes` op 处理**：清除 `batchId`/`batchIndex`/`batchGroupSource`。
- **`execute_group` op 处理**：`topoSort` 排序后推入 `runRequests`，由 `applyOps` 依次调用 `onRunNode`。

### UI 集成
- **工具栏**：`WorkflowToolbar` 新增脚本节点按钮（FileText 图标）；`WorkflowCreateMenu` 新增"脚本节点"选项。
- **Script 节点缩略卡片**（`WorkflowNode.tsx` → `ScriptNodeCard`）：480×560 节点内显示 Clapperboard 图标 + 统计（N 资产 / N 分镜 / N/N 完成）+ 前 4 个分镜摘要行 + "+N 更多分镜"。空脚本显示"双击打开编辑器拆解剧本"。
- **双击打开编辑器**：`WorkflowNode` 新增 `onDoubleClick` prop，Script 节点双击触发 `setScriptEditorNodeId` → 挂载 `ScriptNodeEditor` 全屏 Modal。
- **Ctrl+G 打组快捷键**：`InfiniteWorkflow` keydown handler 新增 `Ctrl/Cmd+G`，选中≥2 节点时 `applyOps([{type:'group_nodes',ids,batchId:nanoid(),source:'manual'}])`。
- **工具栏打组/取消打组/整组执行按钮**：`WorkflowNodeToolbar` 新增 `onGroup`/`onUngroup`/`onExecuteGroup` props，多选时显示 Group/Ungroup/Play 图标按钮。
- **applyOps 处理 runRequests**：`applyOps` 新增 `result.runRequests.forEach(({nodeId}) => onRunNode(nodeId))`，使 `execute_group` op 能触发实际生成。
- **Script 节点不显示 Run/Stop/PromptFocus 按钮**：`WorkflowNodeToolbar` 排除 `node.type === 'script'` 的 run/stop/prompt-focus 操作。
- **workflowGeneration.ts 防护**：`runWorkflowGeneration` 检测 `initialNode.type === 'script'` 时直接返回错误状态"脚本节点请双击打开编辑器进行拆解和批量生成"。

### ScriptNodeEditor（全屏 Modal 分镜编辑器）
- **左侧栏（280px）**：剧本原文 textarea + "AI 拆解剧本"按钮（直调 `generateTextWithProvider`，system prompt 要求输出结构化 JSON）+ 资产列表（角色/场景/道具卡片，可增删改）。
- **右侧主区域**：Segmented 切换"分镜/资产"Tab。分镜列表为 ShotCard 网格，每张卡片含 #序号 + 情绪/动作/场景/台词/音效/生图Prompt 字段 + 已关联图片/视频节点角标 + 删除按钮。支持添加空白分镜。
- **AI 拆解**：调用 `generateTextWithProvider(sourceText, model, undefined, {systemPrompt, temperature:0.7, maxTokens:8192})`，响应解析为 `ScriptBreakdown`（assets + shots），失败时显示错误。
- **模型选择**：优先 `modelPreference.textModel`，无则取第一个有模型的文本 Key。无可用 Key 时提示并调用 `onOpenSettings`。

### CSS 样式
- `workflow.css` 新增 `.workflow-node__script-card`/`__script-header`/`__script-stats`/`__script-empty`/`__script-shot-row`/`__script-shot-index`/`__script-shot-desc`/`__script-more` 样式。

### 连线数据传递（workflowGeneration.ts）
- **`getUpstreamData` 集成**：`runWorkflowGeneration` 改用 `getUpstreamData(initiating, source.nodes, source.connections)` 推断上游节点，支持 `metadata.referenceNodeIds` 显式覆盖连线推断。
- **prompt 构建**：使用 `upstream.textContents`（包含上游文本节点 content + 配置节点 prompt）替代手动 filter，同时保留 `mentionedNodeIds`（@-引用）union。
- **references 构建**：保持 `resolveMediaHref`（支持 storageKey → blob URL 解析），`mediaSources` 从 `related` 节点构建。

### ScriptNodeEditor 批量生图/生视频
- **批量生成按钮**：分镜 Tab 头部新增"批量生图"和"批量生视频"按钮（无分镜时 disabled），点击触发 `onBatchGenerate(mode)` 回调。
- **InfiniteWorkflow `handleScriptBatchGenerate`**：
  - 读取 Script 节点的 `scriptBreakdown.shots`，过滤出有 prompt（`promptOverride` 或 `action`/`scene`）且未已关联节点的分镜。
  - 在 Script 节点右侧创建网格布局（每行 5 个，间距 20px），自动避碰：检测右侧已有节点，若有碰撞则将网格起点右移到已有节点右边缘之后。
  - 每个分镜创建一个 image/video 节点：`prompt` = `shot.promptOverride` 或从 `scene/action/emotion` 拼接，`referenceNodeIds: [scriptNodeId]`，标题 `分镜 N`。
  - 创建 script→image/video 连线。
  - 更新 `scriptBreakdown.shots` 中每个分镜的 `imageNodeId`/`videoNodeId` 指向新建节点。
  - `group_nodes` op 将所有新节点打组（`batchGroupSource: 'auto'`）。
  - `applyOps` 成功后依次调用 `onRunNode` 触发生成，并选中所有新节点。

### 待用户验证
- ① `npm run dev` 编译无报错；② 工具栏和双击空白菜单出现"脚本节点"选项，可创建 Script 节点；③ Script 节点缩略卡片正确显示统计和分镜摘要；④ 双击 Script 节点打开全屏 Modal 编辑器；⑤ 输入剧本 + 点击"AI 拆解剧本"能正确调用 LLM 并生成分镜列表；⑥ 分镜卡片可编辑/删除，资产列表可增删改；⑦ Ctrl+G 选中多个节点能打组（batchId + batchGroupSource:'manual'）；⑧ 工具栏出现"打组"/"整组执行"/"取消打组"按钮；⑨ "整组执行"按钮按拓扑顺序依次运行节点；⑩ Script 节点不显示 Run/Stop/PromptFocus 按钮；⑪ "批量生图"/"批量生视频"按钮在分镜 Tab 出现，无分镜时 disabled；⑫ 点击批量生图后右侧出现网格布局的图片节点，每个节点 prompt 为分镜的 promptOverride，且自动开始生成；⑬ 批量生成的节点自动打组（batchGroupSource:'auto'）并选中；⑭ 已关联图片节点的分镜不会被重复生成；⑮ script→image/video 连线自动创建。

## Phase 2.2 — Slash 命令面板 + 生成器增强（风格库 / 运镜 / 摄像机参数）

### 类型与数据结构
- **新增类型**（`types.ts`）：`CameraParams{camera,lens,focalLength,aperture}`、`StylePreset{id,name,category,promptPrefix,previewUrl?,isCustom?}`、`CameraMovement{id,name,description,promptKeyword,isCustom?}`、`SlashCommand{id,label,description,icon,category,mode,minSources,maxSources,generateCount,gridCols,promptBuilder}`。
- **WorkflowGenerationConfig 扩展**：新增 `camera?: CameraParams`、`styleId?: string`、`cameraMovement?: string`、`customMovement?: string` 字段。

### 常量库
- **`CAMERA_MOVEMENTS`**（21 项）：推进/拉远/平移/俯仰/移动/环绕/升降/手持/固定/变焦推进/变焦拉远/甩镜头/荷兰角/第一人称/航拍/跟随/建立镜头/特写/过肩/仰拍/俯拍，每项含 `id`/`name`/`description`/`promptKeyword`。
- **`STYLE_PRESETS`**（22 项）：anime/realistic/oil-painting/watercolor/cyberpunk/ghibli/pixar/claymation/lowpoly/voxel/ink-wash/pencil-sketch/charcoal/3d-render/isometric/neon-punk/vaporwave/surreal/gothic/baroque/pop-art/comic-book，每项含 `id`/`name`/`category`/`promptPrefix`，按 `category` 分为 `cinematic`/`illustration`/`art`/`stylized` 四组。
- **`CAMERA_OPTIONS`**：6 种相机（ARRI Alexa/RED Komodo/Canon EOS R5/Sony A7S III/35mm Film/IMAX）、6 种镜头（24mm/35mm/50mm/85mm/135mm/Anamorphic）、5 种焦距（14mm/24mm/35mm/50mm/85mm）、5 种光圈（f/1.4/f/2.0/f/2.8/f/4.0/f/5.6）。

### 配置面板（WorkflowConfigPanel.tsx）
- **风格下拉**：按 `category` optgroup 分组，选中后注入 `styleId` 到 config。
- **相机/镜头/焦距/光圈下拉**：仅 image 模式显示，选中后注入 `camera` 到 config。
- **运镜下拉**：仅 video 模式显示，选中预设运镜或输入自定义运镜关键词。

### Prompt 前缀注入（workflowGeneration.ts）
- **注入逻辑**：`promptPrefix = [stylePreset?.promptPrefix, cameraPrefix, movementKeyword].filter(Boolean).join(', ')`，拼到用户 prompt 最前面。
- **cameraPrefix 构建**：`camera: {camera} {lens}, {focalLength} lens, {aperture}`（有 camera 参数时才构建）。
- **movementKeyword**：优先 `customMovement`，其次从 `CAMERA_MOVEMENTS` 查找 `cameraMovement` 对应的 `promptKeyword`。
- **纯前端 prompt 工程**：不改 Provider 层，不传额外参数给 API。

### Slash 命令面板（SlashMenu.tsx）
- **6 条 Slash 命令**：
  - `multi-camera-9`（多机位九宫格）：从 1 张图生成 9 个不同机位视角（3×3 网格），promptBuilder 按 index 注入 front/front-left/left/back-left/back/back-right/right/front-right/top-down 9 个角度。
  - `character-3-view`（角色三视图）：从 1 张角色图生成前/侧/后三视图（3 列），promptBuilder 注入 front view/side profile view/back view。
  - `story-4-grid`（剧情推演四宫格）：将描述拆为 4 格剧情演绎图（2×2 网格），promptBuilder 注入 `scene N of 4`。
  - `lighting-fix`（电影级光影矫正）：修复不稳定光线，专业色彩分级，单图输出。
  - `portrait-enhance`（人像质感增强）：增强皮肤质感、光影细节，单图输出。
  - `storyboard-25`（25宫格连贯分镜）：将描述拆为 25 帧连贯分镜（5×5 网格），promptBuilder 注入 `frame N of 25`。
- **搜索过滤**：输入关键词过滤命令列表（label + description + 分类标签匹配）。
- **键盘导航**：ArrowUp/Down 切换高亮，Enter 执行，Escape 关闭。
- **分类标签**：分镜/角色/机位/增强，显示在每条命令右侧。

### Slash 命令集成（InfiniteWorkflow.tsx）
- **`/` 键触发**：在画布空白处按 `/` 键打开 SlashMenu（居中定位），在 textarea/input/select 等 BLOCKED_TARGET 中不触发。
- **命令执行**（`handleSlashCommand`）：
  - 从当前选中节点中筛选 `command.mode` 类型且 `status:'success'` 的节点作为源（最多 `maxSources` 个）。
  - 源不足 `minSources` 时显示 notice 提示用户先选择源节点。
  - 按 `gridCols` 网格布局创建 `generateCount` 个新节点（居中放置），每个节点 prompt 由 `command.promptBuilder(index, total)` 生成。
  - `referenceNodeIds` 设为源节点 ID 列表，`metadata.status` 为 `idle`。
  - 每个源节点 → 每个新节点创建 `connect_nodes` op。
  - `group_nodes` op 自动打组（`batchGroupSource:'auto'`）。
  - `applyOps` 成功后依次 `onRunNode` 触发生成 + `selectNodes` 选中所有新节点。
- **Escape 关闭**：SlashMenu 打开时按 Escape 优先关闭 SlashMenu（不触发其他 Escape 行为）。
- **点击空白关闭**：`onSurfacePointerDown` 新增 `setSlashMenu(null)`。

### 网格拼图工具（gridComposer.ts）
- **`composeImageGrid(sources, options)`**：将多张图片加载后 cover-fit 绘制到 Canvas 网格中，返回合成 PNG Blob。
- **参数**：`cols`/`cellWidth`(512)/`cellHeight`(512)/`gap`(8)/`padding`(12)/`background`(#1a1a2e)/`labels`/`labelColor`/`labelBackground`。
- **图片加载**：复用 `loadWorkflowMediaBlob`，支持 storageKey 和 href 两种来源；加载失败的格子绘制占位色块。
- **标签绘制**：可选在每格左上角绘制文字标签（带半透明背景条）。

### CSS 样式
- `workflow.css` 新增 `.workflow-slash-menu`/`__search`/`__search-icon`/`__input`/`__list`/`__empty`/`__item`/`__item--active`/`__item-icon`/`__item-text`/`__item-label`/`__item-desc`/`__item-badge` 样式。

### 待用户验证
- ① `npm run dev` 编译无报错；② 选中配置节点时配置面板出现风格/相机/镜头/焦距/光圈/运镜下拉；③ 选择风格后生成的图片 prompt 前缀包含风格关键词（如 `anime style, ...`）；④ 选择运镜后生成的视频 prompt 前缀包含运镜关键词（如 `camera push in, ...`）；⑤ 在画布空白处按 `/` 键弹出 Slash 命令面板；⑥ Slash 面板支持搜索过滤和键盘导航（Arrow/Enter/Escape）；⑦ 选中 1 张已生成图片后执行"多机位九宫格"，右侧出现 3×3 网格的 9 个图片节点并自动开始生成；⑧ 选中 1 张角色图后执行"角色三视图"，出现 3 列图片节点（前/侧/后视图）；⑨ 执行"25宫格连贯分镜"，出现 5×5 网格的 25 个图片节点；⑩ Slash 命令生成的节点自动打组并选中；⑪ 源节点不足时显示提示信息；⑫ `gridComposer.ts` 的 `composeImageGrid` 能正确合成网格图片（需后续 Phase 在 UI 中接入使用）；⑬ Escape 键能正确关闭 Slash 面板；⑭ 点击画布空白处能关闭 Slash 面板。

## Phase 2.3 — 图像工具补齐 + 视频工具（ffmpeg.wasm）

### 依赖与构建配置
- **新增依赖**：`@ffmpeg/ffmpeg@0.12.15` + `@ffmpeg/util@0.12.2`，懒加载 `@ffmpeg/core@0.12.6`（从 unpkg CDN，~31MB），仅用户首次点击视频工具时下载。
- **vite.config.ts**：`server.headers` 添加 `Cross-Origin-Opener-Policy: same-origin` + `Cross-Origin-Embedder-Policy: require-corp`，启用 SharedArrayBuffer 多线程；`build.rollupOptions.output.manualChunks` 新增 `vendor-ffmpeg` chunk，把 `@ffmpeg/ffmpeg` + `@ffmpeg/util` 单独打包便于懒加载。
- **COOP/COEP 影响**：开启后所有跨域资源必须带 CORP 头；当前项目图片主要走本地 blob/同源，未受影响。生产部署需在反代/CDN 上同样下发这两个头。

### ffmpeg 客户端（services/ffmpegClient.ts）
- **`getFFmpeg()`**：单例懒加载，首次调用 `FFmpegWASM.load()` 时根据 `isMultiThreadAvailable()` 决定加载单线程或多线程 core URL（`unpkg.com/@ffmpeg/core@0.12.6/dist/umd` 或 `@ffmpeg/core-mt@0.12.6`）。
- **`isFFmpegSupported()`**：检测 `SharedArrayBuffer` + `WebAssembly` 是否可用。
- **`isMultiThreadAvailable()`**：检测 `SharedArrayBuffer` + 多线程 core 可用性，决定是否启用 MT core。

### 视频工具服务（services/videoTools.ts）
- **`trimVideo(file, startSec, endSec)`**：用 ffmpeg `-ss` + `-to` + `-c copy` 流复制剪裁，不重编码，极快；返回 `Blob { type: video/mp4 }`。
- **`splitAudioVideo(file)`**：`-vn -c:a copy` 提取音频 + `-an -c:v copy` 提取纯视频；返回 `{ video: Blob, audio: Blob }`。
- **`mergeVideos(files)`**：先为每个输入生成 ts 片段（`-c copy -bsf:v h264_mp4toannexb`），再 concat demuxer 合并 + `-c copy` 输出 MP4；要求所有输入编码参数一致。
- **`getVideoDuration(file)`**：`-i` 解析 stdout 中 `Duration: hh:mm:ss.xx` 行。
- 所有命令统一通过 `ffmpeg.exec([...])` 调用；文件挂载/卸载通过 `ffmpeg.writeFile` / `ffmpeg.deleteFile`。

### 图像变换服务（services/imageTransform.ts）
- **`transformImage(src, action)`**：纯 Canvas 实现，`action` 支持 `rotate-90`/`rotate-180`/`rotate-270`/`flip-h`/`flip-v`；返回 PNG Blob。无 AI、无网络。

### 宫格切分服务（services/gridSplitter.ts）
- **`splitGrid(src, rows, cols)`**：纯 Canvas 按 `rows × cols` 切分源图，返回 `SplitGridResult[]`（含 `blob` + `row` + `col` + `index`）。无 AI、无网络。

### 打光预设库（components/workflow/LightingPresets.ts）
- **主光预设 26 项**：按 7 大类分组（自然光/电影感/戏剧光/时尚/恐怖/科幻/古典），每项含 `id`/`name`/`category`/`prompt`（如 `soft window light from the left`、`hard rim light from behind`、`neon under-lighting cyan` 等）。
- **轮廓光预设 9 项**：`rim-back`/`rim-left`/`rim-right`/`kicker`/`hair-light`/`backlight`/`edge-light`/`halo`/`split-rim`。
- **`buildRelightPrompt(keyLight, rimLight, intensity)`**：拼接成图生图 prompt，如 `cinematic relighting, key: soft window light from the left, rim: hard rim light from behind, intensity: strong, preserve identity and composition, relight only`。

### 视频工具对话框（components/workflow/WorkflowVideoToolDialogs.tsx）— 新建
- **`WorkflowVideoToolKind`**：`trim` | `av-split` | `merge`（3 种）。
- **TrimDialog**：双 `Slider` 选择起止时间（0~duration），实时显示 `MM:SS` 标签，`FFmpegStatus` 显示加载/进度/错误。
- **SimpleVideoDialog**：用于 `av-split`（提示"将拆为纯视频 + 纯音频两个节点"）和 `merge`（要求至少选中 2 个视频节点）。
- **FFmpegStatus 组件**：进度条 + 状态文案 + 多线程指示徽章。

### 图像工具对话框扩展（components/workflow/WorkflowImageToolDialogs.tsx）
- **`WorkflowImageToolKind` 现有 12 种**：crop / filter / upscale / remove-background / outpaint / mask / split / **rotate** / **splitGrid** / **annotate** / **relight** / **storyboard**（后 5 个为 Phase 2.3 新增）。
- **RotateFlipDialog**：4 旋转按钮 + 2 翻转按钮 + 预览网格（`.workflow-rotate-grid`），确认后调 `transformImage`。
- **SplitGridDialog**：行/列 Slider（1~5），实时预览切分网格（`.workflow-grid-preview` + `.workflow-grid-cell`），确认后调 `splitGrid` 生成 `rows*cols` 个新节点。
- **AnnotateDialog**：提示词输入 + 颜色选择（`ColorPicker`）+ 笔刷/箭头/文字说明，确认后将 prompt 注入 `runWorkflowImageEdit`（复用现有图生图链路，annotation prompt 作为 mask 引导）。
- **RelightDialog**：主光选择列表（按 category 分组）+ 轮廓光选择列表 + 强度 Slider（low/medium/strong）+ 实时预览 `buildRelightPrompt` 结果；确认后调 `runWorkflowImageEdit`。
- **StoryboardDialog**：源节点选择（多选已成功图像节点）+ 网格布局选择（2×2/3×3/4×4/5×5）+ 标签开关；确认后调 `composeImageGrid`（复用 Phase 2.2 `gridComposer.ts`）生成单张拼图节点。

### WorkflowNodeToolbar 扩展
- **新增 `WorkflowVideoToolHandlers` 接口**：`trim`/`avSplit`/`merge` 三个方法。
- **`WorkflowImageToolHandlers` 新增 5 方法**：`rotate`/`splitGrid`/`annotate`/`relight`/`storyboard`。
- **新增 6 个工具按钮**（lucide 图标）：`RotateCw`（旋转镜像）+ `Grid2x2`（宫格切分）+ `Frame`（分镜组拼接）+ `Lightbulb`（打光面板）+ `Scissors`（音视频分离）+ `ScissorsLineDashed`（剪辑）；`merge` 复用 `Combine` 图标。
- **视频按钮仅在 video 节点显示**：trim/av-split 单选视频节点时显示；merge 选中≥2 视频节点时显示。
- **图像按钮仅在 image 节点显示**：rotate/splitGrid/annotate/relight/storyboard 单选或多选图像节点时显示。

### InfiniteWorkflow 集成
- **新增 state**：`videoTool`（当前打开的视频工具 kind + source 节点）/`videoToolBusy`/`videoToolError`/`storyboardNodeIds`；`videoToolBusyRef` + `videoToolTransactionRef` 防止重复触发。
- **`builtInVideoTools` useMemo**：构造 `WorkflowVideoToolHandlers` 实现，绑定当前 state。
- **`openVideoTool`/`closeVideoTool`/`confirmVideoTool`**：打开/关闭/确认视频工具对话框。
- **`confirmVideoTool` 行为**：
  - `trim`：调 `trimVideo` → 用结果创建新视频节点（连接 source → new）。
  - `av-split`：调 `splitAudioVideo` → 创建 1 个纯视频节点 + 1 个纯音频节点（均连接 source）。
  - `merge`：调 `mergeVideos` → 用合并结果替换或新建视频节点（连接所有 source → new，自动打组 `batchGroupSource:'auto'`）。
- **`confirmImageTool` 新增 5 handler**：
  - `rotate`：调 `transformImage` → 用结果创建新图像节点（连接 source）。
  - `splitGrid`：调 `splitGrid` → 为每个 cell 创建新图像节点（连接 source，自动打组）。
  - `annotate`：将 annotation prompt 作为 mask 注入 `runWorkflowImageEdit`，生成新图像节点。
  - `relight`：调 `buildRelightPrompt` 生成 prompt → `runWorkflowImageEdit` 生成新图像节点。
  - `storyboard`：调 `composeImageGrid` 合成 → 创建单张拼图图像节点（连接所有选中 source）。
- **新增 imports**：`trimVideo`/`splitAudioVideo`/`mergeVideos` from `videoTools` + `composeImageGrid` from `gridComposer` + `buildRelightPrompt` from `LightingPresets` + `transformImage` + `splitGrid` service + `workflowBlobToDataUrl`。
- **`videoTools`/`videoToolBusy` props 传给 `WorkflowNodeToolbar`** + **`WorkflowVideoToolDialogs` 渲染**在 Stage 底部。

### CSS 样式（styles/workflow.css）
- 新增 `.workflow-rotate-grid` / `.workflow-grid-preview` / `.workflow-grid-cell` / `.workflow-image-tool__hint` / `.workflow-relight-*` 样式。
- 视频工具对话框复用现有 `.workflow-image-tool-*` 样式骨架，仅新增 ffmpeg 进度条样式。

### 待用户验证
- ① `npm install` 无报错（`@ffmpeg/ffmpeg` + `@ffmpeg/util` 安装成功，可能有 Excalidraw radix-ui peer dep warnings，非阻塞）；② `npm run dev` 编译无报错，浏览器控制台无 COOP/COEP 报错；③ 选中 1 张图像节点 → 工具栏出现"旋转镜像"按钮 → 点击打开对话框 → 选择 90° → 确认后画布出现新图像节点且方向正确；④ 选中 1 张图像节点 → 点击"宫格切分" → 选 3×3 → 确认后画布出现 9 个新图像节点（自动打组）；⑤ 选中 1 张图像节点 → 点击"打光面板" → 选主光 + 轮廓光 → 确认后调用图生图生成新节点；⑥ 选中多张图像节点 → 点击"分镜组拼接" → 选 2×2 → 确认后出现 1 张拼图节点；⑦ 选中 1 张图像节点 → 点击"标注" → 输入标注文字 → 确认后调用图生图生成带标注的新节点；⑧ 选中 1 个视频节点 → 工具栏出现"剪辑"按钮 → 点击打开 TrimDialog → 首次加载 ffmpeg.wasm（显示加载进度，~31MB）→ 拖动双 Slider 选区间 → 确认后生成新视频节点；⑨ 选中 1 个视频节点 → 点击"音视频分离" → 确认后生成 1 个纯视频 + 1 个纯音频节点；⑩ 选中 ≥2 个视频节点 → 点击"合成" → 确认后生成合并后的新视频节点；⑪ ffmpeg.wasm 多线程在支持 SharedArrayBuffer 的浏览器（Chrome/Edge 桌面端）自动启用，控制台可见 MT core 加载日志；⑫ 不支持 SharedArrayBuffer 的环境（Safari/Firefox 部分 版本）自动降级单线程，功能仍可用但速度较慢；⑬ ffmpeg 加载失败时显示明确错误提示，不阻塞其他工具使用。

## Phase 2.5 — 模型清单全量适配（模块 10）

对齐 LibTV 模型清单模块的 P0+P1+P2 全量适配，覆盖模型注册表能力派生、配置面板能力徽章、Seedance 12 槽位参考输入、合规校验+prompt 优化 pre-flight、音色库 UI。

### 模型注册表能力派生（services/modelTemplateRegistry.ts）
- **ModelTemplate 新增 6 字段**：`referenceSlots`（`ModelReferenceSlotInfo[]`）/ `maxReferences`（number）/ `referenceTypes`（`ModelReferenceKind[]`）/ `complianceCheck`（boolean）/ `promptOptimization`（boolean）/ `notes`（string）。
- **新增类型**：`ModelReferenceSlotInfo` `{ kind: ModelReferenceKind; max: number; label?: string }` + `ModelReferenceKind` = `'image' | 'video' | 'audio'`。
- **`deriveReferenceSlots(model, provider, capability)`**：从 `getCapabilityDictionary` 的 `multimodalSlots` 提取 referenceSlots/maxReferences/referenceTypes，派生到 ModelTemplate。
- **`deriveComplianceAndPromptOptimization(model, provider, capability)`**：按 provider+model 名匹配 Seedance → `complianceCheck:true` + `promptOptimization:true` + notes 文案。
- **新增导出函数**：
  - `getModelCapabilities(model, capability?)` → 返回 `{ referenceSlots, maxReferences, referenceTypes, complianceCheck, promptOptimization, notes, supportsReferenceImage, supportsMaskEdit }`。
  - `isSeedanceModel(model)` → 检测模型名包含 'seedance'。
  - `getReferenceSlotMax(model, kind, capability?)` → 获取指定 kind 的最大槽位数。

### 能力徽章 UI（components/workflow/WorkflowConfigPanel.tsx）
- **`modelCapabilities` useMemo**：调 `getModelCapabilities(config.modelId, mode)` 获取能力信息。
- **5 种能力徽章**（模型下拉下方，`.workflow-config__badges`）：
  - `refs`：`{maxReferences} 参考输入`（如 `12 参考输入`），仅在 maxReferences > 0 时显示。
  - `compliance`：`合规校验`，仅在 complianceCheck 为 true 时显示。
  - `prompt-opt`：`Prompt 优化`，仅在 promptOptimization 为 true 时显示。
  - `ref-edit`：`参考图编辑`，仅在 supportsReferenceImage 为 true 时显示。
  - `mask`：`蒙版编辑`，仅在 supportsMaskEdit 为 true 时显示。
- **Seedance hint 文案**：`isSeedance && mode === 'video'` 时显示说明文字（`.workflow-config__hint`）。
- **SeedanceSlotPicker 集成**：`isSeedance && mode === 'video' && nodes 存在` 时渲染 `<SeedanceSlotPicker>`。

### Seedance 12 槽位参考输入 UI（components/workflow/SeedanceSlotPicker.tsx — 新建）
- **`SeedanceReferences` 接口**（types.ts）：`{ imageRefs: string[]; videoRefs: string[]; audioRefs: string[] }`，存 WorkflowNode ID。
- **`WorkflowGenerationConfig.seedanceRefs`** 字段新增到 types.ts。
- **SLOT_CONFIGS**：3 组配置 — image（max 9）/ video（max 3）/ audio（max 3），总计 12 槽位。
- **每槽位 UI**：`<select>` 从画布候选节点选择（过滤已被占用的节点），已选显示缩略图（image）或节点名 + 清空按钮。
- **实时计数**：`{已选数}/12` 显示在顶部，超过限制时禁用多余 select。
- **音频单独使用警告**：只选音频没选图/视频时显示提示文案。

### Seedance 参考注入集成（services/workflowGeneration.ts）
- **seedanceRefs → references 注入**：将 `config.seedanceRefs` 中的 node ID 解析为媒体 href，构造 `UnifiedIgnitionInput.references`，与自动检测的 references 合并去重（seedance 优先）。
- **去重逻辑**：`seenIds` Set 按 `elementId` 去重，seedanceReferences 排在前。

### 合规校验 + Prompt 优化 Pre-flight（services/promptPreflight.ts — 新建）
- **`runPreflight(prompt, modelId, userApiKeys, capability)`** → `PreflightResult`：
  - `optimizedPrompt`：优化后的 prompt（未优化时等于原 prompt）。
  - `complianceWarnings`：命中的敏感关键词列表。
  - `skippedOptimization` / `skippedCompliance`：是否跳过。
- **合规校验**：基础客户端关键词匹配（暴力/色情/恐怖主义等 11 个中英文关键词），命中时 workflowGeneration.ts 将节点标记 error 并终止生成。
- **Prompt 优化**：用用户 text-capable Key 调 `generateTextWithProvider` 调用 LLM 优化 prompt（Seedance 专用 system prompt，英文输出 ≤200 词）。
- **BYOK 优雅降级**：无 text-capable Key 或调用失败/超时（15s）时，`skippedOptimization = true`，静默跳过，不阻塞生成。
- **workflowGeneration.ts 集成**：在 prompt 组装后、references 构建前调用 `runPreflight`，使用 `effectivePrompt` 替代原始 prompt 传给生成器 + 历史记录。

### 音色库（services/voiceCatalog.ts — 新建 + WorkflowConfigPanel 集成）
- **`VoicePreset` 接口**：`{ id, provider, voiceId, name, description, language, gender, emotionCapable }`。
- **MiniMax Speech 2.8 音色**（17 个）：Wise_Woman / Friendly_Person / Inspirational_girl / Deep_Voice_Man / Calm_Woman / Casual_Guy / Lively_Girl / Patient_Man / Young_Knight / Determined_Man / Lovely_Girl / Decent_Boy / Imposing_Manner / Elegant_Man / Abbess / Sweet_Girl_2 / Exuberant_Girl。
- **OpenAI TTS 音色**（9 个）：alloy / echo / fable / onyx / nova / shimmer / corral / ash / sage。
- **`getVoicesByProvider(provider)`** / **`findVoice(voiceId)`** / **`isCustomVoiceId(value)`** 导出函数。
- **WorkflowConfigPanel 音频模式音色选择器**：原 free-text input 替换为 `<select>` 分组下拉（MiniMax optgroup + OpenAI optgroup），26 个音色可选 + "默认"选项。

### CSS 样式（styles/workflow.css）
- 新增 `.workflow-config__badges`（flex wrap 容器）/ `.workflow-badge--*`（5 种 badge 颜色，CSS color-mix 实现深浅变体）/ `.workflow-config__hint`（Seedance 说明文字样式）。
- 新增 `.workflow-seedance-picker*` 系列样式（容器/标题/计数/槽位行/select/缩略图/清空按钮/警告）。

### 待用户验证
- ① `npm run dev` 编译无报错；② 在 Workflow 配置节点选择 Seedance 视频模型 → 模型下拉下方出现徽章（`12 参考输入` + `合规校验` + `Prompt 优化`）+ Seedance hint 文案 + SeedanceSlotPicker 出现；③ 在 SeedanceSlotPicker 中选择画布上的图片节点 → 计数器更新 `x/12`，已选节点显示缩略图；④ 选择视频节点到 video 槽位 → 计数器更新；⑤ 清空某槽位 → 计数器递减；⑥ 选 Seedance 模型 + 写 prompt + 运行节点 → 生成时先执行合规校验（敏感词 prompt 会被拦截显示 error）→ 有 text Key 时 prompt 被优化为英文 → 生成结果使用优化后的 prompt；⑦ 无 text Key 时 prompt 优化静默跳过，生成正常进行（使用原始 prompt）；⑧ 选非 Seedance 模型 → 不出现徽章和 SeedanceSlotPicker，合规/prompt 优化不触发；⑨ 音频模式 → 音色下拉显示 MiniMax 17 个 + OpenAI 9 个共 26 个选项，分组显示；⑩ `getVoicesByProvider('minimax')` 返回 17 项，`getVoicesByProvider('openai')` 返回 9 项，`findVoice('Wise_Woman')` 返回非 null。

## Phase 2.4 — 音频工具（截取 / 变速）

对齐 LibTV 模块 06+07 音频工具部分。导演台 3D 构图（three.js / @react-three/fiber）为 P2 最低优先级，本轮未实现，后续视用户需求再补。

### 音频工具服务（services/audioTools.ts — 新建）
- **`trimAudio(blob, startSec, endSec, name)`**：用 ffmpeg.wasm `-ss` + `-to` + `-c copy` 流复制截取音频片段，不重编码，极快；返回 `AudioTrimResult { blob, durationSec }`，输出格式延续源文件 MIME（默认 `audio/mpeg`）。
- **`changeAudioSpeed(blob, speed, name)`**：用 ffmpeg `atempo` 滤镜变速不变调。`buildAtempoChain(speed)` 将超出单级范围（0.5~2.0）的速度链式拆解（如 4.0x = `atempo=2.0,atempo=2.0`；0.25x = `atempo=0.5,atempo=0.5`），输出 `audio/mp3`。
- **`getAudioDuration(blob)`**：`-i` 解析 stdout 中 `Duration: hh:mm:ss.xx` 行，返回秒数（number）。
- **复用 `ffmpegClient.ts`**：与视频工具共享 `getFFmpeg()` 单例 + COOP/COEP 配置 + 懒加载 core。

### 音频工具对话框（components/workflow/WorkflowAudioToolDialogs.tsx — 新建）
- **`WorkflowAudioToolKind`**：`trim` | `speed`（2 种）。
- **`AudioTrimDialog`**：双 `Slider` 选择起止时间（0~duration），实时显示 `MM:SS` 标签 + `<audio>` 预览源音频，`FFmpegStatus` 显示加载/进度/错误（复用视频工具的 FFmpegStatus 组件）。
- **`AudioSpeedDialog`**：`Slider`（0.25~4.0，步进 0.05）+ 实时显示 `x` 倍速 + atempo 链式提示（如 `2.0 → 2.0` 表示两级串联）+ `<audio>` 预览源音频。
- **`WorkflowAudioToolState`**：`{ kind, nodeId }`；**`WorkflowAudioToolConfirmation`**：trim 为 `{ kind:'trim', startSec, endSec }`，speed 为 `{ kind:'speed', speed }`。

### WorkflowNodeToolbar 扩展
- **新增 `WorkflowAudioToolHandlers` 接口**：`trim(id)` / `speed(id)` 两个方法。
- **新增 2 个工具按钮**（lucide 图标）：`Scissors`（音频截取）+ `Gauge`（音频变速），仅在 `node.type === 'audio'` 且 `mediaUrl` 存在时显示。
- **`audioTools` / `audioToolBusy` props 新增**，传递给工具栏。

### InfiniteWorkflow 集成
- **新增 state**：`audioTool` / `audioToolBusy` / `audioToolError`；`audioToolBusyRef` + `audioToolTransactionRef` 防止重复触发（与 videoTool 同款事务机制）。
- **`openAudioTool` / `closeAudioTool` / `confirmAudioTool`**：打开/关闭/确认音频工具对话框。
- **`builtInAudioTools` useMemo**：构造 `WorkflowAudioToolHandlers` 实现，绑定当前 state。
- **`confirmAudioTool` 行为**：
  - `trim`：调 `trimAudio` → 用结果创建新音频节点（位于源节点右侧偏移 +40/-40，连接 source → new），节点标题 `trim-{源名}`。
  - `speed`：调 `changeAudioSpeed` → 用结果创建新音频节点（mimeType `audio/mp3`，位于源节点右侧），节点标题 `speed-{speed}x-{源名}`。
- **`audioTools` / `audioToolBusy` props 传给 `WorkflowNodeToolbar`** + **`WorkflowAudioToolDialogs` 渲染**在 Stage 底部（与 `WorkflowVideoToolDialogs` 同级）。
- **新增 imports**：`trimAudio` / `changeAudioSpeed` from `audioTools` + `WorkflowAudioToolDialogs` + `WorkflowAudioToolHandlers` 类型。

### CSS 样式
- 音频工具对话框复用现有 `.workflow-image-tool__*` 样式骨架，无新增 CSS。

### 待用户验证
- ① `npm run dev` 编译无报错；② 选中 1 个音频节点 → 工具栏出现"音频截取"（Scissors）和"音频变速"（Gauge）按钮；③ 点击"音频截取" → 打开 AudioTrimDialog → 首次加载 ffmpeg.wasm（若此前视频工具已加载则复用单例）→ 拖动双 Slider 选区间 → `<audio>` 预览正常 → 确认后画布出现新音频节点（截取片段，连接源节点）；④ 点击"音频变速" → 打开 AudioSpeedDialog → Slider 0.25~4.0 → 选 2.0x → 确认后画布出现新音频节点（2 倍速，连接源节点）；⑤ 选 4.0x 时 atempo 链式提示显示 `2.0 → 2.0`，变速后音调不变；⑥ 选 0.25x 时 atempo 链式提示显示 `0.5 → 0.5`，变速后音调不变；⑦ 工具进行中（busy）时关闭按钮禁用，避免事务中断；⑧ 同时打开视频工具和音频工具时，第二个工具打开会提示"请先完成或关闭当前音频/视频工具"；⑨ 非音频节点（image/video/text）不显示音频工具按钮；⑩ 音频节点无 mediaUrl 时不显示音频工具按钮。

## Phase 3 — Agent Skill 双入口（操作模式）

对齐 LibTV 双入口哲学：外部 Agent（Claude Code / Codex CLI / OpenClaw）通过标准 MCP stdio 协议直接操作 Flovart 画布。不建 Go 后端创作 Agent FSM（"后端不碰 LLM"已拍板），改为纯前端工具层。

### MCP Server（tools/flovart/mcp-server.js — 新建）
- **技术栈**：`@modelcontextprotocol/sdk` v1.29.0 `McpServer` + `StdioServerTransport` + `zod` v4.0.17。
- **自动注册**：遍历 `COMMAND_REGISTRY` 全部 50+ 命令，每个命令自动转为 MCP tool。tool name 将命令名中的 `.` 和 `-` 替换为 `_`（如 `canvas.inspect` → `canvas_inspect`，`element.update-prompt` → `element_update_prompt`）。
- **类型映射**：`argTypeToZod()` 将 COMMAND_REGISTRY 的类型标注（`string`/`number?`/`boolean`/`object`/`array`/`string[]`/`key=value[]`/`image|video|all?` 枚举等）自动转为 Zod schema。
- **命令路由**：与 `cli.js` 完全一致——LOCAL_COMMANDS 调 `executeFlovartCommand(command, args, {})`；BROWSER_COMMANDS 调 `enqueueAndWait`/`enqueueCommand`（默认 `wait:true`，`timeoutMs:60000`）；其余调 `executeFlovartCommand(command, args, createShadowRuntimeFacade())`。
- **Browser 命令扩展参数**：`generate.*`、`element.ignite`、`workflow.node.run/stop` 自动添加可选 `wait`（boolean，默认 true）和 `timeoutMs`（number，默认 60000）参数。超时后返回 pending 结果，Agent 可用 `video.status` 或 `element.watch` 轮询。
- **参数转换**：`toCliArgs()` 将 camelCase 同时生成 kebab-case 键（如 `aspectRatio` → `aspect-ratio`），兼容 `executeFlovartCommand` 内部两种读取方式。
- **错误处理**：`executeFlovartCommand` 返回 `{ ok: false }` 时，MCP response 标记 `isError: true`；异常被 catch 后返回 error message 作为 text content。

### CLI 包升级（tools/flovart/package.json）
- **版本**：0.2.0 → 0.3.0。
- **新增 bin**：`flovart-mcp` → `./mcp-server.js`（与 `flovart`/`flovart-cli` 并列）。
- **新增 dependencies**：`@modelcontextprotocol/sdk: ^1.29.0` + `zod: ^4.0.17`。
- **files 字段**：新增 `mcp-server.js`。

### SKILL.md 更新（.agents/skills/flovart/SKILL.md）
- **YAML frontmatter**：新增 `metadata.openclaw`（category: creative, tags: [canvas, image-generation, video-generation, workflow, mcp, agent], entrypoints: [cli, mcp]）。
- **新增 MCP Server 章节**：说明启动方式（`npm run flovart:mcp` 或 `node tools/flovart/mcp-server.js`）、Claude Code 配置（`.mcp.json`）、Codex/OpenClaw 子进程集成、tool name 命名规则、browser 命令 `wait`/`timeoutMs` 参数。

### Claude Code 集成（.mcp.json — 新建）
- 项目根目录新建 `.mcp.json`，配置 `flovart` MCP server 指向 `node tools/flovart/mcp-server.js`。Claude Code 自动读取项目级 `.mcp.json` 注册 MCP server。

### Root package.json
- 新增 `flovart:mcp` script：`node tools/flovart/mcp-server.js`。

### 待用户验证
- ① `npm run flovart:mcp` 能启动 MCP server（stdio 模式，无报错）；② Claude Code 打开项目后自动识别 `.mcp.json` 并注册 `flovart` MCP server，工具列表显示 50+ 个 `flovart_*` tool；③ 调用 `flovart_status` 返回运行时状态 JSON；④ 调用 `flovart_canvas_inspect` 返回画布检视结果；⑤ 调用 `flovart_generate_image`（prompt + aspectRatio）在浏览器打开时触发生成并返回结果或 pending；⑥ `flovart_command_list` 返回完整命令注册表；⑦ `flovart_doctor` 诊断本地文件桥接设置无报错；⑧ OpenClaw `openclaw skills install @flovart/agent-skill` 能识别 SKILL.md frontmatter（待 ClawHub 发布后验证）；⑨ Codex CLI 通过 `codex app-server --stdio` 子进程模式能发现并调用 flovart MCP tools。

## Canvas 持久化 Bug 修复（Canvas ↔ Workflow 往返丢数据）

- **根因 #1 — `isSyncingRef` 异步时序**：`FlovartExcalidrawStage.tsx` 中 `pushFlovartToExcalidraw` 的 `finally` 块清 `isSyncingRef.current = false`，但 `updateScene` 是异步生效，Excalidraw 会在 flag 清除后再次触发 `onChange` → 重入。修复：不在 `finally` 中清，改在 `handleSceneChange` 中通过 `getSceneElements()` 的 ID 与 `lastExSnapshotRef.current` 比对确认同步完成后才清。
- **根因 #2 — `lastFlovartRef` 未更新**：`pushFlovartToExcalidraw` 在 booted 跳过 push 时也未更新 `lastFlovartRef.current`，导致下次 `handleSceneChange` diff 永远判定为"远端变更" → 死循环。修复：始终更新 ref，只在 booted 时跳过 `updateScene` push。
- **根因 #3 — `initialData` 覆盖 `updateScene`**：React 19 + StrictMode 下 `ExcalidrawInner` 的 `initialData` prop 在 `updateScene` 调用之后又被 React 重新应用，覆盖刚 push 的元素。修复：用 `setTimeout(0)` 延迟 push 到下一个事件循环，避开 React 重渲染窗口。
- **验证**：通过 Playwright 自动化在 Canvas 创建 2 个元素 → 切到 Workflow → 切回 Canvas，确认 2 items 完整保留、图层列表正确、Excalidraw scene 不被重置。
- **Debug trace 清理**：`[TRACE pushFlovart]` / `[TRACE sceneChange]` / `[TRACE guard]` / `[TRACE diff]` 四处 `console.warn` 已从 `FlovartExcalidrawStage.tsx` 移除。

## Canvas 图片不显示 Bug 修复（Excalidraw 0.18.1 图片渲染机制）

- **Excalidraw 0.18.1 图片渲染链路分析**（源码定位）：
  - 图片通过 **canvas 2D context.drawImage()** 渲染，不是 DOM `<img>` 标签；`.SVGLayer` 为空 `<svg></svg>` 是 0.18.x 正常行为。
  - 渲染链：`addFiles()` → `addMissingFiles()` → `addNewImagesToImageCache()` → `updateImageCache()` → `loadHTMLImageElement(dataURL)` → `imageCache.set(fileId, {image, mimeType})` → `scene.triggerUpdate()` → 重新渲染 → `drawElementOnCanvas` 从 `renderConfig.imageCache.get(fileId).image` 取 HTMLImageElement → `context.drawImage()`。
  - **关键发现**：`updateScene()` **不触发** `addNewImagesToImageCache()` — 必须通过 `addFiles()` 或 action 路径。`refresh()` 会强制 canvas 重绘但不重新加载 image cache。
- **根因 #1 — `baseFields` width/height 回退错误**：`utils/excalidrawAdapter.ts:117` 用 `overrides.width ?? 0`，但 Flovart ImageElement 的 `width` 字段在旧路径中可能未设置，应从 `boundsOf(el).width` 取真实宽高。修复：`baseFields` 改为 `overrides.width ?? bw ?? 0` / `overrides.height ?? bh ?? 0`（bw/bh 来自 `boundsOf(el)`）。此修复同时影响 `convertImage`/`convertVideo`/`convertShape`/`convertText`。
- **根因 #2 — `pushFlovartToExcalidraw` 调用顺序错误**：原顺序是 `registerImageFiles`（→ `addFiles` → `addNewImagesToImageCache`）在 `updateScene` 之前调用，但 `addNewImagesToImageCache` 扫描 scene 时图片元素还没进去 → imageCache 为空。修复：重排顺序为 **先 `updateScene`**（元素进 scene）→ **后 `registerImageFiles`**（`addFiles` → `addNewImagesToImageCache` 能找到元素）→ **`setTimeout(refresh, 120)`** 强制重绘。
- **验证**：通过 Playwright 自动化在 Canvas 创建红色 200×200 图片元素 → 像素采样确认 144 个红色像素 (255,0,0) 在正确坐标渲染，w:200 h:200 ✅。

## Workflow CreateMenu 手绘涂鸦风重构

- **设计方向**：联网搜索 2026 手绘 UI 趋势，确认 Excalidraw 官方设计系统（indigo #6965db + Excalifont + 8px radius）+ TypeUI Hand Drawn skill + Rough.js 模式为当前主流。本项目采用 Excalidraw 原生风 + 2×3 网格卡片 + Caveat 手写字体 + CSS 模拟手绘边框（不引入 rough.js）。
- **Caveat 字体引入**：`index.html` 添加 Google Fonts preconnect + Caveat wght 500/600/700。
- **CreateMenu 重构**（`components/workflow/WorkflowCreateMenu.tsx`）：
  - 从垂直列表（280×428px）改为 2×3 网格（360×388px）。
  - Caveat 手写字体用于 header + card title。
  - 不规则 border-radius 变化（每张卡片通过 `:nth-child` 选择器设置不同圆角组合）模拟手绘感。
  - hover：translateY(-2px) + 轻微旋转（±0.6deg）+ accent border + tint。
  - 键盘导航适配 2D 网格（ArrowLeft/Right/Up/Down，Down/Up 移动 2 格=列数）。
- **CSS 新增**（`styles/workflow.css`）：`.workflow-create-menu*` 系列类（`.workflow-create-menu` 360px 宽不规则 border-radius + `.workflow-create-menu__grid` 2 列 grid gap 8px + `.workflow-create-menu__card` 不规则圆角变化 + `.workflow-create-menu__card-icon` 36px 圆形 accent 色 + `.workflow-create-menu__card-title` Caveat 700 16px）。
- **验证**：浅色/深色双模式视觉验证通过；节点创建（click + Enter）验证通过；键盘导航 2D 网格验证通过。

### 待用户验证
- ① Canvas → Workflow → Canvas 往返后画布元素完整保留（不丢数据）；② Canvas 中创建的图片元素正确显示（非空白/非占位）；③ `index.html` 加载 Caveat 字体成功（Network 面板无失败）；④ Workflow 双击空白或从连线拖出 CreateMenu 显示 2×3 网格手绘风卡片；⑤ CreateMenu 卡片 hover 有轻微上抬 + 旋转 + accent 描边效果；⑥ CreateMenu 键盘箭头键导航 2D 网格正确（Down/Up 跨列移动）；⑦ 深色主题下 CreateMenu 样式正常；⑧ Console 无 `[TRACE *]` debug 日志输出。

## Canvas 视频上传 Bug 修复（Excalidraw 拦截 + MIME 空值）

- **根因 — Excalidraw 捕获阶段拦截**：Excalidraw 内部 `onDrop` 会尝试把拖入的文件解析为场景 JSON，视频文件解析失败时抛 `couldNotLoadInvalidFile`（"Couldn't load invalid file"）。原 `AppShell` 的 `onDrop` 处于冒泡阶段，Excalidraw 的 drop handler 先执行 → 视频被吞掉并报错。
- **根因 — Windows Chrome MIME 空值**：Windows Chrome 对 `.mov`/`.mkv` 等格式返回空 `file.type`，原 `file.type.startsWith('video/')` 判断失效 → 视频被误判为非媒体文件。
- **修复 #1 — 捕获阶段拦截**（`components/AppShell.tsx`）：新增 `onDragOverCapture` + `onDropCapture` props 并挂到根 div。`App.tsx` 新增 `handleDragOverCapture`（检查 `dataTransfer.items` 含 file kind 时 `preventDefault` + `dropEffect:'copy'`）和 `handleDropCapture`（遍历 `dataTransfer.files` 调 `handleAddMediaElement`，最后 `stopPropagation()` 阻断 Excalidraw）；在 `AppShell` 使用处（~3024 行）接入。原冒泡阶段 `handleDrop` 保留作为 text/asset 路径 fallback。
- **修复 #2 — 扩展名 MIME 回退**（`utils/fileUtils.ts` — 新增）：`MIME_BY_EXTENSION` 覆盖 png/jpg/jpeg/webp/gif/avif/bmp/svg/mp4/m4v/mov/webm/ogv/mkv/avi/mp3/wav/m4a/aac/flac/ogg/opus；`detectMediaType(file)` 返回 `'image'|'video'|'audio'|null`（优先 `file.type`，空值回退扩展名）；`resolveMimeType(file)` 返回 MIME 字符串。
- **修复 #3 — 路由统一**（`App.tsx`）：`handleAddMediaElement`（~1266）改用 `detectMediaType` 分发 → video 调 `handleAddVideoElement`，audio 提示"音频请通过 PromptBar 上传"，image 调 `handleAddImageElement`；`handleAddVideoElement`（~1230）用 `resolveMimeType` 写 `mimeType` 字段；`handleAddImageElement`（~1179）用 `detectMediaType` 做守卫。错误文案统一中文。
- **Workflow 拖拽确认正常**：`InfiniteWorkflow.tsx` 根 div（1490-1495）已有捕获阶段 handler，`dropMedia`（863）处理 `workflowDropSharedMedia`（Studio 媒体，`STUDIO_MEDIA_DRAG_TYPE`）和 `workflowDropFiles`（OS 文件），`workflowMediaMimeType`（media.ts:74）已含扩展名回退。本次未改动 Workflow 拖拽链路。

### 待用户验证
- ① 从 OS 拖 `.mp4` 文件到 Canvas → 出现视频节点，不报 "Couldn't load invalid file"；② 从 OS 拖 `.mov`/`.mkv` 文件到 Canvas → 正确识别为视频（Windows Chrome 空 MIME 回退扩展名）；③ 从 OS 拖图片到 Canvas → 出现图片节点；④ 从 OS 拖 `.mp3` 文件到 Canvas → 提示"音频请通过 PromptBar 上传"，不创建节点；⑤ 从 Studio 媒体浏览器拖媒体到 Workflow → 正常创建节点；⑥ 从 OS 拖视频/图片到 Workflow → 正常创建节点；⑦ Toolbar 上传按钮（accept `image/*,video/*`）选择视频 → 正确创建视频节点；⑧ Canvas 刷新/切板后视频节点仍在（持久化已由 boardPersistence.ts IDB 落盘）。

## Workflow 节点上传交互升级（双击上传 / 拖拽替换 / 上传动画 / 预览放大）

- **双击上传**（`WorkflowNode.tsx:82-85`）：空媒体节点双击 → 打开文件选择器；已有媒体节点双击 → 弹出 Lightbox 预览放大；文本/脚本/配置节点保留原双击行为（脚本双击打开编辑器）。
- **拖拽替换**（`WorkflowNode.tsx:87-89`）：文件拖到媒体节点上 → 直接替换该节点媒体（无需确认弹窗）；拖拽时节点显示 accent 色描边高亮（`.is-drop-target`）；`onDragLeave` 用 `relatedTarget.contains` 检查避免子元素穿越闪烁。文件拖到非媒体节点或画布空白 → 沿用原 `dropMedia` 创建新节点逻辑。
- **根 dropMedia 早期返回**（`InfiniteWorkflow.tsx:867-870`）：根 `onDropCapture` 检测到 file 拖拽落在 `.workflow-node--image/video/audio` 上时直接 return（不 stopPropagation），让节点冒泡阶段 `onDrop` 接管替换；非媒体节点不拦截 → fall through 到创建新节点。
- **上传加载动画**（`WorkflowNode.tsx:143-157`）：复用生成中节点同套视觉（`flv-generation-glass` 蓝灰紫毛玻璃 + 青绿描边 + shimmer 扫光 + opacity 呼吸），状态标签显示 `上传中 · X MB`（总字节数来自 `file.size`，不造假进度条）；节点体 `scale` 呼吸在 `uploading` 时也触发（`isLoading = status === 'loading' || uploading`）。
- **上传状态模型**（`types.ts:111-112`）：新增 `metadata.uploading?: boolean` + `metadata.uploadBytes?: number`，独立于 `status:'loading'`（生成专用），互不影响。
- **replaceMedia 上传态置位**（`InfiniteWorkflow.tsx:1234-1236`）：`ingestWorkflowMedia` 调用前 `commitFrame` 设 `uploading:true, uploadBytes:file.size`；成功后 metadata 合并时 `uploading:false`（1253）；catch 分支也 `commitFrame` 清 `uploading:false`（1263-1265）。
- **预览 Lightbox**（`components/workflow/MediaPreviewModal.tsx` — 新增）：`position:fixed` 全屏遮罩 + 居中 `img`/`video`/`audio`，ESC 键 / 点遮罩关闭，`AnimatePresence` + `motion` 淡入淡出；复用 `useWorkflowMediaUrl` 解析 media URL。
- **CSS 新增**（`styles/workflow.css`）：`.workflow-node.is-drop-target`（accent 描边 + 3px glow）；`.workflow-preview-modal`（z-index 200 全屏遮罩 blur 8px）；`.workflow-preview-modal__media img/video`（max 92vw/92vh object-fit contain）。

### 待用户验证
- ① Workflow 空图片节点双击 → 弹出文件选择器，选择图片后节点显示该图片；② Workflow 已有图片节点双击 → 弹出 Lightbox 放大预览，ESC/点遮罩关闭；③ Workflow 已有视频节点双击 → Lightbox 内视频可播放；④ 从 OS 拖图片到已有图片节点 → 节点高亮（accent 描边）→ 松手后替换为新图片；⑤ 从 OS 拖视频到已有视频节点 → 替换为新视频；⑥ 从 OS 拖文件到非媒体节点（text/config）→ 不替换，而是创建新节点；⑦ 上传大视频时节点显示"上传中 · X MB"毛玻璃动画（蓝灰紫 + 青绿描边 + 扫光）；⑧ 上传完成后动画消失，节点显示新媒体；⑨ 上传失败时动画消失并显示错误提示。

## Landing Page（ToC 社区版 + ToB 企业版）

### 路由重构（RouterHost.tsx）
- `/` → `ToCLanding`（lazy + Suspense），`/business` → `ToBLanding`（lazy + Suspense），`/app` → `App`（原 `/` 主应用移至此路径），`/enterprise/*` → `EnterpriseApp`（不变），`*` → `Navigate to="/"`。
- ToCLanding / ToBLanding 均为 `React.lazy` + `Suspense` 包裹，fallback 显示"加载中..."。

### ToC Landing — `components/landing/ToCLanding.tsx`（社区类，`/`）
- **整体风格**：暗色背景 `#0a0a0a` + 手绘装饰元素（SVG 手绘下划线 `HandDrawnUnderline`、手绘箭头 `HandDrawnArrow`、手绘星标 `SketchStar`）+ 噪点纹理 + radial-gradient 光晕。
- **Header**：fixed 毛玻璃导航——Flovart logo + 跳转锚点（功能/模板/展示/企业版/GitHub）+ "开始创作" CTA（teal `#19c8b9` + glow）→ `/app`。
- **Hero**：badge（"开源免费 · 本地优先 · 多模型融合"）+ 大标题"用画布造梦"（"造梦"手绘下划线）+ 副标题 + 双 CTA（"开始创作" → `/app`，"探索模板" → `#templates`）+ `AgentDemoAnimation` 脚本动画演示（5 场景循环：input 打字机 → thinking 步骤动画 → generating 渐变卡片 → touchEdit 弹窗 → done CTA，Framer Motion spring 物理，替换原"视频即将上线"占位符）。
- **Features**（`#features`）：4 张编号卡片 01-04——画布+工作流融合 / 多模型一站式 / 原生中文创作 / 本地优先·开源免费。每张含图标 + 编号 + 标题 + 描述，spring 入场动画。
- **Template Gallery**（`#templates`）：6 分类 tab（全部/人物/风景/产品/动漫/视频）+ 8 模板卡片网格（3:4 竖图，渐变占位背景 + badge 热门/视频 + 模型名 + "一键使用" → `/app?templateId=xxx`）。`motion.div layout` 切换分类有动画。
- **Showcase**（`#showcase`）：8 作品 masonry 瀑布流（columns 2/3/4 响应式，交替 3:4 和 1:1 比例），hover 显示标题/作者/"创作同款"，右上角常显类型图标。
- **Bottom CTA**：radial 光晕 + 星标 + "开启你的创作之旅" + 大按钮 → `/app`。
- **Footer**：Flovart logo + GitHub / 企业版 / 文档链接 + MIT 版权。

### ToB Landing — `components/landing/ToBLanding.tsx`（SaaS 类，`/business`）
- **整体风格**：同暗色 `#0a0a0a`，企业级定位。
- **Header**：Building2 图标 + "企业版"标签 + 导航（功能/模型/对比/定价/FAQ/社区版）+ "免费开始" CTA。
- **Hero**：badge（"开源 · 自部署 · 企业级 AI 创作平台"）+ 标题"企业级 AI 创作画布 / 私有部署 · 数据自主" + 双 CTA + 4 信任指标（MIT 开源 / 数据本地化 / 20+ 模型 / 私有化部署）。
- **Features**（`#features`）：4 卡片——开源可自部署 / 画布+工作流融合 / 原生中文创作 / 本地优先·隐私安全。
- **Model Ecosystem**（`#models`）：8 模型卡片网格——Seedance 2.0 / Kling 2.0 / Seedream 4.5 / Flux Pro / Nano Banana / RunningHub / OpenAI 兼容 / Qwen Image，每张含供应商 + 类型。
- **Comparison**（`#comparison`）：8 行对比表——开源协议 / 私有化部署 / 画布+工作流 / 本地数据存储 / 中文原生 / 多模型聚合 / API Key 自管 / 按量付费。Flovart 列 teal 高亮，✓/—/文字三种状态。
- **Pricing**（`#pricing`）：2 方案——社区版（免费，5 项功能）vs 企业版（联系我们，7 项功能含 SSO/SLA，"推荐"badge + teal 描边）。
- **FAQ**（`#faq`）：5 问答——是否免费 / 数据存储 / 支持模型 / 企业版区别 / 商业化使用。
- **CTA**："让 AI 创作能力成为企业基础设施" + "免费开始" + "联系销售"（mailto）。
- **Footer**：企业版 logo + 社区版 / GitHub / 联系我们 + MIT 版权。

### 模板一键复用（`components/landing/templates.ts` + App.tsx）
- **`templates.ts`**：导出 `LANDING_TEMPLATES`（8 模板：portrait-cinematic / landscape-fantasy / product-minimal / anime-character / video-cinematic / video-product-ad / portrait-ink / landscape-ink）+ `SHOWCASE_WORKS`（8 作品）+ `TEMPLATE_CATEGORIES`（6 分类）。每个模板含 id/title/category/prompt/model/type/gradient/badge。
- **App.tsx 模板加载**：启动后 `dataReady` 时解析 `window.location.hash` 中的 `?templateId=xxx`，匹配 `LANDING_TEMPLATES` 后 `setPrompt(tpl.prompt)` + `setModelPreference`（image/video 对应切换）+ toast `已加载模板：xxx`。`templateAppliedRef` 防止重复执行。

### 社区版 P1-P2：用户系统 + 提示词社区（`stores/useAuthStore.ts` + `components/auth/AuthModal.tsx` + `hooks/useAuth.ts` + `components/community/`）

- **P1 用户系统**：
- `useAuthStore`（Zustand）：`login`/`register`/`logout`/`init` 方法，token 只保留在当前浏览器会话（`sessionStorage['flovart.hub.token']`），`init` 通过 `authApi.me()` 恢复登录态。
  - `AuthModal`：登录/注册弹窗组件，模式下可切换，字段校验 + loading + 成功/失败 toast。兼容 Ant Design Modal/Input/Button。
  - `useAuth` hook：封装 `useAuthStore` + `authOpen` 状态 + `requireAuth` 方法（需登录才能操作时自动弹出 AuthModal）。
  - 后端 `Me` 接口已升级：返回 `{ userId, username, email, role }` 而非仅 `userId`，确保页面刷新后用户名正确恢复。
  - `StudioTopMenu`：右上角新增"用户"图标，未登录点击弹出 AuthModal，已登录显示用户名。
  - ToC Landing Header：新增"提示词社区"链接 + "登录"入口（已登录显示用户名）。
- **P2 提示词社区**：
  - `PromptsPage`（`/prompts`）：公共提示词浏览页。顶部搜索栏（关键词） + 类型筛选（全部/图片/视频/文本） + 排序（最新/最热/下载最多） + 卡片网格（标题/标签/作者/赞/下载/条目数） + hover 操作（点赞/下载） + 分页。
  - `PromptDetailDrawer`：点击卡片弹出抽屉，展示标签/描述/作者 + 条目列表（名称 + prompt 内容 + 单条复制按钮） + 点赞/下载全部。
  - `PromptUploadModal`：创建提示词包弹窗，字段包括标题/slug/类型/描述/标签（最多5个）/多条目增删。需登录才能上传，未登录点击"分享提示词"弹出 AuthModal。
  - 路由：`/prompts` 已加入 RouterHost，lazy 加载 + Suspense。
- **待用户验证**：
  - ① 访问 `/#/` ToC Landing → Header 右侧出现"提示词社区"链接和"登录"按钮。
  - ② 点击"登录" → 弹出 AuthModal → 切换到注册模式 → 输入用户名/邮箱/密码 → 注册成功 → Header 显示用户名。
  - ③ 点击"提示词社区" → 跳转 `/#/prompts` → 看到提示词浏览页（无数据时显示空状态引导上传）。
  - ④ 搜索关键词 + 切换类型 + 切换排序 → 列表刷新。
  - ⑤ 点击"分享提示词"（未登录） → 弹出 AuthModal；登录后 → 弹出 PromptUploadModal。
  - ⑥ 填写标题/slug/类型/描述/标签/条目 → 发布 → 列表刷新出现新提示词包。
  - ⑦ 点击卡片 → 弹出 PromptDetailDrawer → 看到条目列表 → 点击复制按钮 → toast"已复制"。
  - ⑧ 画布内（`/#/app`）右上角出现用户图标 → 点击弹出 AuthModal → 登录后显示用户名。
  - ⑨ 后端 `/api/v1/auth/me` 返回 `{ userId, username, email, role }`，前端刷新页面后用户名正确恢复。

## Tauri 桌面端打包 + CLI 开发环境命令

- **NSIS 安装包配置**：`tauri.conf.json` 的 `bundle.targets` 使用 `["nsis"]`（绕过 WiX `light.exe` 依赖 VBSCRIPT 可选功能导致 MSI 失败的问题）。当前 `0.3.0` 已通过 `npm run tauri:build -- --bundles nsis` 在 Windows 本地生成 `Flovart_0.3.0_x64-setup.exe`（5,230,553 bytes，SHA-256 `B7B274DC186CF70D71EF2F2C0D71AC62A8D61CB653F731A09143FD71F2ECE67A`）；本地覆盖配置关闭更新产物签名，`npm run tauri:build:release` 保留正式发布所需的签名更新包。GitHub tag 的签名产物仍需实测。
- **CLI 新增开发环境命令**（`tools/flovart/cli.js` 扩展，`tools/flovart/dev-commands.js` 新增）：
  - `flovart install`：默认下载与 npm bootstrapper 同版本的 Agent Toolkit manifest，校验字节数与 SHA-256 后解包 Runtime / CLI / Managed Agent；源码贡献者使用 `flovart install --source`安装开发依赖。
  - `flovart start`：默认启动已安装的 Desktop Runtime + Managed Agent；`--no-agent` 只启 Runtime。`--source --all --open` 才会在源码仓库并行启动 Vite / Hub / Enterprise，`--source --docker` 启动 SaaS Compose。
  - `flovart update`：默认下载并切换新 Toolkit 版本；`--source` 模式才执行 `git pull` + 前后端依赖更新。
  - 三个命令在 `cli.js` 顶层拦截，不进入 Workflow 原子命令路由；已删除不存在的 Canvas / Element 命令。
  - `package.json` 的 `files` 数组已加入 `dev-commands.js`，`npm publish` 会包含新文件。
- **启动方式重构**（本轮）：CLI npm 包名统一为 `flovart-cli`，发布后可直接 `npx flovart-cli` 进入 TUI；Toolkit 与源码开发模式通过 `--source` 显式分离；根目录 `启动.bat` 默认打开 TUI，Docker Compose 与 Makefile 统一到 web/hub/enterprise/db 四服务和 11451/11452/11453/5433 端口。
- **待用户验证**：
  - ① 推送 `v0.3.0` tag 后 GitHub Actions 生成 `Flovart_0.3.0_x64-setup.exe` 与带 Ed25519 签名的 updater 产物，双击安装后可正常启动与更新；Windows Authenticode 代码签名证书尚未配置，不能把 updater 签名描述成 EXE 已通过发布者代码签名。
  - ② `npx flovart-cli install` → 下载 `0.3.0` Toolkit → SHA-256 / 字节数校验通过 → launcher 写入 PATH。
  - ③ `npx flovart-cli start` → Desktop Runtime + Managed Agent 并行启动；`--no-agent` 仅启动 Runtime。
  - ④ 源码仓库执行 `npx flovart-cli install --source` 与 `npx flovart-cli start --source --all --open` → Vite + Hub + Enterprise 本地联调。
  - ⑤ `npx flovart-cli update` 会安装新 Toolkit；`npx flovart-cli update --source` 会更新源码仓库与依赖。

## Tauri 自动更新 + GitHub Actions CI

- **签名密钥**：`npm run tauri signer generate -w "C:\Users\ava\.tauri\flovart.key" --password "" --ci` 生成新 Ed25519 密钥对（非模板旧 key）。私钥 `flovart.key` 存本地、永不提交；公钥已嵌入 `tauri.conf.json` 的 `bundle.windows.updater.pubkey`。
- **tauri.conf.json 更新**：pubkey 替换为新公钥；`endpoints` 修正为 `["https://github.com/avabbbb/Flovart/releases/latest/download/latest.json"]`；`createUpdaterArtifacts: true` + `installMode: "passive"` 已启用。
- **前端 updater store**（`stores/useUpdaterStore.ts` 新增）：Zustand store 提供 `checkForUpdates`/`applyUpdate`/`autoCheckOnStartup`。动态 `import('@tauri-apps/plugin-updater')` 避免把 Tauri API 打进 web bundle；模块级 `currentUpdate` ref + `autoChecked` guard 防重；`downloadAndInstall` 事件回调跟踪下载进度。
- **tauri-plugin-process**：为更新后 `relaunch()` 添加。`Cargo.toml` 加 `tauri-plugin-process = "2"`，`lib.rs` 加 `.plugin(tauri_plugin_process::init())`，`capabilities/default.json` 加 `process:default`，`package.json` 加 `@tauri-apps/plugin-process: ^2.4.0`。
- **自动检查**：`RouterHost.tsx` 挂载时 `useEffect` 调用 `autoCheckOnStartup()`（静默检查，模块级防重）。
- **手动检查**：`StudioTopMenu.tsx` 新增 isTauri 检测 + updater 按钮——idle=「检查更新」(RefreshCw)/available=「更新 vX」(Download, 绿色)/checking=spinner/downloading=进度%/up-to-date/error(红色)，busy 时 disabled。
- **GitHub Actions CI**（`.github/workflows/release.yml` 新增）：`v*` tag push 触发，`tauri-action` 矩阵构建 Windows(nsis)/macOS(app,dmg)/Linux(deb,appimage)。每平台先 `cargo build --release --bin flovart-host` 构建 sidecar 并复制到 `binaries/flovart-host-{target-triple}[.exe]`，再 tauri build。`updaterJsonPreferNsis: true` 让 `latest.json` 优先 NSIS。`TAURI_SIGNING_PRIVATE_KEY` / `TAURI_SIGNING_PRIVATE_KEY_PASSWORD` 从 repo secrets 读取。
- **待用户验证**：
  - ① `npm install` 安装新增的 `@tauri-apps/plugin-process`。
  - ② `cargo build`（或 `npm run tauri build`）验证 `tauri-plugin-process` Rust 依赖编译通过。
  - ③ `npm run dev` → Tauri 窗口顶部 StudioTopMenu 出现「检查更新」按钮 → 点击 → 首次返回「已是最新」或报错（无 Release 时正常）。
  - ④ GitHub repo Settings → Secrets → 添加 `TAURI_SIGNING_PRIVATE_KEY`（粘贴 `flovart.key` 私钥文件内容）+ `TAURI_SIGNING_PRIVATE_KEY_PASSWORD`（空字符串）。
  - ⑤ `git tag v0.2.1 && git push origin v0.2.1` → CI 三平台并行构建 → Release 发布 → `latest.json` 生成 → 应用内检查更新可发现新版本并下载安装重启。

## Lovart 启发改进（Landing 演示动画 + Touch Edit + Agent 思考可视化）

参照竞品 Lovart.ai 的 Talk.Tab.Tune 模型与 MCoT 可视化理念，在保持 Flovart 画布优先+本地优先定位的前提下实现三项 UX 改进。

### Landing 页 Agent 脚本动画演示（`components/landing/AgentDemoAnimation.tsx` — 新增）
- **5 场景循环动画**（Framer Motion spring 物理，~12s 循环）：
  1. **input**：模拟用户输入 prompt（打字机效果逐字显示）
  2. **thinking**：Agent 思考步骤动画（3-4 个步骤逐个显示 ✓ 完成标记）
  3. **generating**：渐变卡片网格展示生成中的图片（呼吸动效 + 扫光）
  4. **touchEdit**：模拟 Touch Edit 弹窗（选中区域高亮 + 蒙版绘制 + prompt 修改）
  5. **done**：最终结果展示 + CTA 按钮"开始创作"
- **替换占位符**：ToCLanding Hero 区原"Canvas 操作演示视频即将上线"渐变占位卡片替换为 `<AgentDemoAnimation />`。
- **纯前端实现**：不依赖后端 API Key，全部用 Framer Motion + CSS 动画模拟。

### Touch Edit / 局部重绘（`components/canvas/InpaintDialog.tsx` — 新增）
- **交互流程**：选中图片元素 → ElementToolbar 点击"局部重绘"按钮 → 弹出 InpaintDialog（Ant Design Modal）→ 在 canvas 上手绘白色蒙版（要修改的区域）→ 输入修改 prompt → 确认调用 `editImageWithProvider` with mask。
- **蒙版绘制**：复用 Workflow MaskDialog 的 canvas 手绘模式——白线绘制 → `destination-out` 反转 → 白色=保持/透明=重绘。
- **`handleInpaint`**（`hooks/useGeneration.ts` 新增）：调 `supportsMaskImageEditing()` 检查模型能力 → 调 `editImageWithProvider(element.href, prompt, maskDataUrl)` → 结果替换元素 href。
- **ElementToolbar 集成**：新增 `handleInpaint`/`inpaintElementId`/`setInpaintElementId` props；图片类型工具栏新增"局部重绘"按钮（Brush 图标）；`toolbarScreenWidth` 从 560/612/600 调整为 620/672/660 以容纳新按钮。
- **App.tsx 集成**：新增 `inpaintElementId` state，传 `handleInpaint`/`inpaintElementId`/`setInpaintElementId` 给 ElementToolbar。

### Agent 思考可视化（`components/AgentThinkingPanel.tsx` — 新增，替换 Loader）
- **替换 Loader**：原 `Loader.tsx` 组件（简单 spinner）完全被 `AgentThinkingPanel` 替代，App.tsx 中 `<Loader>` 替换为 `<AgentThinkingPanel>`。
- **步骤累积逻辑**：`progressMessage` 变化时 push 到 `steps` 数组（去重）；`isLoading=false` 后 2s 自动清空；显示最后 5 步。
- **UI 设计**：每步显示 ✓（完成，绿色）/ →（进行中，脉冲动画）；Framer Motion `AnimatePresence` + spring 入场/退场；毛玻璃背景 + 青绿色描边高亮，符合 AGENTS.md Canvas 生成中节点视觉规范。
- **`progressMessage` 来源**：复用 `useGeneration` hook 已有的 progressMessage 状态（Provider 轮询/流式返回的进度文本）。

### Landing 页内导航 Bug 修复（HashRouter anchor 冲突）
- **根因**：ToCLanding / ToBLanding 的导航链接使用 `<a href="#features">` 等锚点。HashRouter 下 URL hash 是路由路径（如 `#/business`），点击 `href="#features"` 会把 hash 覆盖为 `#features` → 路由跳转到不存在的 `/features` → 回退到 `/`（ToC 页面）。导致在 ToB 页面点击"功能""模型"等会跳到 ToC 页面。
- **修复**：两个 Landing 组件新增 `scrollTo(id)` 辅助函数（`document.getElementById(id)?.scrollIntoView({ behavior: 'smooth' })`），所有 `<a href="#xxx">` 替换为 `<a onClick={() => scrollTo('xxx')}>`，不修改 hash，纯页面内滚动。
- **影响范围**：ToCLanding 3 个导航链接 + 1 个 CTA 按钮；ToBLanding 5 个导航链接 + 1 个 CTA 按钮。

### 待用户验证
- ① 访问 `/#/` ToC Landing → Hero 区显示 AgentDemoAnimation 脚本动画（5 场景循环），不再是"视频即将上线"占位符。
- ② 动画循环：input 打字机 → thinking 步骤 → generating 渐变卡片 → touchEdit 弹窗 → done CTA → 循环。
- ③ 在画布选中一张图片元素 → ElementToolbar 出现"局部重绘"按钮（Brush 图标）→ 点击弹出 InpaintDialog。
- ④ InpaintDialog 中用鼠标在图片上绘制白色蒙版区域 → 输入修改 prompt → 确认后图片被局部重绘。
- ⑤ 模型不支持 mask editing 时，`handleInpaint` 会提示错误。
- ⑥ 生成中时画布显示 AgentThinkingPanel（步骤列表 + ✓/→ 状态图标），不再是简单 spinner。
- ⑦ 生成完成后 AgentThinkingPanel 2s 后自动消失。
- ⑧ ToB Landing（`/#/business`）点击导航"功能""模型""对比""定价""FAQ"→ 页面内平滑滚动到对应 section，不跳转到 ToC 页面。
- ⑨ ToC Landing（`/#/`）点击导航"功能""模板""展示"→ 页面内平滑滚动，不跳路由。
- ⑩ ToC Landing Hero 区"探索模板"按钮 → 平滑滚动到模板 section。
- ⑪ ToB Landing Hero 区"查看定价"按钮 → 平滑滚动到定价 section。

## ToB 企业后台后端安全修复

- Enterprise 部门和角色 CRUD 改为按 `deptId` / `roleId` 反查组织后再做 RBAC，部门成员列表/新增/更新/删除不再裸奔；部门成员写入会校验用户存在和角色属于同组织。
- Hub 与 Enterprise JWT 中间件限制 HS256，并检查 token 有效性、`subject` 和 `audience`，避免畸形 token 触发 panic。
- Enterprise API Key 新写入时使用 AES-GCM 密文存储；生产环境应配置独立 `API_KEY_ENCRYPTION_SECRET`，旧明文 Key 需重新保存。
- Enterprise AI proxy 增加 provider/endpoint 白名单，覆盖 OpenAI、Anthropic、RunningHub、Volcengine、OpenAI-compatible 和 custom 的受控路径；成功返回前必须完成扣费。
- 积分扣费和退款对余额与成员额度使用事务行锁；审批流当前节点指针错误和 pricing repository 包名遮蔽编译错误已修复。
- Canvas/Agent 的 `element.ignite` 复用节点级生成链路，视频会沿用节点自己的模型、比例、时长、分辨率、音频、水印和 `@`/附件引用；CLI `generate.video` 新增 `generateAudio` 和 `watermark` 参数。

### 待用户验证

- ① 非管理员/无权限成员访问 `/departments/:deptId/members` 新增/更新/删除应返回 403；有 `member:manage` 权限的成员可正常操作。
- ② `/departments/:deptId` 与 `/roles/:roleId` 更新/删除不再返回“缺少组织 id”。
- ③ 新保存 Enterprise API Key 后数据库 `org_api_keys.api_key` 为 `v1:` 密文，列表接口仍只返回 `keyHint`。
- ④ Enterprise AI proxy 对未知 endpoint 返回错误；RunningHub `/openapi/v2/...` 与 Volcengine `/contents/generations/tasks` 可进入受控转发。
- ⑤ 通过 MCP/CLI 对已有视频节点执行 `element.ignite`，应使用该节点 PromptBar 里设置的模型、时长、分辨率、音频和水印参数。

## ToB 企业后台 — 积分/Key 池/资源/审批/敏感词/项目

### 后端（enterprise 服务，`backend/enterprise/`）

- **11 张新表 AutoMigrate**：`OrgCredit`/`CreditTransaction`/`RechargeRequest`/`UsageRecord`（积分）、`OrgApiKey`/`ModelPricing`/`MemberQuota`（Key 池+定价+额度）、`ResourceLevel`/`Resource`（资源库+密级）、`ApprovalWorkflow`/`ApprovalNode`/`ApprovalRecord`/`ApprovalStep`（审批流）、`SensitiveWord`（敏感词）、`Project`（项目镜像）。`cmd/server/main.go` 现注册全部 15 张表（4 旧 + 11 新）。
- **权限点扩展 11→14**：`model/permissions.go` 新增 `apikey:manage` / `pricing:manage` / `quota:manage`；owner 全权限，admin 除 `org:manage` + `view:audit_log` 外全部。
- **积分链路**：`credit_repository.go` + `credit_service.go` + `credit_handler.go` — 余额查询、流水列表、充值申请（待审批）、用量记录、`ReviewRecharge` 审批（approve=true 事务内入账+写流水，reject 仅改状态）；`ConsumeCredits` 在事务内同时写 `UsageRecord` + `CreditTransaction` + 扣 `OrgCredit.Balance` + 累加 `MemberQuota.UsedThisMonth`；`RefundCredits` 仅退已成功记录。路由 `PUT /orgs/:id/credit/recharges/:rechargeId/review` 由 `credit:adjust` 权限守门。
- **Key 池+定价+额度链路**：`apikey_repository.go` + `apikey_service.go` + `apikey_handler.go` — OrgApiKey 增删改查（`json:"-"` 永不返回明文，响应只返回 `KeyHint` 末 4 位）、ModelPricing 增删改查、MemberQuota 按月配额管理。
- **AI 代理链路**：`proxy_service.go` + `proxy_handler.go` — 预检余额+额度 → 转发 HTTP 到 OpenAI 兼容接口 → 成功扣分 / 失败记录不扣；用 `minInt` 而非内置 `min` 避免 Go 1.21+ 冲突。
- **资源库链路**：`resource_repository.go` + `resource_service.go` + `resource_handler.go` — ResourceLevel 密级管理、Resource 上架/审核/发布流程。
- **审批流链路**：`approval_repository.go` + `approval_service.go` + `approval_handler.go` — ApprovalWorkflow 多节点配置（sequential 全员审 / parallel 全员审 / any 任一人审；reject 直接驳回整条）、ApprovalRecord 提交+审批、ApprovalStep 记录每步。
- **敏感词链路**：`sensitive_repository.go` + `sensitive_service.go` + `sensitive_handler.go` — SensitiveWord 词库 CRUD + 文本检测接口。
- **项目镜像链路**：`project_repository.go` + `project_service.go` + `project_handler.go` — Project 增删改查。
- **路由全部注册**：`cmd/server/main.go` 按 `/api/v1/enterprise/credit` `/apikeys` `/pricing` `/quota` `/ai/proxy` `/resources` `/resource-levels` `/approvals` `/approval-workflows` `/sensitive` `/projects` 前缀分组注册。

### 前端（`services/` + `components/enterprise/`）

- **`services/enterpriseApi.ts`（新增）**：封装 `creditApi` / `apiKeyApi` / `resourceApi` / `approvalApi` / `sensitiveApi` / `projectApi` 全部端点 + TypeScript 类型定义，统一走 `hubClient.ts` 的 `api` 工具（自动带 token + `ENTERPRISE_BASE_URL`）。
- **`services/orgApi.ts` 更新**：`ALL_PERMISSIONS` 从 11→14 个，与后端 `permissions.go` 对齐。
- **`components/enterprise/shared.tsx`（新增）**：`FormInput` / `PanelCard` / `EmptyState` / `PageHeader` / `PanelProps` 共享组件，统一面板视觉风格（`isl-*` CSS 类 + `var(--isl-*)` 主题变量）。
- **`EnterpriseApp.tsx` 重写**：从 3-tab 布局改为侧边栏导航；`Section` 类型覆盖 9 个管理区域（members/depts/roles/credit/apikeys/resources/approval/sensitive/projects），分 7 组；移动端降级为 `<select>` 选择器；删除本地 FormInput 改用 shared 导入；保留 MemberRoster/DeptPanel/RolePanel/AuthPanel/OrgListPanel 在文件内。
- **`panels/credit-panel.tsx`**：余额与流水 / 充值申请 / 用量记录（3 个 sub-tabs）。充值申请 pending 行对有 `credit:adjust` 权限的用户显示 ✓通过 / ✗拒绝 行内按钮（reject 弹 prompt 输入原因），对仅有 `credit:grant` 权限的用户显示取消按钮。流水类型（充值/消耗/退款）和用量状态（成功/失败/已退款）已本地化。
- **`panels/apikey-panel.tsx`**：API Key 池 / 模型定价 / 成员额度（3 个 sub-tabs）。
- **`panels/resource-panel.tsx`**：资源库 / 资源密级（2 个 sub-tabs，含审核+发布流程）。
- **`panels/approval-panel.tsx`**：审批流配置（多节点）/ 审批记录（2 个 sub-tabs）。
- **`panels/sensitive-panel.tsx`**：敏感词库 + 文本检测器。
- **`panels/project-panel.tsx`**：项目镜像列表。

### 待用户验证

- ① 后端编译通过：`cd backend/enterprise && go build ./cmd/server`（需 `GOPROXY=https://goproxy.cn,direct` + `GOSUMDB=off`）。
- ② 启动 enterprise 服务（`:11453`），AutoMigrate 成功创建 11 张新表（PostgreSQL `flovart-pg` 容器 `:5433`）。
- ③ 企业后台（`/#/enterprise`）侧边栏显示 9 个管理区域，分 7 组，移动端降级为 select。
- ④ 积分面板 3 个 sub-tab 切换正常，余额/流水/充值申请/用量记录 UI 渲染；有 `credit:adjust` 权限的用户在 pending 充值申请行可见 ✓通过/✗拒绝 按钮，通过后自动入账并写流水，拒绝可填原因；仅有 `credit:grant` 权限的用户只见取消按钮。流水类型和用量状态显示中文。
- ⑤ API Key 面板 3 个 sub-tab：Key 池列表（只显示末 4 位 KeyHint，不返回明文）、模型定价、成员额度。
- ⑥ 资源面板 2 个 sub-tab：资源库 + 密级，含审核+发布流程按钮。
- ⑦ 审批面板 2 个 sub-tab：审批流配置（可添加多节点，选 sequential/parallel/any）+ 审批记录。
- ⑧ 敏感词面板：词库 CRUD + 文本检测器输入文本后返回命中词。
- ⑨ 项目面板：项目镜像列表渲染。
- ⑩ 权限矩阵：owner 可见全部 9 区域，admin 不可见"组织设置"中 owner 转让 + 审计日志。
- ⑪ AI 代理 `POST /api/v1/enterprise/ai/proxy` 转发请求，成功扣分、失败不扣；额度超限或余额不足时预检拦截。

## ToB 企业后台 6 面板审计与补全

### 权限门修复（4 处前端权限与后端不匹配）

- **sensitive-panel**：`canManage` 从 `org:manage` 改为 `asset:approve`，与后端 `POST/DELETE /sensitive-words` 守门一致；此前 Admin 角色无 `org:manage` 导致被锁死。
- **resource-panel LevelsTab**：`canManage` 从 `canPublish`（`asset:publish`）改为 `canApprove`（`asset:approve`），与后端 `POST/DELETE /resource-levels` 守门一致。
- **approval-panel**：`canManage` 从 `workflow:publish` 改为 `asset:approve`，与后端 `POST/DELETE /approval-workflows` 守门一致。
- **apikey-panel QuotaTab**：仅 `canManageQuota`（`quota:manage`）时才显示"成员额度"tab；此前所有用户可见 tab 但后端 `GET /quotas` 要求 `quota:manage`，无权限用户点击后 403。

### 功能补全（4 个已定义但未使用的 API 端点接入 UI）

- **approval-panel 审批流详情展开**：点击审批流行首的展开箭头调用 `getWorkflow` 显示节点列表（序号 + 节点类型 + 审批人类型 + ID 列表）。
- **approval-panel 审批记录详情展开**：点击审批记录行首的展开箭头调用 `getRecord` 显示步骤列表（序号 + 动作 + 审批人 + 备注 + 时间）。
- **approval-panel 提交审批**：审批记录 tab 右上新增"提交审批"按钮，表单含 `targetType`（resource/project/credit_recharge）+ `targetId`，调用 `approvalApi.submit`。
- **resource-panel 添加资源**：资源库 tab 右上新增"添加资源"按钮，表单含 type（link/image/video/audio/document）+ title + href（必填）+ thumbnail + levelId（从密级列表加载），调用 `resourceApi.create`。
- **project-panel 同步项目**：项目列表右上新增"同步项目"按钮，表单含 项目 ID + 标题 + 所有者 ID（预填当前用户）+ 节点数 + 连接数，调用 `projectApi.sync`。

### PanelProps 扩展

- `shared.tsx` 的 `PanelProps` 新增 `userId: string`，`EnterpriseApp.tsx` 构造时传入 `user.id`，供 project-panel 同步表单预填所有者。

### 待用户验证

- ① 用 Admin 角色（无 `org:manage`）登录企业后台，敏感词面板可正常添加/删除词（此前被锁死）。
- ② 资源密级 tab 仅 `asset:approve` 权限可操作（有 `asset:publish` 无 `asset:approve` 的自定义角色不再看到按钮）。
- ③ 审批流 tab 仅 `asset:approve` 权限可创建/删除（`workflow:publish` 不再误开门）。
- ④ 无 `quota:manage` 权限的用户在 API Key 面板看不到"成员额度"tab（此前可见但 403）。
- ⑤ 审批流行点击展开箭头显示节点详情，审批记录行点击展开显示步骤详情。
- ⑥ 审批记录 tab"提交审批"表单提交后生成新 pending 记录。
- ⑦ 资源库 tab"添加资源"表单提交后新资源出现在列表。
- ⑧ 项目列表"同步项目"表单提交后新项目镜像出现在列表。

## Prompt 历史 Ctrl+K 命令面板（Lovart 启发改进 #4）

参照 Lovart.ai 的 Ctrl+K Prompt 历史搜索，实现全局面板，搜索历史提示词并一键插入到当前 PromptBar 或 Workflow 节点。

### 实现

- **`stores/usePromptHistoryStore.ts`（新增）**：Zustand store + localforage `flovart/prompt-history` 持久化。`record()` 去重（同 prompt 更新 timestamp 移到顶部），最多 500 条。`search(query)` 子串匹配返回最多 50 条。`insert(text)` 设置 `pendingInsert { text, nonce }` 并关闭面板。`consumeInsert()` 清除 pendingInsert。
- **`components/PromptHistoryPalette.tsx`（新增）**：Ctrl+K 命令面板 UI。搜索框 + 结果列表 + 键盘导航（↑↓ 导航 / Enter 插入 / ESC 关闭）。每条记录显示 prompt 文本（2 行截断）、来源（画布/工作流）、模式图标（图片/视频/文本）、时间（刚刚/N 分钟前/N 小时前/M/D）。底部清空按钮 + 操作提示。主题适配（light/dark）。
- **Ctrl+K 全局快捷键**（`App.tsx`）：`useEffect` 监听 `(ctrlKey || metaKey) && key === 'k'` → `togglePromptHistory()`。无现有 Ctrl+K 冲突。
- **Prompt 记录**：
  - Canvas 侧（`hooks/useGeneration.ts:handleGenerate`）：生成前调 `usePromptHistoryStore.getState().record({ prompt, mode, source: 'canvas' })`。
  - Workflow 侧（`services/workflowGeneration.ts:runWorkflowGeneration`）：prompt 组装后调 `usePromptHistoryStore.getState().record({ prompt, mode, source: 'workflow' })`。
- **Prompt 插入**：
  - Canvas（`App.tsx`）：`pendingInsert` watcher 设置 `setPrompt(text)` + 清空 `promptDocument` + `mentionedElementIds` → PromptBar useEffect 同步 TipTap editor。
  - Workflow（`InfiniteWorkflow.tsx`）：`pendingInsert` watcher 设置选中节点的 `metadata.prompt` + 清空 `richTextDocument` + `mentionedNodeIds` → WorkflowNodePromptBar 同步。仅当单选 image/video/text 节点时生效。
- **面板渲染**：`<PromptHistoryPalette theme={resolvedTheme} />` 在 workflow 和 canvas 两条返回路径中都渲染。

### 待用户验证

- ① 在画布输入 prompt 生成图片 → prompt 自动记录到历史。
- ② 在 Workflow 节点输入 prompt 生成 → prompt 自动记录到历史（source 标记为"工作流"）。
- ③ 按 Ctrl+K（或 Cmd+K）打开命令面板。
- ④ 面板中搜索关键词 → 实时过滤匹配的历史 prompt。
- ⑤ ↑↓ 键导航 → 高亮项跟随。Enter 插入选中 prompt 到当前 PromptBar（Canvas）或选中节点（Workflow）。
- ⑥ ESC 关闭面板。
- ⑦ Canvas 侧：插入后 TipTap 编辑器显示 prompt 文本，可直接生成。
- ⑧ Workflow 侧：选中一个 image/video/text 节点 → Ctrl+K → 选 prompt → 节点 prompt 更新。
- ⑨ Workflow 侧：未选中节点 → Ctrl+K → 选 prompt → 无效果（静默消费，不报错）。
- ⑩ 点击"清空"按钮 → 二次确认后清空全部历史。
- ⑪ 刷新页面后历史仍在（localforage 持久化生效）。

## Lovart/LibTV 2026 启发改进 — 升级现有功能（6 项）

参照 2026 年 Lovart.ai 与 LibTV 最新功能，对 6 个已有功能进行升级。用户选择"6 项升级一起做"。

### Compare Mode + Batch 升级

**问题**：原有 `batchCount` 1/2/4 选择只影响 PromptBar 按钮文案（"生成 N 版"），但 `onGenerate` 永远调 `handleGenerate()`，不检查 `batchCount`。`handleBatchGenerate`（独立函数）从未被 UI 调用，批量生成实际是坏的。批量结果通过 `batchResults` 模态弹窗展示，用户需点击"选择此方案"或"全部放入画布"。

**升级内容**：
- 移除 `batchResults` state、`handleBatchGenerate`、`handleSelectBatchResult`、`handleSelectAllBatchResults` 函数及 App.tsx 中的批量结果弹窗 JSX。
- 将 batch 逻辑合并进 `handleGenerate` 的两个图片生成分支：
  - **纯文生图分支**（无选中元素 + 无 @引用）：`batchCount > 1` 时并行 N 次 `generateImageWithProvider`（同一 prompt + 同一参考图），结果放画布 2 列网格，每张图垂直居中，分组成组（`批量生成 (N)`），选中所有新元素。
  - **@参考图分支**（有 mentionedImageElements）：同样 `batchCount > 1` 时并行 N 次，使用 `mentionPrompt2` + `allRefs`，结果放画布网格。
- 每个结果保留 `generationState`（prompt/model/provider/referenceIds）。
- 生成进度消息：`正在批量生成 N 张方案...` → `生成完成: M/N 张成功`。
- 失败容错：每个并行请求 `.catch(() => null)`，只放置成功的结果。
- PromptBar 按钮行为不变（`batchCount > 1` 显示"生成 N 版"），但 `onGenerate` 调 `handleGenerate()` 即可自动处理 batch。

**测试要点**：
- ① Canvas 中设置 `batchCount = 2` → 输入 prompt → 点生成 → 画布上出现 2 张图并排排列，均被选中。
- ② 设置 `batchCount = 4` → 生成 → 4 张图 2×2 网格排列。
- ③ @引用一张图 + `batchCount = 2` → 生成 → 2 张图均使用参考图。
- ④ 原 `batchResults` 弹窗不再出现。
- ⑤ 部分请求失败时，只放置成功的图，进度消息显示 `M/N 张成功`。

### 角色三视图升级

**升级内容**：
- SlashMenu `character-3-view` 的 `promptBuilder` 从 `character turnaround, front view, consistent design` 升级为 `character turnaround sheet, front view (T-pose), maintain identical character design, colors, proportions, clothing and accessories, clean concept art reference background`。
- `handleSlashCommand`（InfiniteWorkflow.tsx）创建节点时同时设置 `mentionedNodeIds: sourceIds`（此前只有 `referenceNodeIds`）。workflow generation 同时读取两者，源图片作为 @参考图传入生成 API。

**测试要点**：
- ① Workflow 中选中 1 张角色图 → 角色三视图 → 生成 3 张前/侧/后视图，角色设计保持一致。
- ② 节点元数据含 `mentionedNodeIds`（可通过节点检查器或日志确认）。

### 25/9 宫格升级

**升级内容**：
- `storyboard-25` 的 `promptBuilder` 从统一 `storyboard frame N of 25, sequential narrative beat` 升级为按剧情五幕分配：`opening act`（帧 1-7）/ `development act`（帧 8-14）/ `midpoint turn`（帧 15-21）/ `escalation act`（帧 22-25）/ `climax and resolution`（帧 26-25 降级为通用描述）。
- `story-4-grid` 的 `promptBuilder` 从 `story progression, scene N of 4` 升级为四拍结构：`opening scene, establishing shot` / `rising action, conflict begins` / `climax, dramatic peak moment` / `resolution, aftermath and conclusion`。
- 连接的文本节点内容通过 `upstream.textContents` 自动拼入 prompt（已有机制，无需额外改动）。

**测试要点**：
- ① Workflow 中 `story-4-grid`（无源）→ 生成 4 格，每格对应开场/发展/高潮/结局。
- ② 连接一个文本节点写故事 → `storyboard-25` → 25 帧按五幕分配。
- ③ 文本节点内容出现在生成 prompt 中。

### Multi-Camera Grid 升级

**升级内容**：
- `multi-camera-9` 的 `promptBuilder` 从 `same subject, front view, different camera angle, consistent identity` 升级为 9 个带俯仰角度的具体机位：`front view, eye-level` / `front-left 3/4 view, slight high angle` / `left side profile, eye-level` / `back-left 3/4 view, slight low angle` / `back view, eye-level` / `back-right 3/4 view, slight high angle` / `right side profile, eye-level` / `front-right 3/4 view, slight low angle` / `top-down bird's eye view`。
- 增加约束描述：`keep identical subject identity, pose, clothing, hairstyle and style, do not alter the subject itself`。
- 同样设置 `mentionedNodeIds`。

**测试要点**：
- ① 选中 1 张图 → 多机位九宫格 → 9 张图分别为不同机位角度，主体一致。
- ② 角度描述更具体（含俯仰角度），不再是简单的 `front view`。

### Edit Elements 升级

**升级内容**：
- `handleSplitImageLayers`（useGeneration.ts）的 `hideOriginalAfterSplit` 从 `true` 改为 `false`——原图保持可见。
- 拆分图层不再重叠放在原图位置（`element.x + layer.offsetX`），改为排列在原图右侧水平排列：`layerBaseX = element.x + element.width + 40`，每层垂直居中对齐。
- 分组名从 `Layer Group` 改为 `拆分图层`。
- 便于对比原图和拆分后的图层，独立编辑各层。

**测试要点**：
- ① 选中一张图 → ElementToolbar 点"拆分图层" → 原图保持可见，拆分出的图层排列在右侧。
- ② 各层水平排列，垂直居中对齐。
- ③ 分组名为 `图片名 / 拆分图层`。
- ④ 选中所有新拆分图层。

### Batch 升级（与 Compare Mode 一并完成）

Batch 逻辑已从独立 `handleBatchGenerate` 合并进 `handleGenerate`。PromptBar 的 `batchCount` 选择（1/2/4）现在真正生效——`batchCount > 1` 时自动并行生成 N 张直接放画布网格。不再有弹窗选择步骤。

## 画布视觉打磨

### Canvas 背景点阵网格 + 环境光晕

**变更内容**：
- `excalidraw-canvas.css` 中 `.excalidrawCanvasWrapper / .excalidrawCanvas` 背景从纯色 `var(--isl-surface-2)` 升级为：顶部 mint 环境光晕 `radial-gradient(ellipse 70% 45% at 50% 0%, var(--isl-mint-bg), transparent 60%)` + 22px 间距点阵网格 `radial-gradient(circle, var(--isl-border) 1px, transparent 1.5px)`。
- 点阵颜色跟随主题（light: `#e3e0d8` / dark: `#3a3834`），间距 22px，点径 1px，视觉上类似 Figma/Lovart 的专业画布背景。

### 浮动面板毛玻璃修复 + 暗色适配

**问题**：所有浮动面板（工具栏 / ElementToolbar / 图层面板 / 右键菜单）的 `backdrop-filter: blur()` 完全失效——背景使用不透明 `var(--isl-card)`（纯白/纯黑），backdrop-filter 无法穿透。

**修复**：
- 4 个浮动面板背景从 `var(--isl-card)` 改为 `rgba(255,255,255,0.68)`（light）/ `rgba(32,31,29,0.68)`（dark），`backdrop-filter` 升级为 `blur(24px) saturate(180%)`。
- 边框从 `1.5px` 减至 `1px`，更精致。
- 新增 `inset 0 1px rgba(255,255,255,0.5)` 高光（light）/ `inset 0 1px rgba(255,255,255,0.06)`（dark）模拟玻璃顶部光泽。
- 阴影升级为 `var(--isl-shadow-lg)`（工具栏/右键菜单）或 `var(--isl-shadow)`（ElementToolbar/图层面板）。
- 所有 4 个面板新增 `:root[data-theme='dark']` 暗色覆盖。

### PromptBar 毛玻璃覆盖

- `.excalidraw-stage-bottombar .isl-shell` 和 `.canvas-overlay-fixed .isl-shell` 新增 scoped 覆盖：背景从不透明渐变改为半透明渐变 `rgba(255,255,255,0.72)` + `backdrop-filter: blur(24px) saturate(180%)`，暗色对应 `rgba(32,31,29,0.72)`。
- 不影响其他页面（enterprise / landing）的 `.isl-shell` 默认样式。

### Excalidraw 原生组件毛玻璃

- `.excalidraw .context-menu` 和 `.excalidraw .color-picker-container` 背景从 `var(--isl-card)` 改为半透明 + `backdrop-filter`，暗色对应覆盖。

**测试要点**：
- ① 画布背景出现点阵网格（22px 间距），浅色主题下点为暖灰色，暗色主题下点为深灰色。
- ② 画布顶部有微弱的 mint 色环境光晕，向下渐隐。
- ③ 底部浮动工具栏呈现半透明毛玻璃效果（能透过看到画布内容），暗色主题同样有效。
- ④ ElementToolbar（选中元素时出现的浮动工具栏）同样呈现毛玻璃效果。
- ⑤ 左侧图层面板呈现毛玻璃效果。
- ⑥ 右键菜单呈现毛玻璃效果。
- ⑦ 底部 PromptBar 呈现毛玻璃效果，不影响其他页面的 PromptBar 样式。
- ⑧ 暗色主题下所有面板的毛玻璃和高光效果正常。

### 画布视觉打磨第二轮

**变更内容**：
- `excalidraw-canvas.css` 新增 `.excalidraw-container::after` 伪元素：画布边缘微妙内阴影暗角（`inset box-shadow`），增加画布物理纵深感；暗色主题阴影加深。
- Excalidraw 滚动条细化：宽度 6px→5px，新增 `scrollbar-track: transparent`、`scrollbar-thumb:hover` 状态（悬停变深）、`scrollbar-corner: transparent`。
- `generation.css` 生成中毛玻璃占位边框 1.5px→1px，新增外发光 `0 0 0 1px rgba(25,200,185,.08)`；状态标签微调间距/圆角/阴影/字号。
- `.excalidraw-layer-panel__header` 边框 1.5px→1px，新增 `linear-gradient(180deg, var(--isl-surface), transparent)` 渐变背景。
- `.excalidraw` 新增 Excalidraw 内部主题变量覆盖：`--color-primary`/`--color-primary-hover`/`--color-primary-light`/`--island-bg-color`/`--color-surface-*`/`--color-on-surface`/`--color-muted`/`--color-border-outline*` 全部对齐 Flovart `isl-` 色系；暗色 `--island-bg-color` 覆盖。

**测试要点**：
- ① 画布四边有微弱内阴影暗角（浅色主题极淡，暗色主题稍明显），不影响画布交互。
- ② 画布内滚动条更细（5px），悬停时滚动条颜色加深。
- ③ 生成中节点边框更细（1px），外圈有极淡青绿发光。
- ④ 生成中状态标签视觉更精致（间距、圆角、阴影微调）。
- ⑤ 图层面板标题区有渐变背景过渡。
- ⑥ Excalidraw 原生 UI（颜色选择器等）的主色、背景色、文字色与 Flovart 主题一致，暗色主题下 island 背景为半透明深色。
### Version History 画布版本回滚

**实现要点**：
- `stores/useVersionHistoryStore.ts`：in-memory Zustand store，`CanvasVersion` 接口（id/timestamp/type/description/elements），最多 30 个版本，超出自动淘汰最旧。`addVersion`/`removeVersion`/`clearVersions`/`getVersion`。
- `App.tsx commitAction` 新增可选 `versionMeta: { description, type: VersionType }` 参数；有 meta 时在 `updateActiveBoard` 内部用当前 `newElements` 调 `useVersionHistoryStore.getState().addVersion()`，避免 stale closure。
- `useGeneration.ts` 全部 12 处生成相关 `commitAction` 调用已注入版本元数据（split/inpaint/video/generate 等）；手动 move/resize/delete 不传 meta，不创建版本。
- `useCanvasInteraction.ts` `commitAction` 类型签名同步更新（可选 versionMeta），line 614 手动编辑调用不传 meta。
- `components/VersionHistoryPanel.tsx`：浮动毛玻璃面板（右上，z-40），时间线倒序列表，每项含类型徽章+时间+描述+元素数+恢复/删除按钮，顶部清空/关闭；恢复时 `commitAction(() => version.elements, { description: '恢复版本: ...', type: 'restore' })`，并 toast 提示。
- `components/Toolbar.tsx` 新增 `onHistoryClick?` prop，在 LeftCluster 渲染历史按钮（时钟+环形箭头 SVG），仅在传入时显示。
- App.tsx `isHistoryPanelOpen` state + Toolbar `onHistoryClick` 切换 + overlays 渲染面板。

**测试要点**：
- 1. 主工具栏点击历史按钮，面板应从右上滑出，为空时显示"暂无历史版本"。
- 2. 生成一张图（文生图/批量/@参考图/编辑生成）后，面板应出现一条"生成"记录，含时间、描述、元素数。
- 3. 拆分图层应出现"拆分图层"记录；局部重绘应出现"局部重绘"记录；视频生成应出现"视频生成"记录。
- 4. 手动移动/缩放/删除元素不应产生新的版本记录（仅在生成类操作时记录）。
- 5. 鼠标 hover 列表项，右侧出现"恢复"+"删除"两个按钮。
- 6. 点击"恢复"，画布回滚到该版本元素，并新增一条"恢复版本"记录。
- 7. 点击"删除"移除单条；点击顶部"清空"并确认后清空全部。
- 8. 超过 30 条后，最旧的自动被淘汰。
- 9. 切换深色/浅色主题，面板配色应跟随。

## 画布从 Excalidraw 回退到 5d2fc18 SVG/Konva 版 + Agent Chat 接线

**背景**：Excalidraw 主路径化重构（97d416a 及之后）在画布交互和持久化上反复出现回归，决定回退到 2026-06-25最后一个稳定版本 `5d2fc18`（"修复UI BUG"，纯 SVG/Konva 渲染路径），同时保留 Phase 1.3-1.4 的 Agent Chat 整套组件不丢。

**回退范围**（不回退 Git 历史，用 `git checkout 5d2fc18 -- <files>` + 新 commit）：

- 画布核心文件回退到 `5d2fc18`：`App.tsx` / `AppShell.tsx` / `ABCompareOverlay.tsx` / `CanvasMentionExtension.tsx` / `CanvasSettings.tsx` / `RichPromptEditor.tsx` / `canvas-ui/CanvasChrome.tsx` / `Toolbar.tsx` / `ElementToolbar.tsx` / `PromptBar.tsx` / `useCanvasInteraction.ts` / `useGeneration.ts` / `KonvaCanvas.tsx` / `KonvaCanvasDemo.tsx` / `CanvasKonvaMediaLayer.tsx`。
- 删除 Excalidraw 专属文件（不留死代码）：`CanvasFixedOverlay` / `ExcalidrawCanvas` / `ExcalidrawInner` / `ExcalidrawLayerPanel` / `FlovartExcalidrawStage` / `FlovartToolbar` / `excalidraw-canvas.css` / `utils/excalidrawAdapter.ts`。
- 其他业务扩展（Tauri / Landing / 企业后台 / Seedance 2.0 / RunningHub 模型扩展 / Prompt 历史面板 / Version History 面板 / Workflow 模块）保留当前状态不动。
- `RightPanel.tsx` 保留当前 HEAD 版（已挂接新 Chat 与旧 AgentChatPanel 条件渲染）。
- `components/agent-chat/Chat.tsx` 整套工具调用 + SSE 流式组件保留不动。
- `types/index.ts` / `utils/boardPersistence.ts` / `utils/canvasHelpers.ts` 已无 `viewport` 字段与 backfill，无需再手动清理。
- 死代码 `components/canvas/InpaintDialog.tsx` 保留文件但不接入（5d2fc18 版 `App.tsx` 不引用）。

**Agent Chat 接线**（在 5d2fc18 版 `App.tsx` 重新接入 Phase 1.3-1.4 能力，去掉 Excalidraw 专属调用）：

- `App.tsx` 新增 `pendingChatAttachments` state（`Array<{ url, mimeType }>`）。
- `App.tsx` 新增 `triggerAddToChat` callback：选中单图元 → `resolveColdMediaRef` 解析 href → `setPendingChatAttachments([{ url, mimeType }])` → `setSelectedElementIds([])` 清选中。已删除原 79b2d2d 版中的 `excalidrawApiRef.current?.updateScene(...)` 行（Excalidraw 专属）。
- `App.tsx` `RightPanel` 调用新增 9 个 props：`elements` / `selectedElementIds` / `setSelectedElementIds` / `commitAction` / `handleGenerate` / `userApiKeys` / `modelPreference` / `pendingChatAttachments` / `onConsumeChatAttachments={() => setPendingChatAttachments([])}`。
- `App.tsx` `ElementToolbar` 调用新增 `onAddToChat={triggerAddToChat}` prop。
- `ElementToolbar.tsx` `ElementToolbarProps` 新增 `onAddToChat?: () => void`；选中单图元时渲染"加入对话"按钮（聊天气泡图标，位于"加入素材库"之后），点击调 `onAddToChat`；`toolbarScreenWidth` 在 `onAddToChat` 启用时增加 52px 容纳新按钮。

**测试要点**：

- ① 画布渲染使用 5d2fc18 SVG/Konva 路径，`components/canvas/` 下只剩 KonvaCanvas/KonvaCanvasDemo/CanvasKonvaMediaLayer/InpaintDialog 文件。
- ② 选中一张图片 → ElementToolbar 在"加入素材库"旁出现"加入对话"气泡按钮 → 点击后选中态清空。
- ③ 右侧 RightPanel 切换到 Agent tab 显示新版 Chat（13 工具 + Tool-Calling 流式），第②步点击的图片作为附件出现在输入框上方。
- ④ 在 Chat 输入 prompt + 附件 → 流式 Agent 调用工具操作画布（element.create/update-prompt/ignite 等）。
- ⑤ `commitAction` / `handleGenerate` / `userApiKeys` / `modelPreference` 等下游 store/util/Provider 路径与 5d2fc18 版一致。
- ⑥ 画布持久化、视频上传、Landing、企业后台等保留功能仍可正常访问。
- ⑦ 拖图片上画布时不再触发 React `insertBefore` NotFoundError：在 image (`App.tsx` 4167) 与 video (`App.tsx` 4292) 两条 `renderMediaInKonva ? <rect> : <image|foreignObject>` ternary 各分支加显式 `key="konva-placeholder"` / `key="svg-media"`，让 React 在 Konva 异步 ready 后由占位 `<rect>` 切到真实媒体节点时走 unmount/mount，而不是 position-reuse mutation placement，消除 5d2fc18 原版预存的 placement 失败。
- ⑧ 控制台 `transparent 不符合 #rrggbb 格式` 的告警属于 5d2fc18 原版预存（4 处 `fill="transparent"` / `background: transparent` 来自 5d2fc18 行 4173/4298/4611 等），为 React DevTools dev 模式对 SVG `fill` 属性的 HTML5 一致性提示，不影响渲染，未改；用户若要求清理可后续单独处理。

### Workflow 生成修复（原地替换 pendingReferences 保活 + 停止生成 AbortError 误吞 + 进度 UI CSS）

- **RunningHub 画布不返回图片修复**（`services/workflowGeneration.ts:300-310`）：非批量原地替换路径此前在 `ingestWorkflowMedia` 返回后立即调 `releaseWorkflowMediaRecord(record.storageKey)`，把新 storageKey 从 `pendingReferences` 移除；此后到 `publish`（`updateProject` → `setWorkflowMediaCanonicalProjects`）之间有多个 `await`（history poster / dataUrl 编码），此窗口内 `InfiniteWorkflow.tsx:203` 的 nodes effect 会触发 `pruneWorkflowMedia`，新 key 既不在 `pendingReferences` 又不在 `canonicalReferences`，blob 被 IDB 删除，`useWorkflowMediaUrl` 读到 null → 画布不显示。修复：推迟 release 到 `publish` 之后（与批量路径在 commit 后释放的时机对齐）。
- **停止生成功能失效修复**（`services/workflowGeneration.ts:268`）：`executeUnifiedIgnition` 的 catch 块把 abort 错误吞成 `{ ok: false, errorMessage: '生成已停止，可重新发起。' }`，`workflowGeneration.ts:268` 随后 `throw new Error(result.errorMessage)` 抛出的是普通 `Error` 而非 `AbortError`；catch 块 `isAbort(error)` 判否，节点被设成 `status:'error'` 覆盖 `cancelWorkflowGeneration` 已发布的 `status:'idle'`。修复：在 `raceAbort` 返回后、检查 `result.ok` 之前加 `if (controller.signal.aborted) throw abortError()`，确保 abort 路径走 `isAbort → idle` 分支。
- **进度 UI CSS 补全**（`styles/workflow.css:124-127`）：新增 `.workflow-node__generation-meta`（右上角 flex 列）、`.workflow-node__generation-message`（半透明黑底毛玻璃胶囊，max 140px 截断）、`.workflow-node__generation-elapsed`（tabular-nums 计时）、`.workflow-node__generation-stale`（底部居中过期提示）。

## Canvas / Art 代码删除 + Table 改名

- **Canvas 专用文件全部删除**：`components/canvas/{KonvaCanvas,KonvaCanvasDemo,InpaintDialog,CanvasKonvaMediaLayer}.tsx`、`hooks/{useGeneration,useCanvasInteraction,useIdbImageResolution}.ts`、`utils/{boardPersistence,canvasViewport,canvasKonvaMediaEligibility}.ts`、相关测试文件。共 13 个文件。
- **Art 工具代码全部删除**：`components/art/` 5 文件（artTools/useArtWorker/ArtToolPack/ArtRippleOverlay/ArtEditDialog）+ `workers/art-*` 6 文件（art-worker/art-protocol/art-processors/{edges,normals,depth,lineart}）。
- **Canvas Toolbar 删除**：`components/Toolbar.tsx` + `tests/toolbarUpload.test.tsx` + `tests/canvasKonvaMediaLayer.test.tsx`，`tests/studioShell.test.tsx` 移除 Toolbar 相关 2 个测试，仅保留 menu model 测试。
- **`utils/canvasHelpers.ts` 重命名为 `utils/helpers.ts`**：只保留 `generateId` 和 `type Rect`，更新所有 import 路径（App.tsx、agent-chat/Chat.tsx、ElementToolbar.tsx）。
- **`WorkspaceView` 类型扩展**：`'workflow'` → `'workflow' | 'table'`（`types/index.ts`）；`StudioMenuModel.mode` + `changeMode` 改为 `StudioMode`（`'workflow' | 'table'`）；`StudioTopMenu` 中间区域新增 Workflow/Table 切换 tabs；`studioShell.test.tsx` / `workspaceSwitch.test.tsx` 同步更新。
- **App.tsx 重写**：从 3766 行精简到 ~400 行。删除所有 Canvas/Art state/handlers/渲染分支，保留 Workflow early return + Table 占位 return（"Table 工作台筹备中"居中占位）。`handleEnhancePrompt`/`saveGenerationToHistory` 改为内联实现。移除未使用的 `dispatchWorkflowCommand` import 和重复的模块级 `handleWorkflowReversePrompt`。`studioMenuModel` 补全 `title`/`resolvedTheme`/`status` 字段。

**测试要点**：

- ① StudioTopMenu 顶部出现 Workflow / Table 两个 tab，点击切换各自视图正确显示。
- ② Workflow 视图功能与删除前一致：节点流编排、Agent Chat、素材库、生成历史、导入导出等全部正常。
- ③ Table 视图显示"Table 工作台筹备中 / React Flow 故事板 + Agent SKILL 即将上线"居中占位。
- ④ 设置面板（CanvasSettings）在两个视图下均能正常打开使用。
- ⑤ `npx tsc --noEmit` 无新增类型错误（仅预存 `workflowImageTools.test.tsx:81` 语法错误）。
- ⑥ `tests/workspaceSwitch.test.tsx` 测试通过。
- ⑦ 项目中不再存在对 `components/canvas/`、`components/art/`、`components/Toolbar`、`workers/art-*`、`canvasHelpers` 的引用。

## Workflow PromptBar 与 BYOK 模型路由

- Workflow 继续复用共享 `PromptBar`：第一层固定展示 GPT Image 2、Gemini 3 Pro Image、Seedream 5 Pro、Seedance 2.0、Veo 3.1、Kling VIDEO 等产品模型；具体 API Key 与上游模型 ID 在设置页映射，PromptBar 不再直接暴露供应商返回的杂乱模型列表。
- 模型切换后按产品能力渐进披露生成方式、画质、清晰度、比例、时长、音频、数量、联网搜索和真人素材预检测；不支持的选项隐藏，已选参数不受支持时才自动校正。
- Veo 的文生视频、图生视频、参考图和首尾帧模式分别校验素材数量与顺序；参考图、高分辨率等官方限定场景会在界面和提交层共同约束为 8 秒，不再只在请求失败后提示。
- GPT Image 的清晰度与比例改为使用满足官方边长、16 倍数、像素总量和宽高比限制的安全尺寸表；已下线的 Gemini 图片预览模型不会再被自动映射。
- 设置页的供应商表单新增固定模型映射、主备优先级、价格规则增删改和月预算策略；预算硬上限只阻止新任务，不会伪装成可以撤回供应商已接受的任务。
- API Key 卡片新增本月费用、累计费用、待核账次数和月预算进度；同一适用范围内的按请求与按图片/视频价格可组合预估，币种冲突时保留未知而不是错误相加。
- Workflow 生成前会按 Key/模型价格规则预估并预留用量，结果按 `reserved / submission_unknown / queued / running / succeeded / failed / canceled` 记录在本机 `localforage`；失败或中止未取得供应商账单时标为费用未知，不自动退款。
- Seedance 在提交请求开始后遇到网络错误会进入“提交结果未知”，不会自动重试或切换备用 Key；手动重试会提示可能二次扣费。“停止等待”只终止本机轮询，不声称已取消上游任务。
- 真人素材预检测当前仅是本机提示词级基础检查，不代表供应商权威合规结果；仍需用真实 API Key 验证各供应商返回字段与最终账单。

**测试要点**：

- ① Workflow 新建图片/视频节点并选中，确认共享 PromptBar 出现；模型卡、参数面板和高级选项能正常展开，浏览器控制台无 Hook 或渲染错误。
- ② 在设置中添加供应商，确认自动建议匹配的固定产品模型仍可手动改上游模型 ID、启停和优先级；未映射模型在 PromptBar 显示“去配置”且不能生成。
- ③ 分别切换图片与视频产品模型，确认只出现该模型支持的生成方式和参数；Veo 高分辨率/参考模式自动约束为支持的时长。
- ④ 新增/编辑/删除价格规则并开启月预算；低于预警、达到预警、超过硬上限时分别验证提示与新任务阻止行为。
- ⑤ 模拟 Seedance POST 后断网，确认状态为“提交结果未知”且不自动重试；点击重试前必须二次确认可能重复计费。
- ⑥ Prompt 编辑器空态应显示可见占位文案并具备文本框无障碍名称；低高度窗口展开参数时可纵向滚动，模型卡支持横向吸附浏览。
- ⑦ 本轮隔离测试共 11 个文件、123 项通过；全量类型检查和浏览器复测仍受当前并行改造中的既有编译失败影响，不能作为本功能完成判据。

## 顶栏三态主题 + 设置页 Tab 重构 + 节点聚焦动画 + PromptBar 模型过滤 + 文件夹 UI v2

- **顶栏主题切换改为三态下拉**：`StudioTopMenu` 的 `StudioMenuModel` 接口从 `theme: 'light'|'dark'` + `actions.toggleTheme` 改为 `themeMode: ThemeMode`（`'light'|'dark'|'system'`）+ `resolvedTheme: 'light'|'dark'` + `actions.setThemeMode`。下拉菜单含三项（浅色 / 深色 / 跟随系统，图标 Sun / Moon / Monitor），click-outside 关闭，active 项带 Check icon。`App.tsx` `studioMenuModel` 同步改用 `themeMode` / `resolvedTheme` / `setThemeMode`（`useWorkspaceStore` 持久化 `themeMode`）。
- **CanvasSettings 重构为 3-Tab 布局**：移除 props `language` / `setLanguage` / `themeMode` / `setThemeMode` / `wheelAction` / `setWheelAction`，新增内部 `activeTab: 'api'|'models'|'security'` state，三个 tab：API 配置 / 模型偏好 / 安全。移除主题三态卡片 section、语言 + 滚轮 section。header 描述改为"管理 API 供应商、模型映射和本地安全策略。主题与语言请在顶栏切换。" API Key 编辑弹窗（L1083+）保留不变。`App.tsx` 两处 `CanvasSettings` 渲染（L3811、L4033）同步精简 6 个废弃 props。
- **Workflow 节点双击聚焦动画**：`InfiniteWorkflow.tsx` `focusNode` 重写为 `fitView` 风格 `k` 计算（`Math.min(1.5, Math.max(0.12, Math.min((w-160)/nodeW, (h-160)/nodeH)))`）+ `padding=120` + `rAF` 驱动 `easeInOutCubic` tween 420ms；修复 `node.x`/`node.y` → `node.position.x`/`node.position.y`。新增 `focusAnimRef`，用户拖拽 / 滚轮交互时立即取消未结束动画。
- **PromptBar 按节点类型过滤模型**：`WorkflowNodePromptBar.tsx` `modeOptions` 按 `node.type` 过滤——`image` 节点只展示图像模型，`video` 节点只展示视频模型，`text` 节点锁定文本模式。
- **未配置产品模型可点击打开设置**：`PromptBar.tsx` 未配置模型的卡片从 `disabled` 改为可点击，点击调用 `onOpenSettings` 打开设置页（不再禁用）。
- **文件夹 UI v2 全链路**：新建 `components/studio/DeleteFolderDialog.tsx`（两选项：移动内容到未分类 / 永久删除文件夹及所有内容，红色危险按钮，空文件夹直接删除）；新建 `components/studio/FolderTree.tsx`（文件夹树 + 新建 / inline 重命名 / 删除，轻量 props）；`components/studio/StudioMediaBrowser.tsx` 集成 `FolderTree`；`components/workflow/WorkflowSidebar.tsx` / `WorkflowWorkspace.tsx` 透传 folder props；`App.tsx` 接入 folder callbacks（复用 `utils/assetStorage.ts` `addFolder` / `removeFolder` / `renameFolder`）。

**测试要点**：

- ① 顶栏点击主题下拉，分别选浅色 / 深色 / 跟随系统，确认画布与设置页主题立即切换且刷新后保持；第三方系统主题切换时"跟随系统"项应实时跟随。
- ② 打开设置页，确认只有 API 配置 / 模型偏好 / 安全 3 个 tab，主题与语言 section 已移除；切到各 tab 内容正常显示，API Key 编辑弹窗能正常打开与保存。
- ③ Workflow 双击图片 / 视频 / 文本节点，确认画布平滑缩放到该节点居中（padding 120，420ms easeInOutCubic）；动画过程中拖拽或滚轮应立即打断动画。
- ④ Workflow 新建 image 节点选中，PromptBar 模型列表只含图像模型；video 节点只含视频模型；text 节点锁定文本模式不可切模型。
- ⑤ 设置页未配置供应商的模型卡片在 PromptBar 显示"去配置"，点击应打开设置页而非禁用灰态。
- ⑥ Studio 素材浏览器左侧文件夹树：新建文件夹、inline 重命名、删除空文件夹（直接删除）、删除非空文件夹（弹窗两选项）；选"移动到未分类"后内容保留，选"永久删除"后内容同步消失。
- ⑦ 顶栏主题三态切换与设置页移除的主题/语言/滚轮 section 不应回退或重复出现；`studioShell.test.tsx` / `canvasSettingsProviderUi.test.tsx` 已更新 props，预存在失败（react-router Link context、workflowImageTools 语法）与本次改动无关。

## 节点聚焦后"还原缩放"浮层

- **聚焦后弹出可还原浮层**：`InfiniteWorkflow.tsx` 新增 `focusBadge` state。`focusNode` 动画结束（或节点已居中提前返回）时 `setFocusBadge(true)`，在画布底部居中渲染极简扁平按钮"还原缩放"（`Maximize2` 图标，无背景 / 无边框 / 透明胶囊，仅 hover 反馈，`motion.spring` 出入场）。点击调用 `fitView()` 还原全画布视图并隐藏浮层。任何用户主导的视口导航（`startPan`、滚轮、`zoomBy` / `zoomReset`、小地图 `onCenter`、`fitView`）都会 `setFocusBadge(false)` 关闭浮层；节点拖拽 / 框选等不改视口的操作不关闭。

**测试要点**：

- ① 双击图片 / 视频 / 文本节点聚焦后，画布底部居中弹出"还原缩放"浮层；点击后回到全画布适配视图，浮层消失。
- ② 浮层显示期间拖拽空白平移、滚轮缩放、点击顶栏放大 / 缩小 / 重置、点击小地图任一位置，浮层应立即关闭。
- ③ 浮层显示期间拖拽节点、框选节点不改视口，浮层保持可见。连续聚焦不同节点时浮层应在新动画结束后重新出现。

## PromptBar 严格两栏结构重构

- **模型面板按公司分组**：`PromptBar.tsx` 的 model 面板从 flat snap-x 横滚卡片改为按公司分组的 section 列表。每 section 顶部公司名（`uppercase tracking-[0.14em]`）+ 该公司模型卡片横向 snap-x 滚动。底层调用 `getProductModelsByCompany(activeProductModel.capability)`。无 `activeProductModel`（如纯文本模式）时回退旧的 flat 模型列表。搜索框保留，搜索同时匹配模型名/简称/公司/badge/描述。
- **删除独立 mode / ratio / duration 面板**：`ExpandPanel` 类型从 `'mode'|'model'|'parameters'|'ratio'|'duration'|'advanced'|'more'` 精简为 `'model'|'parameters'|'advanced'|'more'|null`。图片/视频/首尾帧模式选择完全收进 parameters 面板「生成方式」一行（仅 `activeProductModel.capabilities.modes.length > 1` 时显示，即只有视频模型多 mode 时出现）。画面比例 chip 与时长 chip 不再作为独立 trigger 出现，全部并入 parameters 面板。同步删除 `showRatioTrigger` / `showDurationTrigger` / `showAdaptiveRatio` / `activeRatioOptions` / `supportedRatios` / `supportedImageRatios` / `supportedDurations` / `DEFAULT_VIDEO_RATIOS` / `DEFAULT_IMAGE_RATIOS` 等死代码与对应 import (`SOCIAL_PRESETS` / `getSupportedRatios` / `getSupportedDurations` / `getSupportedImageRatios` / `inferProviderFromModel` / `getModelCapabilityTags`)。
- **数量行进入 parameters 面板**：原 trigger 行右侧的 `[X1/X2/X4]` 批量方案选择器（`batchCount`）移入 parameters 面板最后一行，标题"数量"，仅 `generationMode === 'image'` 或 video + `allowVideoBatch` 时显示。trigger 行不再展示批量选择。
- **trigger 行四段结构**：[未配置 API Key 红框（仅 `keyCount===0` 时显示）] + [模型 chip] · [参数摘要 chip（`paramSummary` memo：低画质/标准/高画质 + 分辨率 + 比例 + 时长 + ×N）] · [润色][译][高级][更多] · [生成/停止]。已配置 API Key 时 trigger 行不再显示 key chip，全部折叠进「更多」面板。
- **「更多」面板新增 API Key 状态行**：`expandedPanel === 'more'` 在 Seedance 视频参数之后、上传参考图之前新增 `MenuOptionButton`：label=`API Key · N 个`，description=默认 key 名或"点击打开设置管理 Key 与映射"，点击调 `onOpenSettings()` 并关闭浮层。
- 同步养代码：PRECATED `getModeLabel` 仍被 trigger 行封装参数摘要内部间接使用，但 trigger 行不再展示独立 mode chip。
- typecheck 通过（仅 `tests/workflowImageTools.test.tsx(81,94)` 预存语法错误）。

**测试要点**：

- ① 点击模型 chip → 模型卡片按公司分组排列（如 Google / 字节 / Kuaishou 等），每组顶部公司名 + 横向卡片滚动；搜索关键词能跨公司过滤单卡。
- ② 切换到视频模型后 parameters chip 摘要展示「清晰度 · 比例 · 时长」，点击展开 parameters 面板，第一行是"生成方式"（仅多 mode 模型出现，如 Seedance 文生/图生/全能参考/首尾帧）；视频参数顺序为：生成方式 → 画质 → 清晰度 → 比例 → 时长 → 音频 → 数量。
- ③ 切换到图片模型后 parameters 面板顺序为：画质 → 尺寸 → 比例 → 数量（无"生成方式"行）。
- ④ trigger 行不再出现独立"图片/视频"mode chip、独立"比例" chip、独立"时长" chip。
- ⑤ 未配置任何 API Key 时 trigger 行最左侧红色 dashed "🔑 未配置 API Key" chip 显示；配置至少 1 个 key 后该 chip 消失，进入「更多」面板才能看到 "API Key · N 个" 行。
- ⑥ 批量 ×1 / ×2 / ×4 选择器不再出现在 trigger 行右侧"生成"按钮旁，只能在 parameters 面板"数量"行调整；trigger 行"生成"按钮文字仍反映 `×N` 数量（如 `生成 4 版`）。
- ⑦ parameters chip 摘要文字应随画质 / 分辨率 / 比例 / 时长 / 数量变化即时更新，且点 chip 即开 parameters 全面板。

## CLI Provider 与 Flovart 设置统一

- `provider.status`、`provider.select-model`、`provider.test` 已从 CLI shadow runtime 改为通过 file bridge 在 Flovart 浏览器 runtime 中执行；CLI 与 MCP 共用同一份 browser command 路由表。
- Provider 查询、选择模型和 readiness 检查默认等待浏览器返回；API Key 仍只保存在浏览器加密存储中，命令结果只返回脱敏状态。
- 普通 `status` 仍用于查看本地 file-state 概览，其中的 Provider 字段只是 shadow metadata；真实 Provider 状态以 `provider.status` 为准。
- 已用临时无 Key 浏览器上下文完成自动端到端验证：CLI 查询初始模型 → CLI 切换图片模型 → 浏览器 runtime 与 `modelPreference.v1` 同步 → CLI 再查询得到相同模型；测试未触发任何图片/视频生成。

**测试要点**：

- ① 保持 Flovart 浏览器页打开，在设置中切换图片/视频模型后运行 `npm run flovart:cli -- provider.status --json`，返回模型应与界面一致。
- ② 运行 `npm run flovart:cli -- provider.select-model --image-model <产品模型ID> --json`，刷新设置页后模型偏好保持一致，随后生成使用该模型映射。
- ③ 关闭 Flovart 浏览器页后运行 Provider 命令，应返回 pending/需要打开浏览器，而不是回退读取旧 shadow Provider 状态。
- ④ CLI/MCP 输出不得出现 API Key、token 或 secret。

## 中间工具栏：连线显示开关 + 一键整理画布

- **位置**：`components/workflow/WorkflowToolbar.tsx`（在「磁吸」按钮之后新增两枚图标按钮）；`components/workflow/InfiniteWorkflow.tsx`（状态、布局算法、还原浮层）。
- **连线显示开关**（`Spline` 图标）：
  - 默认开启。关闭后 `<WorkflowConnections>` 的 `connections` 传空数组，已有连线不渲染；
  - 正在拖拽的临时连接预览（`active={connectionDrag}`）仍正常显示，便于用户重新连接；
  - 按钮高亮态对应「显示中」，弱化态对应「隐藏中」，Tooltip 文案随之切换。
- **一键整理画布**（`Workflow` 图标）：
  - 点击后调用 `computeHierarchicalLayout`：按 `connections` 做拓扑分层，从左到右排开（每列 `x = layerIndex * (280+100)`，列内节点纵向 `y += 节点高度 + 40`）；
  - 无任何连线的孤立节点统一放到最右一列，避免与有向图混杂；
  - 整理前通过 `pushHistory(currentFrame())` 记录旧位置，支持 Ctrl+Z 撤销；
  - 整理后画布右下角弹出一颗 toast 浮层（motion spring 入场，不遮画面），文案「已整理节点」+「还原」「保留」两枚按钮，6 秒倒计时自动消失视为保留；
  - 点「还原」会把 `prev` 节点位置回写并同样 pushHistory，保证撤销/重做两边可走；
  - 浮层按钮 `onPointerDown` `stopPropagation`，避免触发画布平移/选择。
- 测试要点：
  - ① 空 workflow 点「一键整理」无副作用、无浮层。
  - ② 多节点 + 连线场景下点整理，节点按拓扑分列、孤立节点归并到最右一列。
  - ③ 浮层出现后点「还原」，节点回到原位；点「保留」浮层消失、节点位置保留。
  - ④ 浮层 6 秒后自动消失。
  - ⑤ 关闭连线开关后再从节点端点拖拽，临时虚线预览仍可见，松手后连线生成成功；再次打开开关后所有连线重新出现。
  - ⑥ 隐藏连线开关对 Undo/Redo、小地图、批量折叠节点无干扰。

## PromptBar 参考图 chip 面板（Phase 1）

- **数据载体**：`WorkflowNodeMetadata.imageReferenceOrder: string[]`（`components/workflow/types.ts`），仅存上游 image/video/audio 节点的 id 顺序数组；其余 chip 样式（label/thumbnail/elementType/mentioned）运行时由 `references.ts` 从 nodes 派生。
- **派生 + 协调工具**（`components/workflow/references.ts`）：
  - `getOrderedImageReferences()` 按 `imageReferenceOrder` 已有条目排前，新连线节点按 connection 顺序尾部追加，断连 id 自动剔除。
  - `toImageReferenceChips()` 把有序节点转成 chip UI 数据，`mentioned` 布尔值来自 `mentionedNodeIds` 区分「连线已上送」vs「连线未上送」。
  - `reconcileImageReferenceOrder()` / `applyImageReferenceOrder()` / `sortReferencesByOrder()`。
- **UI**（`components/PromptBar.tsx`）：在 `RichPromptEditor` 上方新增可选 chip 面板，使用 `Reorder.Group`/`Reorder.Item`（`motion/react`）实现拖拽排序；每个 chip 显示缩略图（img / video / audio 占位）+ 1-based 自动编号 + × 删除按钮。新增 props `imageReferenceChips?` / `onImageReferenceReorder?` / `onImageReferenceRemove?`，未传参时面板不渲染。
- **接线**（`components/workflow/WorkflowNodePromptBar.tsx`）：
  - 新增 `onDisconnectReference?` prop。
  - `handleReorder()` 调 `applyImageReferenceOrder`，无变化时不触发 onChange。
  - `handleRemoveReference()` 同时更新 `imageReferenceOrder` 与 `mentionedNodeIds`，再调 `onDisconnectReference`。
  - `useEffect` 自动 reconcile：连线增删导致 drift 时把 `reconciledOrder` 写回 metadata，无需手动改 order。
- **画布断连**（`components/workflow/InfiniteWorkflow.tsx`）：`onDisconnectReference` 找到当前节点到指定上游节点的 connection，执行 `applyOps([{type:'delete_connections', ids:[conn.id]}])`。
- **Provider 排序同步**（`services/workflowGeneration.ts`）：去重 references 后用 `sortReferencesByOrder` 按 `initiating.metadata.imageReferenceOrder` 排序，再走 video submode slotRole 分配；order 缺省时保持原顺序。
- **双源共存原则**（沿用决策）：
  - `imageReferenceOrder` 决定 chip 顺序 + Provider 参考图排序 + 缩略图列表展示；
  - `mentionedNodeIds` 决定是否上送 Provider（连线只是面板 chip 候选，必须有 @ 或拖入面板且 included 才进入 imageReferenceOrder → Provider）。
  - 当前 Phase 1 只做「连线 → 面板 chip → × 断连 → Provider 排序」闭环；@ 素材库新建节点+连线+结构化 Token 已在 Phase 2 实现（见下方 Phase 2 节）。
- **待用户验证**：
  - ① 选中一个图片/视频/文本节点，连接上游 image/video/audio 节点，PromptBar 上方出现 chip 面板，按连线顺序展示缩略图 + 自动编号。
  - ② 在面板上拖拽 chip 调整顺序，松手后顺序持久化，再次聚焦节点顺序保持。
  - ③ 点击 chip 的 × 按钮，对应连线断开、节点从面板消失，`imageReferenceOrder` 与 `mentionedNodeIds` 同步剔除该 id。
  - ④ 断开连线再重连，chip 在尾部重新出现；删除上游节点后 order 自动剔除该 id 而不报错。
  - ⑤ 触发一次生成（Phase 2 @ 注入已实现，原有连线+原有 @ 注入链路应不受影响），Provider 收到的参考图按 `imageReferenceOrder` 顺序排序。
  - ⑥ chip `mentioned` 状态（连线 vs 已 @ 上送）在视觉上有可区分的边框/底色。

## PromptBar @ 素材库注入选站节点（Phase 2）

- **目标**：在 PromptBar `@` 菜单中加入「个人素材库」分区，命中素材后在当前生成节点上方自动新建 image 节点实例 + 连线 Edge + `imageReferenceOrder` 项 + 插入绑定 newNodeId 的 Mention Token。
- **数据源**：① 当前生成节点已连接上游节点（沿用 Phase 1 connected items）② `loadAssetLibraryAsync()` 返回的个人 AssetLibrary items；不纳入跨项目节点 / generationHistory / 游离媒体。
- **作用域分区**：本轮只做「个人」作用域；Skill 显示灰色 disabled 占位 + tooltip「即将支持 Anthropic Claude Skills 作为参考资源」，不可点击，不实现数据接入。
- **两级浏览结构**：第一层列出 `folders[parentId=null]`，第二层进入文件夹显示其 items；搜索时跨文件夹返回最多 50 条匹配；"个人素材库" 是 MentionList 上的虚拟根标题，不是 AssetFolder。
- **基础设施层**：
  - `utils/assetStorage.ts`：加 `cachedLibrary` 模块级缓存 + `setAssetLibraryCache(lib)` + `getAssetLibraryCache()` + `getAssetById(assetId): Promise<AssetItem|undefined>`（命中缓存同步返回，否则全量加载）。
  - `App.tsx`：assetLibrary useEffect 中 `saveAssetLibraryAsync` 后调 `setAssetLibraryCache(assetLibrary)` 同步缓存。
  - `components/workflow/types.ts`：`WorkflowNodeMetadata` 加 `sourceType?: 'assetLibrary'` + `assetId?: string`。
  - `components/workflow/media.ts`：`loadFallbackMediaBlob` 加 `asset-library:` 分支调 `getAssetById` → `workflowDataUrlToBlob(asset.dataUrl)`。
  - `components/CanvasMentionExtension.tsx`：`MentionNodeViewProps` + `addAttributes` + `MentionData` 三处加 `sourceType?` + `assetId?` 可选字段。
- **Mention UI 层**：
  - `components/MentionList.tsx` 完整重写：新增 `AssetSuggestion` 轻量接口（id/name/folderIds/tags/thumbnail/elementType，不含原图 dataUrl）；`MentionItem` 扩展 `kind?: 'item'|'folder'|'back'|'skillDisabled'` + `folderId?`；三区分区渲染（已连接 / 个人素材库 / Skill disabled）；内部 `activeFolderId` state 管理文件夹导航；query 非空时跨文件夹搜索 max 50；键盘 ArrowUp/Down 跨分区跳过不可点击项；文件夹点击触发内部导航不调 command；Skill 项 disabled + tooltip。
  - `components/RichPromptEditor.tsx`：`buildSuggestionExtension` 签名扩展为 `(getItems, assetFoldersRef, assetItemsRef, onSelectAssetRef, skillEnabled)`；tippy `placement: 'top-start'` + `flip.behavior: ['bottom-start','top-start']` + `padding: 8`（自动判断方向，默认朝上，空间不够翻转朝下）；`command({editor,range,props})` 检测 `item.sourceType==='assetLibrary'` → 调 `onSelectAssetRef.current(assetId)` 同步拿 newNodeId → 用 newNodeId 插入 Mention；`RichPromptEditorProps` 加 `assetFolders?` / `assetItems?` / `onSelectAsset?` / `skillEnabled?`。
- **Props 透传链**：`PromptBar.tsx` → `WorkflowNodePromptBar.tsx` → `InfiniteWorkflow.tsx`（`assetLibrary?` prop + `handleSelectAsset(assetId, fromNodeId)` 回调 + `assetFolders`/`assetSuggestions` useMemo）→ `WorkflowWorkspace.tsx`（传 `assetLibrary` 给 InfiniteWorkflow）。
- **节点落位（D 方案）**：新建节点置于发起生成节点上方，`newNodeY = targetNodeY - 240 - 120`，`newNodeX = targetNodeX + (targetWidth - 340) / 2`（居中对齐，image node width=340），基础碰撞避让；不自动 fitView。
- **原子写入**：通过 `applyOps([{type:'add_node'}, {type:'connect_nodes'}, {type:'update_node'(imageReferenceOrder)}])` 一次性 apply，保证 undo/redo 原子性；`applyWorkflowOps` 同步返回，`onSelectAsset` 同步返回 newNodeId 供 Tiptap command 立即插入 Mention。
- **storageKey 命名空间**：节点 metadata 只存 `assetId` + `sourceType:'assetLibrary'` + `href='asset-library:<assetId>'`，不复制 base64/dataUrl，不设 storageKey（避免 workflowMediaStorage 查找截胡）；resolver 优先复用现有 `media.ts` 路径。
- **去重范围**：仅在当前目标生成节点的已连接上游按 assetId 复用（命中同一 assetId 只插 Token，不新建节点）；项目其他分支存在同一 assetId 允许新建节点实例（实例共享同一 assetId），不跨画布复用。
- **删除语义**：`sourceType` / `assetId` 只用于来源追踪、展示、素材匹配；Backspace 删 Token 只删正文 @ 文字，不删节点+连线+imageReferenceOrder；chip × 才断连。
- **待用户验证**：
  - ① 选中一个生成节点，在 PromptBar 输入框打 `@`，弹层默认朝上展开（空间不够自动翻转到朝下），三区分区可见：已连接上游节点 / 个人素材库（文件夹+items）/ Skill（灰色 disabled + tooltip）。
  - ② 点「个人素材库」分区下的文件夹，进入第二层显示该文件夹 items；点「返回」回到文件夹列表；文件夹点击不触发 command、不关闭弹层。
  - ③ 在搜索框输入关键词，跨文件夹返回最多 50 条匹配 items，不分文件夹平铺展示。
  - ④ 选中一个素材 item：弹层关闭，正文插入 `@[素材名]` Mention Token；画布上当前生成节点上方自动出现一个新的 image 节点（按 D 方案落位），新节点与生成节点之间生成连线；chip 面板（Phase 1）出现该新节点 chip。
  - ⑤ 再次 `@` 选中同一个素材（当前生成节点上游已存在同 assetId 节点）：只插入 Mention Token，不新建节点、不新增连线、chip 不重复。
  - ⑥ 新建节点应能正确渲染素材图片（通过 `asset-library:<assetId>` → `getAssetById` → `workflowDataUrlToBlob` 路径），暗色主题下同样正常。
  - ⑦ Backspace 删除 Mention Token：只删正文 @ 文字，节点、连线、chip 不受影响；chip × 删除才断连。
  - ⑧ 触发一次生成：Provider 收到的参考图包含该新建节点（连线即上送），`imageReferenceOrder` 顺序正确。
  - ⑨ 撤销（Ctrl+Z）：节点+连线+imageReferenceOrder+Mention Token 一并回退（原子性）；重做（Ctrl+Y）一并恢复。
  - ⑩ Skill 分区项灰色不可点击，hover 显示 tooltip「即将支持 Anthropic Claude Skills 作为参考资源」。

## 视频参数灰显 + 视频首/尾帧导出 + xAI Grok Imagine Video（本轮新增）

- **视频 PromptBar 三面板灰显**：`PromptBar.tsx` 视频模式 chip 面板改为按 `VIDEO_MODE_ORDER` 全量渲染，对当前产品模型不支持的模式直接 `disabled + opacity-35 + cursor-not-allowed`，hover 显示 `explainUnsupportedVideoMode()` 返回的原因 tooltip；分辨率 / 比例 / 时长三个参数面板同步走新增的 `paramDisabledReason(kind, value)` callback，该 callback 基于 `sanitizeProductGenerationParams` 做 probe 校验，不可用的值统一灰显并带 tooltip。chip 入口触发条件由 `activeProductModel` 硬依赖改为 `generationMode === 'video' && VIDEO_MODE_ORDER.length > 1`，避免未选模型时整个面板崩塌。
- **视频节点首/尾帧导出**：`components/workflow/WorkflowNodeToolbar.tsx` 在 AV 拆分后追加两个按钮（`ArrowUpToLine` 首帧、`ArrowDownToLine` 尾帧），`WorkflowVideoToolHandlers` 接口新增 `extractFrame?: (id, position) => void`。点击后由 `InfiniteWorkflow.tsx` `handleExtractFrame` 处理：经新增 `services/videoFrameExtractor.ts`（`extractVideoFrame(blob, 'first' | 'last')`：`<video>` 加载 → seek 到 0 或 `videoDuration - 0.1` → `canvas.drawImage` → `toBlob('image/png')`，含 `seekTo` 与 error 兜底）抽帧，再 `ingestWorkflowMedia` 落地，按 `fitWorkflowMediaSize('image', w, h)` 计算尺寸，在原视频节点左侧（首帧）或右侧（尾帧）生成图片节点并建立 video→image 连线。处理回调改用 `pushHistory + patchProject` 直接写 `nodes/connections`，不再经 `applyOps`（早先 `useCallback` 在 `applyOps` 声明前闭包引用造成 TDZ `ReferenceError: Cannot access 'applyOps' before initialization`）。
- **xAI Grok Imagine Video Provider 适配**：`services/productModelCatalog.ts` 新增两条目录——`flovart:grok-imagine-video`（officialModelIds `['grok-imagine-video']`）与 `flovart:grok-imagine-video-1.5`（officialModelIds `['grok-imagine-video-1.5']`），provider `'xai'`，modes `['text-to-video', 'image-to-video', 'video-extension']`，7 个 aspectRatio，durations `[1,3,5,8,10,15]`，resolutions `['720p','1080p']`；`getRoutedVideoModes` 新增 `provider === 'xai'` 分支返回全三模。`services/aiGateway.ts` 新增 xai 分支：`POST /v1/videos/generations`（文生 / 图生，body 含 `prompt / duration / aspect_ratio / resolution / image.url`）与 `POST /v1/videos/extensions`（扩展，body 含 `prompt / video.url / duration`）返回 `{ request_id }`，`GET /v1/videos/{request_id}` 轮询直到 `status: 'done' | 'failed'` 后下载 `output.url`；Bearer 鉴权，默认 Base URL `https://api.x.ai/v1`。`types/index.ts` `AIProvider` union 新增 `'xai'`；`aiGateway` 的 `PROVIDER_LABELS / PROVIDER_VIDEO_RATIOS / DEFAULT_BASE_URLS / DEFAULT_PROVIDER_MODELS / inferProviderFromKey / inferProviderFromModel` 同步扩展。
- typecheck 通过（仅 `tests/workflowImageTools.test.tsx(81,94)` 是 HEAD 已预存的语法错误，与本次无关）。
- 待用户验证：
  - ① 选中一个视频节点，切到不同产品模型（如切到 Seedance 1.0 fast 不支持首尾帧的模式），观察模式 chip、时长、比例、分辨率哪些变灰显且 hover 出 tooltip。
  - ② 视频节点工具栏右侧出现首/尾帧两个按钮，点击后在视频节点左侧 / 右侧分别生成图片节点并自动连线。
  - ③ 设置页填入 xAI API Key，在视频节点选 `grok-imagine-video` 或 `grok-imagine-video-1.5`，分别测文生视频 / 图生视频 / 视频扩展三种模式，确认 polling 拿到 `request_id` 后能下载到 mp4 并落地到画布。

## Gemini 图像产品条目与路由收口（本轮新增）

- **产品模型 catalog 补全**：`services/productModelCatalog.ts` 在 Google 图像区新增 3 条目：
  - `flovart:gemini-3.1-flash-image`（Gemini 3.1 Flash Image，即 NB 2）：officialModelIds `['gemini-3.1-flash-image']`，aliases `['gemini-3.1-flash-image-preview']`，badge 「通用」，15 个 aspectRatio（含 `1:4` / `4:1` / `1:8` / `8:1` / `21:9` / `9:21`），resolutions `['512','1K','2K','4K']`，`supportsWebSearch: true`，`maxImageReferences: 14`。
  - `flovart:gemini-3.1-flash-lite-image`（Gemini 3.1 Flash Lite Image，即 NB 2 Lite）：officialModelIds `['gemini-3.1-flash-lite-image']`，badge 「极速」，7 个 aspectRatio，resolutions `['512','1K','2K']`，`supportsWebSearch: true`，`maxImageReferences: 14`。
  - `flovart:imagen-4`（Imagen 4）：officialModelIds `['imagen-4.0-generate-001']`，aliases `['imagen-4.0-fast-generate-001','imagen-4.0-ultra-generate-001']`，badge 「已弃用」，modes 仅 `['text-to-image']`，`maxImageReferences: 0`。
- **DEFAULT_PROVIDER_MODELS 调整**：`services/aiGateway.ts` `google.image` 改为 `['gemini-3.1-flash-image','gemini-3.1-flash-lite-image','gemini-3-pro-image','imagen-4.0-generate-001']`——移除 NB 1 `gemini-2.5-flash-image`，新增 NB 2 Lite 与 Imagen 4。
- **aiGateway google 图像分支简化**：`generateImageWithProvider` 中 google 分支删除 `refs.length > 0` + `supportsReferenceImageEditing` 三层冗余判断，简化为：`isGoogleImageEditModel(model)`（NB 系列含 `gemini-3.1-flash-lite-image`）走 `editImage` → `generateContent`（支持 `refs=[]` 纯文生图），`isGoogleTextToImageModel(model)`（Imagen 系列）走 `generateImageFromText` → `generateImages` API。
- **CanvasSettings.tsx Google Visual Models 模板**：`defaultModel` 从 `gemini-3-pro-image` 改为 `gemini-3.1-flash-image`；`models` 列表追加 `gemini-3.1-flash-image` 与 `gemini-3.1-flash-lite-image`。
- **geminiService.ts 注释**：标注 Imagen 4 已弃用（2026-08-17 关停）；`generateImageFromText` fallback 保持 `imagen-4.0-generate-001` 不变——该函数仅服务 Imagen `generateImages` API，NB 系列不走此路径。
- typecheck 通过。
- 待用户验证：
  - ① 设置页 Google Visual Models 模板默认选 `gemini-3.1-flash-image`，下拉可看到 NB 2 / NB 2 Lite / NB Pro / Imagen 4（标注已弃用）四项。
  - ② 在画布用 NB 2（`gemini-3.1-flash-image`）做纯文生图（不带参考图），确认能出图——NB 系列走 `generateContent` 接口 `refs=[]`，与 Imagen `generateImages` 路径不同。
  - ③ 在画布用 NB 2 Lite（`gemini-3.1-flash-lite-image`）测带参考图的图像编辑，确认能出图且速度比 NB 2 快。
  - ④ 在画布用 Imagen 4 仍可做纯文生图，确认 deprecated 模型当前仍可用（至 2026-08-17 关停前）。
  - ⑤ 确认旧 `gemini-2.5-flash-image`（NB 1）已从设置页模板和默认模型列表消失，不再可选。

## RunningHub Provider 映射与 Route Catalog 落地（本轮新增）

完成 `docs/dev/runninghub-provider-mapping-handoff.md` 中 7 个 Blocker、17 条 Route Contract Test 与旧 regex 回退路径清理。

### 协议决策（ADR-0027 ~ ADR-0033 + ADR-0006/0008）

按 ADR-0027（产品模型绑定到路由）、ADR-0028/0003（Route Schema + Adapter 独自负责字段适配，不再在通用 gateway 里写 endpoint regex/字段特判）、ADR-0029/0004（只有已验证 Route Schema 可执行，未在 Catalog 中登记的路由直接拒绝提交）、ADR-0031/0006（每条 Route 写一份 Contract Test 覆盖全 mode）、ADR-0033/0008（Route Schema 约束控件，Adapter 不得静默删/换/降级用户参数）落地。

### 类型与目录重构

- `types/index.ts`：`ProductModelMapping` → `ProductRouteBinding`（字段 `productModelId / mode: ProductModelMode / routeId / priority / enabled / confirmed`）；`UserApiKey.modelMappings` → `routeBindings`；`ApiPricingRule.upstreamModelId` → `routeId`。按 AGENTS.md "不要兼容旧数据"直接替换，无 dual-track。
- `services/productModelCatalog.ts`：新增 `resolveProductModelRoute(productModelId, mode, keys)` 与 `resolveAnyProductRoute`（按 product 遍历 mode 匹配）+ `suggestProductRouteBindings` + `getRoutedVideoModes`（按 `routeId` 反查 catalog）+ `isProductModelConfigured`。`PRODUCT_MODEL_CATALOG` 改为从 `tools/flovart/product-models.js` 的 `PRODUCT_MODEL_ENTRIES` 派生，TS 与 JS CLI 共享同一份产品模型身份源。
- `utils/modelRefs.ts`：`resolveModelSelection(value, keys, capability, requestedProvider?, submode?)` 返回 `{ routeId, provider, key }`；`keyOwnsBareModel` 走 `key.routeBindings`；产品模型路由按 `submode` 匹配，未传时回退到 `text-to-image`/`text-to-video`。
- `hooks/useApiKeys.ts`、`components/SettingsPanel.tsx`、`components/PromptBar.tsx`、`services/workflowGeneration.ts`、`services/workflowImageTools.ts`、`services/workflowOnlineAgent.ts`、`utils/usageMonitor.ts`、`App.tsx` 全量从 `upstreamModelId` / `modelMappings` / `resolved.model` 切换到 `routeId` / `routeBindings` / `resolved.routeId`；`SettingsPanel` 模型映射编辑器改为按 product × mode 网格平铺，每行独立配置。

### Route Capability Schema 与 Adapter

- `services/runningHubRouteCatalog.ts`：登记 17 条 `RouteCapabilitySchema`（6 图片 + 11 视频），导出 `getRouteSchema(routeId)` / `isVerifiedRoute(routeId)` / `getRouteDurations(routeId)` / `getRouteCatalog()` / `resolveRouteIdByDocId(docId)`（由 catalog 的 `officialEvidence` URL 在模块加载时反查 doc ID → routeId）。
  - 图片 6 条：`youchuan/text-to-image-v81`（悠船 v8.1 文生图，参考图走 `imageUrl` 单数，必填 `hd:false`）+ 5 条 `rhart-image-*`（n-pro/edit、g-2/image-to-image、n-g31-flash/image-to-image、n-g31-flash/text-to-image、g-2/text-to-image）。
  - 视频 10 条：3 条 `rhart-video-v3.1-fast`（t2v/i2v/start-end-to-video，按官方文档标 `generateAudio` 为不支持）+ 2 条 `rhart-video-g`（i2v/t2v）+ `rhart-video-g-official/reference-to-video`（3 张参考图）+ 4 条 `rhart-video/sparkvideo-2.0`（i2v/t2v/multimodal/fast-i2v，多模态最多 9 图 3 视频 3 音频）。
- `services/aiGateway.ts` 新增 `buildRunningHubRoutePayload(schema, prompt, fields, media, userParams)` 按 schema `params[].field` 适配 RunningHub 字段名，`buildRouteMediaFields` 负责参考图数组 / firstFrameUrl / lastFrameUrl 的字段映射与裁剪，`snapDuration` 负责 duration 枚举值向最近的合法值靠拢。
- 图片调用点 (`aiGateway.ts` ~line 2580) 与视频调用点 (~line 3400) 把 PromptBar 当前 `aspectRatio / resolution / generateAudio / seed / returnLastFrame / webSearch / quality` 作为 `userParams` 传入；按 ADR-0008，userParams 只作用于 schema 中已声明 `field` 的参数，且用户值永远覆盖 schema 默认；未声明的参数静默丢弃。图片画质映射：`quality === 'hd' || quality === 'high'` → `hd: true`。
- `services/runningHubService.ts`：删除硬编码的 `RUNNINGHUB_API_DOC_ENDPOINTS`（13 条 endpoint → doc URL），`runningHubApiDocEndpoint` 改为调 `resolveRouteIdByDocId` 从 catalog 派生。

### 旧 regex fallback 路径清理（Option C）

按用户决策 Option C：删除 `buildRunningHubStandardPayload` 中整段 regex fallback（原 line 1515-1587），未在 Route Catalog 中登记的 RunningHub 路由直接 `throw new Error('RunningHub 路由 ${modelEndpoint} 未通过验证（未在 Route Catalog 中登记），暂不可执行。请先在 Route Catalog 中补齐该路由的 Capability Schema 后再使用。')`，阻塞在 Provider emit 之前。

- `resolveRunningHubVideoDurations` 简化为 catalog-only：`return getRouteDurations(model) ?? []`，非 catalog 视频路由返回空数组（无法执行，不再撒谎返回估算时长）。
- 删除仅被 fallback 路径使用的 10 个死 helper：
  - 常量：`DEFAULT_RUNNINGHUB_PROMPT_FIELD`、`DEFAULT_RUNNINGHUB_IMAGE_PAYLOAD`。
  - 工厂：`runningHubStandardModelDefaults`（含 12+ 个路由特化 regex）、`RUNNINGHUB_STANDARD_MODEL_DEFAULTS` 常量表。
  - 模型判定：`isRunningHubVeo31Model` / `isRunningHubVeo31LowPriceModel` / `isRunningHubVeo31ReferenceModel` / `isRunningHubVeo31LiteModel` / `isRunningHubOmniModel` / `isRunningHubVideoSModel` / `isRunningHubNodeFieldModel`。
  - 字段判定：`runningHubAspectRatioField` / `runningHubReferenceFields` / `runningHubMultimodalFields`。
- 保留仍被 `getCapabilityDictionary`（aiGateway.ts line 273-279）使用的 `isRunningHubSeedance20Model` 和 `isRunningHubVideoEndpoint`，不删除。
- `BUILTIN_RUNNINGHUB_MODELS` 保留所有非 catalog 路由（kling/vidu/wan/skyreels-v4/rhart-video-s/rhart-video-v3.1-pro，以及 lite-official 的图生/首尾帧模式等），用户在 Workflow/设置页仍可看到它们，但点击生成会被 Provider emit 前的 catalog 校验拦截并抛"未通过验证"。Lite official 文生视频已经登记并实测；其余非 catalog 路由的 Capability Schema 扩登记仍作为后续单独项目。

### CLI 与产品模型身份统一（Blocker 7）

- 新增 `tools/flovart/product-models.js` 作为 JS CLI 与 TS catalog 共享的单一身份源：`PRODUCT_MODEL_ENTRIES` 16 条 + `listProductModelEntries(capability)` + `findProductModelEntry(id)`。
- `tools/flovart/agent-kit.js` `listAgentModels` 改为从 `listProductModelEntries` 派生；`diagnoseSeedance2` / `defaultPreferences` / `providerSnapshot` 默认模型改为 `flovart:gpt-image-2` / `flovart:seedance-2` / `gemini-3-flash-preview`；`SEEDANCE2_MODELS` → `SEEDANCE2_PRODUCT_IDS` + `SEEDANCE2_ALIASES`。
- `tools/flovart/shadow-runtime.js` `createProviderState` 与 `buildProviderReadiness` 默认模型同步改用产品模型 ID；`kling-v2` 失配置回退到 `flovart:seedance-2`。
- `tools/flovart/core.js` `provider.select-model` summary 和 help 文本改用产品模型 ID 格式。

### 验收与测试

- 17 条 Route Contract Test 全量通过，覆盖每条 Route 的 capability/modes/aspectRatio/durations/resolution/refs 上限/`params[].field` 默认值与 `officialEvidence` 的 doc ID 映射。
- `tests/aiGatewayValidation.test.ts` 69 测试全量通过。其中 9 个原非 catalog 路由的测试已重写为 `rejects.toThrow('未通过验证')` + `expect(fetch).not.toHaveBeenCalled()`，断言 Provider emit 前阻塞生效：
  - `rhart-video-v3.1-pro/image-to-video`、`rhart-video-v3.1-fast-official/image-to-video`、`rhart-video-v3.1-pro-official/reference-to-video`、`skyreels-v4/omni-reference-fast`（t2v/i2v）、`rhart-video-s/text-to-video`（duration snap / durationSec 20 两个 case）、`rhart-video-v3.1-lite-official/start-end-to-video`、`rhart-video-v3.1-fast-official/reference-to-video`。
- `workflowGeneration.test.ts` 26 测试通过（`mappedMediaKey` 为所有 Product Model mode 自动生成 route binding，与 Blocker 3 一致）。
- `flovartAgentKit.test.js`、`flovartProviderRouting.test.ts`、`shadowRuntime.test.ts`、`modelRefs.test.ts`、`productModelCatalog.test.ts` 全部通过。
- `npx tsc --noEmit` 已收口为 0 错误；同时修正 xAI Provider 字典、图像比例联合类型、Workflow 导入校验、LocalForage 实例类型和测试回调契约。

### 未在本轮范围

- 非 catalog 路由的 Capability Schema 扩登记（kling-v2 / vidu / wan / skyreels-v4 / rhart-video-s / rhart-video-v3.1-pro，以及 lite-official 的图生/首尾帧模式等）按用户决策推迟为单独项目。
- 用户已批准并完成 RunningHub Lite 文生视频真实 Smoke/Evaluation：4/4 个镜头成功，Provider task ID 与价格预览已记录；图生视频付费 Smoke 仍未执行。
- `flovartLibtvCompat.test.ts` 已改为当前 Workflow-only Canonical Command Registry 契约，旧 `node.create` / `upload` / `project.create` / `element.create` 兼容期望已删除。

### 待用户验证要点

- ① 在设置页为 `GPT Image 2` 或视频产品模型配置一条 Route Binding 后，选中该模型触发一次生成，抓包确认 RunningHub 收到的 payload 字段与官方文档对应（` Builds Route Payload` 按 schema 适配字段名，没有再走 regex 兜底）。
- ② 禁用或删除某条 Route Binding 后，PromptBar 生成按钮变未就绪，强行触发应在 Provider emit 之前拒掉（catalog 校验拦截，不会发起 fetch）。
- ③ 选一个未在 Catalog 中登记的 RunningHub 路由（例如 `legacyNama的 skyreels-v4/omni-reference-fast` 或 `rhart-video-s/text-to-video`），点击生成，应立即看到 `未通过验证（未在 Route Catalog 中登记）` 错误，浏览器 Network 面板无 `/task/openapi/*` 请求。
- ④ CLI/MCP 从产品模型 ID 选模型 → 浏览器 runtime 与 `modelPreference.v1` 同步 → 再次查询返回同一产品模型 ID；CLI 默认模型 `flovart:gpt-image-2` / `flovart:seedance-2` / `gemini-3-flash-preview` 与 SettingsPanel 默认一致。

## 图生图修复 + 序数 @ 命名 + 保留参考图原始比例（本轮新增）

### 修复项

- **图生图被退化为文生图**：`PromptBar` 之前只为视频模式渲染 submode 切换按钮，导致图像模式始终走 `text-to-image`，RunningHub `rhart-image-g-2/text-to-image` 路由的 `media: []` 使参考图被整体丢弃。本轮新增：
  - `services/productModelCatalog.ts` 新增 `IMAGE_MODE_ORDER`、`getRoutedImageModes(productModelId, keys)`、`routedImageSupportsImageToImage(productModelId, keys)`、`explainUnsupportedImageMode(productModelId, mode, keys)`，与视频侧 helpers 对齐。
  - `components/PromptBar.tsx` 新增 `routedImageModes` useMemo、图像 submode 工具栏图标按钮（`generationMode === 'image' && routedImageModes.length > 1`）、图像 submode 面板块（按 `IMAGE_MODE_ORDER` 渲染 `文生图/图生图`，未支持时禁用并展示 `explainUnsupportedImageMode` tooltip）；`availableModes` 自动回退同样纳入图像分支。
  - `services/workflowGeneration.ts` 自动切换 `productMode` 到 `image-to-image`（已连参考图 + 模型支持 + 路由已配置时），新增图像路由校验、`routeReferenceLimits` 扩到 `mode === 'image'`，新增 `mode === 'image'` 参考图处理块（image-to-image 要求 ≥1 张图片参考并标记 `slotRole: 'reference_image'`，text-to-image 清空 refs）。
- **`@` 引用升级为稳定资产绑定**：连接面板仍可用 `图片1/视频1/音频1/文本1` 作为当前节点内的快捷序数，但富文本 mention 同时保存来源节点 ID 与素材库 `assetId`，Provider 编译优先使用用户可见的语义别名（例如 `@角色1`），不再把全部别名强制改写为 `@图片1`。
  - 复制 Prompt 时同时写入可读纯文本、HTML 与 `application/x-flovart-prompt+json` 结构化剪贴板数据；结构化数据不携带图片 Base64。
  - 粘贴到同一 Workflow 的新节点时自动连接原来源节点；跨 Workflow 且素材仍在个人素材库时按 `assetId` 重建引用节点。只有纯文本时，按“已连接序数 → 画布节点名 → 素材名”优先级做确定性匹配。
  - 同名候选或一个别名对应两个不同资产时不猜测：冲突项保留为普通 `@文字` 并显示“未能唯一匹配引用”提示。
  - `workflowGeneration.ts` 从富文本文档提取 `nodeId → @别名`，将真实媒体 URL/Blob、语义别名、Provider 输入序号和 `slotRole` 一起交给 Provider Adapter；`aiGateway.ts` 的绑定段明确要求不同角色、主体、首尾帧或风格参考不得互换。
- **保留参考图原始比例**：新增全局开关 `WorkflowGenerationConfig.preserveReferenceAspectRatio`（图生图/图生视频都生效）。开启后，`workflowGeneration.ts` 会根据第一张图片参考节点的 `naturalWidth/naturalHeight` 计算最接近的支持比例并覆盖 `config.aspectRatio`，候选集来自 `capability.aspectRatios`（过滤掉 `adaptive`）。
  - `components/workflow/types.ts` 新增 `preserveReferenceAspectRatio?: boolean` 字段。
  - `components/PromptBar.tsx` 在「更多」面板新增「参考图」分区，仅当 `generationMode ∈ {image, video}` 且 `imageReferenceChips.length > 0` 时显示，复用现有 ON/OFF 按钮风格。
  - `WorkflowNodePromptBar.tsx` 通过 `patchConfig` 双向同步该开关到 `config.preserveReferenceAspectRatio`。

### 待用户验证要点

- ① 选中图像节点 + 连接 1+ 张图片参考 + 模型为 `flovart:gpt-image-2`：工具栏应出现图像 submode 按钮；默认自动切换到「图生图」；点击生成抓包 RunningHub `rhart-image-g-2/image-to-image`，payload 中 `imageUrls` 数组按 chip 顺序包含参考图，未再退化为 `text-to-image`。
- ② 在 PromptBar 输入或粘贴 `@角色1`：复制到另一个节点后仍显示 `@角色1` mention，并自动建立到同一来源节点的连线；Provider 收到的绑定段为 `- 图片1 = @角色1...，slot=reference_image`，请求中同时包含真实参考图。
- ③ 仅粘贴纯文本 `@角色1向左走`（别名后无空格）：素材库中名称唯一时自动创建并连接引用节点；存在两个同名素材时不自动选择，并显示未匹配提示。
- ④ 分两行输入 `@角色1执行动作A` 与 `@角色2执行动作B`：抓包确认两张真实参考图、两个不同语义别名与各自 Provider 序号同时存在，未退化为只有提示词文字。
- ⑤ 切换模型到一个条仅支持 `text-to-image` 的产品（如 `imagen-4`）：submode 面板的「图生图」应被禁用并显示有意义的 tooltip；强行不允许切到图生图。
- ⑥ 打开「保留参考图原始比例」开关 + 连接一张宽高比非标准（如 3:2）的参考图：生成请求 payload 的 `aspectRatio` 应自动落在最接近的支持比例（`3:2` 或 fallback），与用户在 PromptBar 顶部比例选择中的值不同；关闭开关后恢复使用 PromptBar 选中比例。
- ⑦ 图生视频场景下打开此开关，等价行为也应生效（解析第一张图片参考节点的原始宽高）。

## Provider 模型能力、分镜双 Prompt 与 Base URL 删除修复

### 修复项

- **模型与 Provider 不再混成假模型名**：模型列表保留每个模型自身的 `text/image/video` 能力；模型映射下拉按单模型能力过滤，不再把同一个 Provider 下的所有模型复制到图片、视频和文本线路。下拉标签只展示产品模型或模型 ID，Provider 作为独立线路信息展示。
- **分镜图片/视频 Prompt 完全独立**：`ScriptShot` 分别保存 `imagePromptOverride` 与 `videoPromptOverride`；AI 分镜拆解、编辑器和批量生图/生视频都使用各自字段，不再复用同一条图片 Prompt。
- **Base URL 全选删除不再触发工作区快捷键**：设置页和首次引导页的 Base URL 输入框隔离键盘事件，`Ctrl+A` 后按 `Delete` 只清空输入值，不会传到 Workflow 的全局删除处理。

### 待用户验证要点

- ① 新增一个同时声明文本、图片、视频能力的自定义 Provider（例如 Agnes），确认 `gpt-5.4` 只出现在文本模型映射；图片和视频下拉只出现各自能力模型，且不再显示 `gpt-5.4.anges` 一类拼接名称。
- ② 在同一分镜分别填写不同的生图 Prompt 和生视频 Prompt，执行「批量生图」与「批量生视频」，确认生成节点分别收到对应文本。
- ③ 在设置页和首次引导页反复执行 Base URL 全选、`Delete`、重新输入，确认配置页面保持挂载且无闪退。

## Production Runtime S1.1/S1.2 持久任务与真实视频生成

`runtime.test.delay` 继续验证客户端断开、桌面端重启和取消请求下的持久任务恢复；新增的 `generate.video --provider runningHub` 已完成首条真实 Provider tracer-bullet。视频生成已迁入 Production Runtime，旁白、字幕、混音和合成仍是评测目录中的本地后期脚本。

### 已实现

- Runtime 在现有本地 SQLite 状态库中使用独立 migration 和 `runtime_tasks`、`runtime_events`、`command_receipts` 表，不复用同步日志充当任务状态。
- 提交 `runtime.test.delay` 时，Task、首个 queued Event 与幂等 receipt 在同一事务中持久化；持久化失败或数据库繁忙时不得返回 accepted receipt。
- CLI/MCP 的 actor instance 在多个本地进程之间保持稳定；相同 actor + idempotency key + payload 返回同一 receipt，复用相同 key 但改变 payload 返回 `IDEMPOTENCY_CONFLICT`。
- 后台 Worker 通过租约认领任务，支持续租、过期后重启回收、完成写回和持久取消；客户端连接不是任务生命周期所有者。
- Canonical Command Registry 可执行 `runtime.test.delay`、`task.get`、`task.list`、`task.cancel`、`event.stream`、`provider.status` 与 `generate.video`；`generate.video` 支持 `google` / `runningHub` provider 选择。
- Control Plane 新增 Task REST 查询/稳定游标分页，以及支持 `Last-Event-ID` 和 task filter 的 SSE Event 快照。当前客户端通过快照结束后重连实现续读，不应宣称为单连接无限推送。
- RunningHub Adapter 使用 Desktop 系统凭据，不向 CLI 返回明文 Key；Veo 3.1 Lite 在提交前调用价格预览，随后持久化 Provider task ID、轮询事件与下载后的 Artifact。
- 真实 VOX 风格评测已完成 4/4 个 8 秒镜头，提交前预估合计 ¥10.24，顺序生成总耗时 13m 24.837s；本地合成为 30.000 秒、1280×720、H.264/AAC、-16.1 LUFS 的中文成片。预估费用不等同于最终扣费。

### 自动验证

- Rust Runtime 全量测试通过：Control Plane、契约、持久账本、租约恢复与取消均通过。
- Runtime/CLI 定向 Vitest 18 项通过，`npx tsc --noEmit` 通过。
- 本次增量验证通过：`cargo test --lib` 4/4、`runtime_contract` 5/5、`runtime_ledger` 5/5、`runtime_recovery` 3/3；RunningHub/Registry 定向 Vitest 225/225，`npm run build` 通过。
- 项目全量 `npm test` 当前为 598 项通过、1 项跳过、6 项失败；失败均位于本轮未修改的 `tests/workflowGeneration.test.ts`，表现为生成执行器未被调用，不属于 S1.1 Runtime 测试。
- 本轮环境未启动桌面 Runtime，因此未执行依赖真实 discovery record 的 CLI 端到端 smoke test。

### 待用户验证

1. 启动桌面端后执行 `npm run flovart:cli -- runtime.test.delay --delay-ms 1000 --idempotency-key demo-delay-1 --json`，保存返回的 `taskId`。
2. 用 `npm run flovart:cli -- task.get --task-id <taskId> --json` 查询状态；再用 `task.list` 验证稳定分页。
3. 执行 `npm run flovart:cli -- event.stream --after-event-id 0 --task-id <taskId> --json`，确认返回事件有单调递增的 ID；把最后一个 ID 作为新的 `--after-event-id` 时不得重复返回旧事件。
4. 用相同 idempotency key 和相同参数再次提交，应返回同一 receipt；保持 key 不变但修改 `delay-ms`，应返回 `IDEMPOTENCY_CONFLICT`。
5. 提交较长 delay 后关闭并重启桌面端，等待旧租约过期后确认任务被恢复并最终完成；运行中调用 `task.cancel` 时确认取消状态与事件可在重启后继续查询。
6. 播放 `tools/flovart/evaluations/vox-history-1776/vox-history-1776.mp4`，确认画面、中文字幕、旁白和混音可接受；到 RunningHub 控制台核对四个 Provider task ID 的最终扣费是否与 ¥10.24 预估一致。
