# 首次生成渐进配置并确认实际范围

用户可以先编辑现有 Workflow，或在已实现的插件入口选择素材；需要生成时才配置 AI 服务并确认本次输入、模型与费用。设计中的新插件入口未交付前不能出现在已完成功能列表。

费用授权由执行代码校验，调用方提交 confirmed 字段不代表已批准。Agent 工具权限与生成费用分开；已确认范围不重复索取授权，输入/费用范围变化再确认。

提交前检查实际参考模式与参数，保存 Provider 任务身份后查询；重启先恢复原任务，不盲目重提。Fake Provider 用于协议测试，真实生成质量、收费和取消另外验证。单步效果不要求先建立复杂授权对象树。
