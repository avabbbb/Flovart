# 制作任务本地执行，云端只分发 Production Skill

Flovart V1 将 Provider 凭据、生成任务、媒体素材和成片渲染留在用户本机，云端 Hub 只负责 Production Skill 的注册、版本、审核和分发。这样可以延续本地优先与 BYOK 边界，避免云端过早承担密钥托管、媒体带宽、跨租户执行隔离和生成成本；如果未来提供托管制作，应作为独立执行模式重新决策，而不是改变本地运行语义。
