# Workflow、Table 与 Agent 各自持有工作区状态

Workflow 负责生成编排，Table 负责独立节点式媒体处理，Agent 提供对话、任务、上下文与产物协作。它们共享素材与已实现能力，不共享图节点/历史，不恢复旧 Canvas/Art。

跨工作区只交接明确素材引用或显式导入内容，不做实时双写。原生效果的参数/关键帧归宿主工程，任务/素材归本地服务；首个效果不要求三张画布先全部完成，也不强制增加抽象 Authority Port。
