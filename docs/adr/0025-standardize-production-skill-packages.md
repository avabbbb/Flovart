# 复用现有 Skill 包

Operation Skill 说明如何通过已实现的 CLI/MCP 工具操作 Flovart；Production Skill 是制作配方，复用现有 SKILL.md 与 flovart.skill.yaml，按需携带参考 Workflow、参数、示例和验收标准。新效果不另建配方格式或强制编译为多层 ProductionSpec。

包版本与公开内容固定，不含凭据，不直接执行 Provider 请求或任意代码。带代码的原生效果/Agent 适配是单独安装的插件，不把 Skill 变成通用插件内核。只发布经过实际工具与结果验证的能力。
